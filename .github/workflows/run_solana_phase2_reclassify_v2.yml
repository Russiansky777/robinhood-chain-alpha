name: "Solana buyer_200 -- пересчёт Фазы 2 v2 (многоминтовый фикс) -- ЖДЁТ снятия лимита Dune"

on:
  workflow_dispatch: {}
  push:
    branches:
      - claude/nifty-sagan-r0polg
    paths:
      - "data/p3_guard_cache/SOLANA_PHASE2_RECLASSIFY_V2_REQUEST"

jobs:
  reclassify_v2:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - name: Git identity
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install dependencies
        run: pip install requests
      - name: "Пересчёт Фазы 2 v2 (многоминтовый фикс)"
        env:
          DUNE_EXPLORER_API: ${{ secrets.DUNE_EXPLORER_API }}
        run: python -u analysis/solana_phase2_reclassify_v2.py
      - name: Commit results
        if: always()
        run: |
          git rm -f --ignore-unmatch data/p3_guard_cache/SOLANA_PHASE2_RECLASSIFY_V2_REQUEST
          [ -e data/solana_phase2_raw_rows_v2.json.gz ] && git add data/solana_phase2_raw_rows_v2.json.gz
          [ -e data/solana_phase2_events_v2.json ] && git add data/solana_phase2_events_v2.json
          [ -e data/solana_phase2_reclassify_v2_comparison.json ] && git add data/solana_phase2_reclassify_v2_comparison.json
          git diff --cached --quiet && exit 0
          git commit -m "Solana buyer_200: пересчёт Фазы 2 v2 (многоминтовый фикс) -- результат [automated]"
          for i in 1 2 3 4 5; do
            git push && exit 0
            echo "push rejected, попытка $i/5 -- git pull --rebase и повтор"
            git pull --rebase origin "${GITHUB_REF_NAME}"
            sleep 3
          done
          echo "не удалось запушить после 5 попыток" >&2
          exit 1
