name: "Фандинг -- полный исторический бэкфилл Lighter x Hyperliquid, 13 пар (только чтение, публичные API)"

on:
  workflow_dispatch: {}
  push:
    branches:
      - claude/sc1-pons-v2-launcher-efdbyt
    paths:
      - "data/p3_guard_cache/FUNDING_HISTORICAL_BACKFILL_REQUEST"

jobs:
  backfill:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - name: Git identity
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install dependencies
        run: pip install -r analysis/requirements.txt
      - name: Полный бэкфилл (только чтение, публичные API)
        run: python -u analysis/funding_historical_backfill.py
      - name: Commit results
        if: always()
        run: |
          git rm -f --ignore-unmatch data/p3_guard_cache/FUNDING_HISTORICAL_BACKFILL_REQUEST
          [ -e data/p3_guard_cache/funding_historical_backfill_result.json ] && git add data/p3_guard_cache/funding_historical_backfill_result.json
          [ -d data/funding_historical_cache ] && git add data/funding_historical_cache/
          git diff --cached --quiet && exit 0
          git commit -m "Фандинг: полный исторический бэкфилл, 13 пар [automated]"
          for i in 1 2 3 4 5; do
            git push && exit 0
            echo "push rejected, попытка $i/5 -- git pull --rebase и повтор"
            git pull --rebase origin "${GITHUB_REF_NAME}"
            sleep 3
          done
          echo "не удалось запушить после 5 попыток" >&2
          exit 1
