name: "Bloom: суточный доклад и живая сверка с DBot"

# Доклад считается НЕ на хосте, а на бегунке: журналы копируются сюда
# только на чтение, и сопоставление с записями follow идёт ключом DBot,
# который на бегунке уже есть. Так на хост не надо везти ни ещё один
# модуль, ни ключ, ни сеть до DBot.
#
# Только чтение: ни одна служба не трогается, ни один ордер не уходит.
on:
  workflow_dispatch:
    inputs:
      rows:
        description: "строк в таблице слотов"
        required: false
        default: "15"
      since:
        description: "начало окна UTC, например 2026-09-23T00:00:00Z (пусто -- всё)"
        required: false
        default: ""
      dbot:
        description: "'yes' -- сопоставить с записями follow у DBot"
        required: false
        default: "yes"
      tasks:
        description: "какие задачи DBot сверять"
        required: false
        default: "BATCH-5,BATCH-3"
      find_code:
        description: "коды через запятую: показать записи журнала целиком"
        required: false
        default: ""
      find_sig:
        description: "подписи через запятую: показать записи журнала целиком"
        required: false
        default: ""
      stand_rows:
        description: "сколько последних решений стенда печатать целиком"
        required: false
        default: "10"

concurrency:
  group: bloom-report
  cancel-in-progress: false

env:
  HOST: 45.59.170.76
  STATE_DIR: /home/bot/bloom_executor_live_data

jobs:
  report:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          ref: claude/nifty-sagan-r0polg

      - name: "Самопроверка доклада ДО его построения"
        run: |
          set -e
          cd analysis
          python3 bloom_report.py --self-test

      - name: "SSH key"
        env:
          VPS_SSH_KEY: ${{ secrets.VPS_SSH_KEY }}
        run: |
          set -e
          [ -n "$VPS_SSH_KEY" ] || { echo "VPS_SSH_KEY не задан"; exit 1; }
          mkdir -p ~/.ssh && chmod 700 ~/.ssh
          printf '%s\n' "$VPS_SSH_KEY" > ~/.ssh/vps_key
          chmod 600 ~/.ssh/vps_key
          echo "SSH_OPTS=-i $HOME/.ssh/vps_key -o ConnectTimeout=15 -o BatchMode=yes -o StrictHostKeyChecking=accept-new" >> $GITHUB_ENV

      - name: "Копия журналов с хоста (только чтение)"
        run: |
          set -e
          mkdir -p /tmp/bloom_state/helius_usage
          for f in decisions.jsonl positions.jsonl detector_status.json seller_heartbeat.json seller.jsonl; do
            if ssh -n $SSH_OPTS root@$HOST "test -s $STATE_DIR/$f"; then
              ssh -n $SSH_OPTS root@$HOST "cat $STATE_DIR/$f" > "/tmp/bloom_state/$f"
              echo "$f: $(wc -c < /tmp/bloom_state/$f) байт"
            else
              echo "$f: нет или пуст на хосте"
            fi
          done
          # Осколки учёта кредитов -- по одному файлу на службу и день.
          OSKOLKI=$(ssh -n $SSH_OPTS root@$HOST "ls -1 $STATE_DIR/helius_usage 2>/dev/null || true")
          for f in $OSKOLKI; do
            ssh -n $SSH_OPTS root@$HOST "cat $STATE_DIR/helius_usage/$f" > "/tmp/bloom_state/helius_usage/$f"
            echo "учёт: $f"
          done
          ls -l /tmp/bloom_state

      - name: "Журнал сторожа: последние записи в файл отчёта"
        run: |
          set -e
          # В лог прогона он тоже печатается, но лог живёт недолго и его
          # неудобно читать частями. Файл коммитится вместе с докладом.
          if [ -s /tmp/bloom_state/seller.jsonl ]; then
            tail -60 /tmp/bloom_state/seller.jsonl > data/bloom_seller_tail.jsonl
            echo "--- журнал сторожа, последние записи ---"
            cat data/bloom_seller_tail.jsonl
          else
            echo "журнал сторожа пуст или не скопирован" > data/bloom_seller_tail.jsonl
            cat data/bloom_seller_tail.jsonl
          fi

      - name: "Диагностика сторожа: признак жизни и строки позиций"
        run: |
          set -e
          python3 analysis/bloom_seller_diag.py --self-test
          python3 analysis/bloom_seller_diag.py --state-dir /tmp/bloom_state \
            --out data/bloom_seller_diag.json

      - name: "Доклад"
        env:
          DBOT_API_KEY: ${{ secrets.DBOT_API_KEY }}
          BLOOM_STATE_DIR: /tmp/bloom_state
        run: |
          set -e
          ARGS="--rows ${{ inputs.rows }} --stand-rows ${{ inputs.stand_rows }}"
          if [ -n "${{ inputs.find_code }}" ]; then
            for C in $(echo "${{ inputs.find_code }}" | tr ',' ' '); do
              ARGS="$ARGS --find-code $C"
            done
          fi
          if [ -n "${{ inputs.find_sig }}" ]; then
            for S in $(echo "${{ inputs.find_sig }}" | tr ',' ' '); do
              ARGS="$ARGS --find-sig $S"
            done
          fi
          if [ -n "${{ inputs.since }}" ]; then ARGS="$ARGS --since ${{ inputs.since }}"; fi
          if [ "${{ inputs.dbot }}" = "yes" ]; then
            ARGS="$ARGS --dbot --tasks ${{ inputs.tasks }} --config data/final/20260923T145755Z/konfig.json"
          fi
          echo "аргументы доклада: $ARGS"
          python3 analysis/bloom_report.py $ARGS --out data/bloom_report.txt

      - name: "Коммит доклада"
        if: always()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          for f in data/bloom_report.txt data/bloom_report.json \
                   data/bloom_seller_tail.jsonl data/bloom_seller_diag.json; do
            [ -f "$f" ] && git add "$f"
          done
          git diff --cached --quiet && { echo "нечего коммитить"; exit 0; }
          git commit -m "Bloom: суточный доклад [automated]"
          for i in 1 2 3 4 5; do
            git push origin HEAD:claude/nifty-sagan-r0polg && exit 0
            git pull --rebase -X theirs origin claude/nifty-sagan-r0polg
            sleep 3
          done
          exit 1
