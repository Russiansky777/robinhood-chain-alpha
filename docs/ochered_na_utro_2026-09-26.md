# Очередь на утро 26.09 и НОЧНОЙ ПЛАН 25->26.09

> ПРИ ПОТЕРЕ КОНТЕКСТА ЧИТАТЬ ЭТОТ ФАЙЛ ПЕРВЫМ (поручение владельца 25.09,
> 23:0xZ). Здесь полный ночной план с состоянием каждого пункта; статус
> обновляется после каждого пункта.

## Рамки ночи (слово владельца 25.09)

* Торговый путь на хосте (детектор, полоса, сторож, пул, env) НЕ трогать до
  утра. Всё остальное -- код без деплоя, расчёты по журналам и цепи.
* ИСКЛЮЧЕНИЕ, одно (23:0xZ): **тёплый nonce (I.7)**. Как только правка готова и
  самопроверка денежного пути пройдена (устаревший nonce -> отказ; после нашей
  отправки nonce инвалидирован) -- деплой детектора с keep_env=yes, ОДНА живая
  сделка 0.01, проверить по журналу "решение -> отправка" и что позиция
  закрылась. Меньше 30 мс -- оставить; что-то не так -- откатить тем же
  деплоем. В утреннюю таблицу колонки "до"/"после" по времени и по доле S+0.
* Тяжёлые прогоны -- на облачном бегунке или на хосте под nice с потоковым
  чтением; детектор не замедлять. Кредиты Helius на ночные расчёты -- до
  150 000 (на 22:45Z израсходовано за сутки 425 204 из 1 000 000).
* Каждая правка -- отдельный коммит под своим флагом, со своей самопроверкой
  (только денежный путь). Утром деплой по одной, каждая -- одна живая сделка
  0.01, потом следующая.
* Проверки стопа (порог: пар >= 50 ИЛИ сумма балансов <= 1.54 SOL) и часовые
  таблицы идут своим чередом и ночью не останавливаются.
* Порядок работ: таблица пар -> I.1 -> I.2 -> II.7 -> II.14 -> II.8 -> I.3-6 ->
  II.9-10 -> III.12, III.14 -> II.11.

> УТРЕННИЙ ПАКЕТ (очередь деплоя, числа, риски) -- отдельным файлом:
> `docs/utrenniy_paket_2026-09-26.md`. Читать его вторым после этого.

## Состояние ночных пунктов (обновляется по ходу)

| пункт | что это | состояние на 23:0xZ |
|---|---|---|
| таблица пар | все пары с 20:07Z строками | **сделано**, 26 пар, отдано владельцу; сводка к утру в II.7 |
| I.1 | настоящий стоп по деньгам: учёт realized_sol, рабочий BLOOM_DAILY_LOSS_SOL, ДВА файла KILL, состояние переживает ротацию | **код готов, не деплоен** (коммиты 7d63244, 6e8c2fa): учёт итога вшит в update_position (ровно один раз на позицию, отметка pnl_counted), новый файл KILL_BLOOM останавливает только площадку, positions() читает ротированный .gz через кэш. Самопроверки денежного пути 116/116; сторож 136, полоса 252, детектор 467, исполнитель 109 -- зелёные. Осталось на утро: стопы групп полосы по «несчитаемым» (I.4 рядом) |
| I.2а | Meteora DAMM v2 (cpamdp...) -- сборщик из c2_swap_build в полосу, min-out по резервам, byte-for-byte по 20 покупкам | не начато |
| I.2б | pump.fun bonding curve (6EF8rrec...) -- новый сборщик по публичной документации, byte-for-byte по 20 покупкам, min-out по кривой | не начато |
| I.2в | инструкция 33e685a4017f83ad: какая программа, доля BUY-решений, берёт ли полоса | не начато |
| I.3 | немедленное переподключение при обрыве, запасной только через 5 с, окна 120 с нет | **уже в коде и живёт** -- проверено по журналу (обрыв 20:50:47Z, "переподключаюсь сразу") и по коду (СРОК_ПОДЪЁМА_ОСНОВНОГО_S=5.0, признак жизни fallback_window_s 5.0). Готовить нечего |
| I.4 | итог полосы без количества -- по возврату и входу; "несчитаемых" не оставлять | **сделано** (коммиты 3696a4a и 78ef7a4). Количество в формуле итога не участвует: вход и возврат -- это SOL. На ночных данных суточный счёт полосы меняется с -0.119224 (старое правило, один расход) на -0.058050 SOL, непосчитанных не остаётся. Две оговорки, обе из живых случаев: общий минт с другой позицией полосы (продажа по остатку кошелька -- в счёт только расход) и отклонённая цепью покупка (вход и чаевые не уходили: 26.09 00:04:23Z списано 0.001005, а не 0.002005 и не 0.051005). Самопроверки 123/123 |
| I.5 | отчёты: одна строка на продажу, одна формула возврата (разница 0.001489), полоса по всем группам в признаке жизни, имя источника в Telegram | **формула сведена к одной** (коммит 82c3bea): суточный счёт полосы и счёт состояния зовут `ST.итог_позиции` и `ST.расход_отправки`; прежнее второе написание пропускало несчитаемые пары целиком и считало чаевые даже у отклонённой транзакции. В отчёт добавлено `no_result_today`. Про "разницу 0.001489": в журнале это позиция 20:54:58Z -- возврат по цепи 0.001489702 при котировке 0.0000012 SOL (токен обесценился, вернулась в основном не цена). Остальные три части I.5 (строка на продажу, группы в признаке жизни, имя источника в Telegram) -- не начаты |
| I.6 | ротация журнала решений | **не нужна**: на хосте уже стоит logrotate (сутки уезжают в decisions.jsonl.1.gz в 22:00Z). Вместо неё -- чтение .gz в расчётах: **сделано** для таблицы полосы и выборки из журнала; остальные прогоны (block_position, journal_stats, bloom_report) -- утром |
| I.7 | время линии в режиме nonce; причина; правка | **замер сделан**: решение->отправка медиана 65.6 мс, p90 80.9 (19 покупок, все nonce, по 6 вариантов); сборка 0.56 мс; остаток 64.9 мс. Причина -- НЕ подпись (разбор ключа 0.021 мс, PDA 0.010 мс), а сетевые вызовы за nonce в горячем пути. Правка тёплого nonce -- **сделана и ЗАДЕПЛОЕНА** 23:20:40Z (коммит 3e0f10b, деплой 36200499275, keep_env=yes): значение читают часы рядом с тёплым blockhash, путь сделки берёт готовое. В признаке жизни 23:29Z: обновлений 42, взят тёплым 2, по сети 0, возраст 10.5 с. Самопроверки 252/252. Числа «после» по «решение -> отправка» ждут первой покупки полосы: с 23:20Z было 2 BUY, и оба отказались на сборке по типу пула |
| II.7 | пары: доля в одном слоте, кто раньше, медиана разницы мест, по отправителям и группам, вывод строкой | **сделано** на всех 40 парах ночи (20:07-00:39Z): в одном слоте 37 (92.5%), мы раньше 21 (52.5%), Bloom раньше 19; медиана разницы мест -2 в нашу пользу (среднее +1.1); S+0 у нас 12, у Bloom 11. По отправителям (наших раньше/всего, медиана): astralane 9/15 (-7), nozomi 5/10 (-6), blockrazor 5/9 (-16), helius 2/5 (+12), zeroslot 0/1 (+4). По группам: speed_only 36 пар (19 раньше, -2), lane_only 2 (-35), bloom_lane 2 (-74). **Вывод: полоса не хуже Bloom**. Гейт A на тех же 40 парах (`analysis/lane_pairs_gate.py`): S+0 полоса 12, Bloom 11; в цели (S+0 в любом месте или S+1 с местом <=100) полоса 18, Bloom 15; медиана разницы сдвига (наш S+N минус Bloom S+N) 0 -- один слот в 37 парах из 40, на слот позже мы в 1, на слот раньше в 2 |
| II.8 | A3 на всех 346 сигналах: 214 непосчитанных, варианты выхода, сверка сумм | **сделано, 107 из 214 закрыты**. Разбор причин: 107 -- неизвестен налоговый статус минта, 96 -- нет spot_after (тип пула), 11 -- нет точки роста. Первая причина была пробелом справочника, а не данных: добор 173 минтов из цепи (`analysis/a2_mint_tax_fill.py`, 173 вызова, 21 с, 104 таксируемых, 153 Token-2022) закрыл её целиком. A3 S+1 t28.8: посчитано 132 -> 239 из 346, сумма 6.750 -> 16.047 SOL, среднее 0.0511 -> 0.0671, доля в плюс 0.599 -> 0.665; вердикт "край есть" на всех четырёх входах, ноль вне доверительного интервала. Сверка сумм сходится точно: сумма и среднее по строкам A1 совпадают со сводкой до шестого знака, половины окна дают 143+96=239 и 11.865+4.182=16.047. Остаток 107 -- настоящий пробел данных, варианты выхода ниже |
| II.9 | продажа по закрытым сделкам с 24.09: время от таймера до посадки, проскальзывание, комиссии, выгода своей сборки | **сделано** (`analysis/sell_timing.py`, прогон `run_vps_sell_timing_nl.yml`): 74 продажи с подтверждением по цепи (73 полоса, 1 Bloom). **Таймер опаздывает: медиана 9.6 с, p90 279 с, макс 3212 с** при сроке удержания 28.8 с. Проскальзывание к котировке: медиана +14.8 %, p90 +17.4 %, мин -15.1 % (66 продаж; одна обесценившаяся исключена). Комиссия сети продажи: медиана 0.0000077 SOL. Своей сборки продажи нет вовсе -- все 73 продажи полосы через Jupiter, так что сравнивать пока не с чем; "до подтверждения" везде ровно 15000 мс -- это шаг опроса сторожа, а не время сети, и оно ни о чём не говорит |
| II.10 | лидер Beqv6: выходы, копировщики, "313"(а) | **сделано частично, по уже собранному кэшу толпы** (127 его сделок за 7 суток): копировщиков за 30 с -- медиана **86** разных кошельков, p90 287, макс 374 (по строгому правилу 2 -- медиана 0, макс 8); рост цены за 30 с -- медиана **x1.846**, за 60 с x1.876; его собственное место в блоке -- медиана 628; размер сделки -- медиана 8.99 SOL-экв, макс 89.2. **ВЫХОДЫ НЕ ИЗМЕРЕНЫ И ИЗ КЭША НЕ ИЗМЕРИМЫ**: полей про его продажи в кэше нет вовсе (проверено -- ни одного поля со sell/exit), нужен отдельный скан его подписей по цепи. Дизайн скана ниже. **Что такое "313"(а) -- из репозитория не восстанавливается**: числа 313 в этом смысле в документах нет, и придумывать его я не буду -- нужна одна строка от владельца, что имелось в виду |
| II.11 | фоном: C1/C2/C4, F (наши сделки через GP), G (StonkFun) | не начато |
| II.14 | география лидера слота: регион по getClusterNodes + гео, доли S+0/в цели по регионам, медиана "сел -> увидели"; регион узла Helius нашего WebSocket и его региональные точки | **сделано** (`analysis/leader_geo.py`, прогон `run_vps_leader_geo_nl.yml`). 207 покупок с 18:00Z, регион лидера известен у всех 207. **S+0 зависит от региона лидера, и сильно**: EU 57/141 = 40.4 %, US-East 5/30 = 16.7 %, Asia 2/19 = 10.5 %, US-West 0/15 = 0 %. В цели: EU 53.8 %, US-East 44.8 %, Asia 22.2 %, US-West 7.1 %. Это не случайность: при доле EU вероятность получить такое или меньшее равна 0.005 (US-East), 0.005 (Asia), 0.0004 (US-West); по "в цели" US-East не значим (p=0.22), Asia и US-West значимы. Слот полосы взят по цепи из таблицы полосы (в журнале его нет) -- добрано 64 из 64. Регион узла Helius -- шаг добавлен в прогон |
| III.12 | шреды: BlockRazor Shred Stream, DoubleZero Edge, Helius LaserStream -- протокол, регионы, задержка, дизайн зонда | **разбор и дизайн сделаны** -- `docs/shredy_i_regionalnaya_otpravka.md`, страницы сохранены в `data/docs/shreds/` (9 файлов, прогон `run_shred_docs_nl.yml`). Цены и требования по их же страницам: BlockRazor $50/поток/сутки и $500/мес, UDP, регионы Франкфурт/Амстердам/Токио/Нью-Йорк; LaserStream gRPC, 9 регионов (FRA, AMS, TYO, SG, EWR, PITT, SLC, LAX, LON), переигровка 48 часов; DoubleZero Edge $1500/мес за Амстердам (Лондон/Нью-Йорк/Сингапур/Токио $900, прочие $450), валидатор подписчику НЕ нужен, но нужны GRE, BGP, PIM и UDP 7733/44880 плюс заявка с одобрением. Зонд НЕ написан: ни одного числа опережения у нас пока нет, и заявленные площадками числа своими не считаются |
| III.14 | дизайн региональной отправки по расписанию лидеров + оценка выигрыша по II.14 | **дизайн сделан** -- три варианта в `docs/shredy_i_regionalnaya_otpravka.md`: оставить как есть, переключать адрес по региону лидера, второй узел отправки рядом с US-East. Реестр 54 региональных точек шести отправителей собран из сохранённой документации (`analysis/sender_regions.py`), замер срока до них с хоста -- прогон `run_vps_sender_region_rtt_nl.yml` (первый прогон померил одну точку из-за того, что curl с telnet:// съедал список через stdin; починено) |

## Находка ночи: расход покупки считался мимо платы за счета

Живая покупка 25.09 18:09:41Z, кошелёк полосы:

* ушло с кошелька по цепи: **0.053493440 SOL** (0.640856911 -> 0.587363471);
* по полям позиции считалось: 0.052005 (вход 0.05 + чаевые 0.001 + приоритет
  0.001 + тариф 0.000005);
* разница **0.001488440** -- плата за счета, которые создаёт сама покупка.

На 63 закрытых сделках ночи это примерно **0.09 SOL расхода, которого суточный
стоп не видел вовсе**. Больше, чем весь посчитанный итог полосы за ночь
(-0.058 SOL). Починено: раз транзакция покупки уже в руках у догона, в позицию
пишется нативная дельта кошелька, и итог считается по ней (-0.004855592 против
-0.003367152 по полям).

Вторая половина той же находки -- ПОЧЕМУ транзакции часто не было в руках.
Догон количества спрашивал узел про ПРИНЯТУЮ подпись, а садится один из шести
вариантов на nonce: 18:09:41Z принял zeroslot (4UV1iMzo...), а села 67Nt8E64...
Узел честно отвечал "нет такой", количество не добиралось, и пара помечалась
несчитаемой -- 61 пара из 63 за ночь. Теперь догон одним вызовом
`getSignatureStatuses` находит севшую среди всех вариантов.

Оба исправления -- в коммите 78ef7a4, НЕ задеплоены: деплой утром по одному,
как велено.

## II.10: что уже измерено про лидера Beqv6 и что нужно сканом

Измерено по уже собранному кэшу толпы (127 сделок лидера за 7 суток
17.09-24.09, `data/a2_crowd_metric_2026-09-24.json`):

| что | число |
|---|---|
| копировщиков за 30 с (разных кошельков) | медиана **86**, p90 287, макс 374 |
| то же по строгому правилу 2 | медиана 0, макс 8 |
| рост цены за 30 с | медиана **x1.846** |
| рост цены за 60 с | медиана x1.876 |
| его место в блоке | медиана 628 |
| размер его сделки | медиана 8.99 SOL-экв, макс 89.2 |

Вывод одной строкой: за ним в первые 30 секунд заходит под сотню кошельков, и
цена к 30-й секунде в среднем почти удваивается. Это и объясняет, почему всё
держится на S+0: на S+1 мы покупаем уже после того, как половина этого роста
случилась.

ЧЕГО НЕТ: его ВЫХОДОВ. Полей про продажи лидера в кэше нет ни одного
(проверено перебором всех ключей записей). Значит "продал ли он и когда" нельзя
сказать ни по одному сигналу, и горизонт "до первой продажи источника" в A3
пустой не случайно.

Дизайн скана выходов (на утро, по одобрению -- он стоит кредитов):
1. `getSignaturesForAddress` по его адресу за окно сделок, страницами по 1000.
2. Каждую подпись -- только если она НЕ в списке его покупок (покупки уже
   известны по кэшу), и только те, где по балансам его кошелька минт УМЕНЬШАЕТСЯ
   а SOL растёт: это и есть продажа. Никаких догадок по программам.
3. На каждый минт из 127 покупок: время первой продажи, доля проданного,
   цена исполнения (|дельта SOL| / |дельта минта|) и через сколько секунд после
   покупки.
4. Ответ: медиана "держал секунд", доля сигналов, где он вышел в первые 30 с
   (то есть где наша продажа по таймеру 28.8 с совпадает с его выходом), и
   сколько он на этом зарабатывал.
5. Цена: примерно 127 минтов x (1 запрос подписей + 1-3 getTransaction) --
   порядка 400-600 вызовов, то есть меньше тысячи кредитов.

## Находка ночи №2: 0.089 SOL лежат запертыми в пустых токен-счетах

Замерено по цепи (`run_bloom_token_accounts.yml`, кошелёк полосы
21DqHD...C9j7w): **59 токен-счетов, все пустые, ренты в них 0.089275920 SOL**.
Каждая покупка создаёт счёт минта, продажа его НЕ закрывает, и рента остаётся
запертой. На сделке 0.01 это 0.0015 SOL, то есть 15 % размера сделки, каждый
раз.

Как это связано с находкой №1. Недоучтённый расход измерен теперь не на одной
сделке, а на **14 севших покупках подряд** (18:09-19:01Z 25.09): по цепи с
кошелька уходило 0.0125-0.0140 SOL там, где учёт считал 0.011, то есть
**0.001488-0.003002 SOL мимо счёта, медиана 0.002990**. Из них 0.00149 (у
Token-2022 -- 0.00151) -- ровно рента того самого счёта, и она видна в кошельке
до сих пор. Остаток (у части сделок ещё около 0.0015) пока не разложен по
получателям -- так и сказано, а не приписано.

Что это значит в деньгах за ночь: 63 закрытых сделки полосы при медиане
0.00299 -- около **0.19 SOL расхода, которого суточный стоп не видел**. Честный
итог полосы за ночь тогда ближе к -0.25 SOL, а не к -0.058. Это ОЦЕНКА по 14
измеренным из 63; после деплоя правки (коммит 78ef7a4) число станет измеренным
на каждой сделке само.

Что можно вернуть сегодня: закрыть пустые счета и получить обратно 0.089 SOL.
Это ЗАПИСЬ в цепь ключом полосы -- ночью не делается, предложение на утро.

## II.8: что осталось непосчитанным и как это закрыть

После добора налогового статуса осталось 107 сигналов из 346. Это уже не
пробел справочника, а пробел измерения, и вот он весь:

**96 -- нет spot_after (цены сразу за сделкой источника).** Причина одна:
спот считается по остаткам хранилищ пула, а у пулов с сосредоточенной
ликвидностью остатки спота не дают. По программам: Raydium Launchlab 31,
Raydium CLMM 22, Meteora DAMM v2 18, Meteora DLMM 13, Dynamic Bonding Curve 2,
Pump.fun 2, Orca Whirlpool 1; ещё 7 -- неоднозначная котировка (в сделке
несколько встречных счетов, обычно USDC).

Варианты выхода, от точного к дешёвому:
 * (а) читать состояние пула на слоте источника и брать цену из него
   (sqrt-цена у CLMM/Whirlpool, активный бин у DLMM, кривая у DBC/Pump.fun).
   Самый точный путь и самый дорогой по работе: нужен разбор состояния для
   четырёх-пяти семейств программ.
 * (б) взять ценой "сразу за источником" цену исполнения ПЕРВОЙ ЧУЖОЙ сделки в
   том же пуле после транзакции источника. Это измерение по цепи, а не
   интерполяция; стоит примерно 96 запросов подписей по хранилищу пула плюс
   по одной-три getTransaction на сигнал -- порядка 400 вызовов, то есть
   копейки против ночного бюджета 150 000 кредитов. Смещение честное и
   известное: первая чужая сделка уже включает свой след.
 * (в) взять базой price_0 -- среднюю цену исполнения самого источника, она в
   кэше уже есть. Бесплатно, но смещает результат ВВЕРХ: средняя цена лежит
   ровно посередине между ценой до и после сделки источника, то есть занижает
   цену входа. Величина смещения измерена и равна наценке входа: +4.4 % на
   S+1 и +9.2 % на S+2.

**11 -- нет точки роста на горизонте.** Из них 10 -- "история хранилища в окне
не выкачана": закрывается запросом подписей по хранилищу пула в окне 30 с,
дёшево. Один -- "ликвидность пула снята до точки", и это НЕ пробел: выйти в
этой сделке было нельзя вовсе. Честный счёт для такого случая -- полный убыток
(вход плюс издержки), а не missing; пока он в missing, результат смещён вверх.

Оговорка к добору налога: ставка читается из минта НА МОМЕНТ ДОБОРА. Если
эмитент менял её после сделки, к сделке относится прежняя ставка; такие случаи
видны по полю `ставка_комиссии_прежняя_bps` в `data/a2_mint_tax_fill.json`.

## II.14: география лидера решает, попадём ли мы в S+0

Покупки с 18:00Z 25.09, регион лидера известен у всех 207. Доля S+0 падает с
удалением лидера от нашего узла в Нидерландах:

| регион лидера | покупок | S+0 | доля S+0 | в цели | вероятность такого при доле EU |
|---|---|---|---|---|---|
| EU | 141 | 57 | 40.4 % | 53.8 % | -- (опора) |
| US-East | 30 | 5 | 16.7 % | 44.8 % | 0.0050 |
| Asia | 19 | 2 | 10.5 % | 22.2 % | 0.0049 |
| US-West | 15 | 0 | 0.0 % | 7.1 % | 0.0004 |

Это прямой ответ на вопрос III.14: региональная отправка имеет смысл, и цена
вопроса видна числом -- на лидерах вне EU мы теряем S+0 почти целиком. Чего
пока НЕ измерено: где именно стоят точки входа отправителей (astralane,
nozomi, blockrazor, helius, zeroslot) по регионам и сколько даст переключение
на ближайшую к лидеру. Это и есть содержание III.14.

### Где стоит наша точка входа

Хост -- Амстердам (45.59.170.76, ExtraVM, EU). Оба имени Helius
(`mainnet.helius-rpc.com`, `atlas-mainnet.helius-rpc.com`) разрешаются в
АНКАСТОВЫЕ адреса Cloudflare, и гео по IP называет Торонто, Монреаль и Лондон
-- это место регистрации сети, а не узел, который нас обслуживает. Верить
этому нельзя, и поэтому померено время с самого хоста (5 замеров на имя):

| имя | TCP мин / медиана, мс | TLS мин / медиана, мс |
|---|---|---|
| mainnet.helius-rpc.com | 2.1 / 2.3 | 19.7 / 20.6 |
| atlas-mainnet.helius-rpc.com | 2.3 / 2.6 | 18.8 / 19.5 |

2 мс по TCP -- это точка Cloudflare в самом Амстердаме. Честная оговорка: это
срок до КРАЯ сети Cloudflare, а не до узла Solana за ним; сколько добавляет
сам узел, отсюда не видно.

Честная оговорка про "сел -> увидели": миллисекунд в журнале нет, оценка
считается как отставание слотов x 400 мс плюс возраст слота сети на момент
решения. Медианы по регионам (EU 134, US-East 106, Asia 57, US-West 160 мс) --
с точностью до дрожания длины слота, и порядок между Asia и EU по ним
объяснить нельзя: выборка Asia 19 покупок.

## Ночные находки, которые стоят денег (все проверены)

1. **Суточный стоп Bloom не срабатывает никогда.** Гейт в
   `bloom_exec_state.py:748` сверяет `realized_sol` с `BLOOM_DAILY_LOSS_SOL`, а
   писать `realized_sol` умеет только `add_pnl` (строка 618), которую в боевом
   коде не зовёт никто: вызовы только в самопроверке того же модуля (1001,
   1004). Счёт всегда 0.0, условие не выполняется. То же нулевое значение
   владелец видит в ответе `/status`.
2. **Ротация журнала обнуляет суточные счётчики полосы.**
   `ExecState.positions()` читает только текущий `positions.jsonl`, а logrotate
   в 22:00Z уводит сутки в `.1.gz`. В признаке жизни 22:25Z полоса показала
   `today: 0, pnl_sol: 0.0` после 25 покупок за вечер. Пределы (50 сделок,
   стопы -0.3/-0.5, потолок 1 SOL) считаются по пустому журналу, то есть
   каждую полночь по хосту стоп-лосс начинается с нуля. Позиция, открытая в
   момент ротации, исчезла бы из виду сторожа (сегодня открытых не было).
3. **Общий файл KILL глушит и полосу.** Полоса сверяется с
   `состояние.kill_active()` раньше своего `KILL_OWN_SEND`
   (`bloom_own_send.py:1323, 2359, 2477`). Отдельного выключателя только для
   Bloom нет -- утром развести на два файла (поручение владельца).


### Сделка 00:04:23Z по цепи (bloom_lane 0.05, blockrazor, S+1 101/827)

Подпись севшего варианта `4Gfakq...udmzE`, слот 450508547. В цепи она ЕСТЬ и
она ОТКЛОНЕНА программой пула: `InstructionError [7, Custom 6040]` --
`BuySlippageBelowMinBaseAmountOut` (pump-amm buy.rs:700), дали 53 364 679 424
при нашем минимуме 59 299 593 162. Покупки не было.

Списано с кошелька полосы `21DqHD...C9j7w` ровно **0.001005 SOL**: приоритет
1 000 000 лампортов плюс базовый тариф 5 000. Ни 0.05 входа, ни 0.001 чаевых
не ушли: отклонённая транзакция откатывает все переводы, платится только
тариф. Баланс 0.456146959 -> 0.455141959.

### Ночной стоп сработал (26.09, 00:46:04Z)

Порог владельца по деньгам: сумма балансов 1.520165 SOL <= 1.54 (-1.0 от
старта 2.539828 на 20:06:44Z). Пар к этому моменту 40 из 50, то есть первым
сработал денежный порог, как и ожидалось по темпу.

ЧИСЛО ПАР: 40, ЕДИНЫЙ СЧЁТ. Числа 33 (доклад 00:33Z) и 31 (проверки 00:46Z и
01:15Z) оба неверны, и причина одна: счёт шёл по времени суток строкой
(`"00:11" >= "20:07:00"` ложно), поэтому все девять покупок после полуночи
(00:04:23, 00:11:40, 00:20:18, 00:23:47, 00:25:10, 00:26:42, 00:29:54,
00:38:26, 00:39:15) отбрасывались, а 33 было промежуточным замером до части
вечерних. Счёт теперь один и только по полной метке UTC --
`analysis/lane_pairs_gate.py --since 2026-09-25T20:07:00Z`: покупок полосы 40,
у каждой есть пара Bloom, гейт A совпал со столбцом "в цели" таблицы на всех
40 строках (расхождение 0).

* Рубильник поставлен ПРОГОНОМ `run_vps_bloom_kill_nl.yml` (не деплоем),
  причина с числами записана в сам файл `/etc/bloom-executor/KILL`.
* Служба его видит: `kill_active: true`; в журнале решений появился код
  `KILL_SWITCH` -- 40 отказов вместо покупок.
* Балансы с момента стопа НЕ изменились ни на лампорт (1.520165439 в 00:44:38Z
  и в 01:10:29Z) -- это и есть доказательство, что рубильник настоящий.
* Продажи не остановлены (`KILL_SELL` нет), открытых позиций 0, зависших 0.
* ВАЖНО: общий KILL глушит и полосу -- отдельного файла для Bloom в живом коде
  ещё нет (код готов, коммит 7d63244, не деплоен).
* Прогон рубильника отметился как failure на проверке подтверждения: он ждал
  40 с, а пульс детектора около минуты. Рубильник при этом встал. Починено:
  ожидание до 120 с.

Итог ночи по деньгам: с 20:06:44Z до стопа ушло 1.0197 SOL за 4 ч 40 мин
(Bloom на трёх группах + полоса, 64 покупки полосы).

## САМОЕ ГЛАВНОЕ: стоп Bloom за сутки не работает вовсе

Найдено на часовой самопроверке 21:2xZ, проверено по коду, не догадка.

* Гейт есть: `bloom_exec_state.py:748` отказывает в покупке, если
  `-pnl()["realized_sol"] >= daily_loss_sol` (код КОД_ДНЕВНОЙ_УБЫТОК).
* Записывать в этот счёт умеет только `add_pnl` (строка 618). **В боевом коде
  её не зовёт никто**: во всём репозитории вызовы только в самопроверке того
  же модуля (строки 1001 и 1004). Ни детектор, ни сторож продаж, ни
  исполнитель не пишут `realized_sol`.
* Значит файл `pnl_ГГГГММДД.json` не создаётся, `realized_sol` всегда 0.0, и
  условие `0.0 >= 0.5` (или 1.0) НИКОГДА не выполняется. Величина
  `BLOOM_DAILY_LOSS_SOL` (в коде по умолчанию 1.0, деплой сторожа пишет 1)
  сегодня не значит ничего.
* Тот же нуль владелец видит в ответе на `/status`: строка "за сутки:
  покупок 0, продаж 0, результат 0.0 SOL" (bloom_detector.py:8744) всегда
  нулевая, и это читается как "потерь нет".
* Что у Bloom РАБОТАЕТ: рубильник KILL, автопаузы (5 ошибок API подряд, 3
  непроданных подряд, 429 дольше 5 минут), кулдаун по минту и проверка
  баланса. Последняя и есть фактический стоп: покупки прекратятся, когда на
  кошельке станет меньше `buy_sol + резерв` (~0.21 SOL), то есть почти при
  пустом кошельке, а не на -0.5.
* У полосы стопы РАБОТАЮТ, они считаются по позициям, а не по этому файлу
  (денежная группа -0.3, скорость -0.5, 5 упавших подряд, 1 открытая, 50 за
  сутки). Но у денежной группы 5 сделок из 6 сегодня "несчитаемые", поэтому и
  её стоп видит меньше настоящего убытка.

Утром: писать realized_sol там, где продажа уже разобрана (сторож знает
sol_delta_net), и только после этого проверять величину предела. До тех пор
ночной стоп Bloom существует лишь как рука владельца: `/kill` в Telegram
запрещает новые покупки без всякого деплоя, `/resume` снимает.

## Торговый путь (деплой утром)

1. **Обрыв подписки.** Немедленное переподключение transactionSubscribe;
   запасной logsSubscribe -- только если основной не встал за
   BLOOM_MAIN_REVIVE_S = 5 с; окно запасного с 120 с снижено до 5 с.
   Цена прежнего поведения измерена: из 398 469 сигналов за сутки 94 разобраны
   добором через getTransaction (+1-2 слота), и ВСЕ 94 пришлись на окна
   запасного пути. Готово и проверено (детектор 467/467).

2. **Учёт итога полосы без количества.** Сейчас пара без lane_bought_raw
   помечается "итог несчитаем", хотя деньги известны: вход задан нами, возврат
   прочитан по цепи с кошелька полосы. Считать итог как (возврат - вход -
   расход), когда продажа шла ВСЕМ остатком нашего отдельного кошелька и
   предыдущая позиция по этому минту закрыта в ноль. 25.09 так потерялись
   пять пар с реальным итогом +0.035 SOL.

3. **Срок ожидания количества -- в признак жизни сторожа.** Сейчас значения
   ждать_количество_s в признаке жизни нет, и проверить его на хосте можно
   только по поведению.

## Измерения и отчёты (без денег)

4. **Пропуски полосы (поручение владельца 25.09, п. 2).** 4 700 сигналов за
   сутки с инструкцией 33e685a4017f83ad и ~730 на пулах вне сборщика
   (Whirlpool, 6EF8rrec..., ojh19oja...): это покупки источников, которые
   полоса пропускает? Если да -- доля от всех BUY-решений и что нужно, чтобы
   полоса их брала. Ответ числом в утренний доклад.

5. **BAM: есть ли наши кошельки в потоке вообще (поручение 25.09, п. 2).**
   Журнал контроля (data/triton/preconfs_bam_20260925T193215Z.jsonl, 13 011
   транзакций за 19:32-19:37Z) хранит ТОЛЬКО подписи -- публичных ключей в нём
   нет. Поэтому сверка идёт так: getSignaturesForAddress по каждому из 85
   наших источников за то же окно (85 вызовов, дёшево), пересечение с 13 011
   подписями журнала. Число в утренний доклад.

6. **Ротация журнала решений.** decisions.jsonl на хосте 635 МБ и 440 тысяч
   строк; у деплоя детектора есть вход rotate_journals.

7. **Признак жизни показывает полосу только по денежной группе.** Детектор
   зовёт `состояние_полосы(positions)` без аргумента `группа`, поэтому в
   lane_state видно только ведро "деньги (все группы кроме скорости)".
   Суточный расход и стоп ГРУППЫ СКОРОСТИ (потолок 1 SOL, стоп -0.5) в
   признаке жизни не видны вовсе -- ночью их не проверить глазами. На деньги
   это не влияет: предел стоит в гейте покупки (`можно_ещё` получает группу и
   зовёт `потолок_группы`), проверено по коду 25.09 в 20:2xZ. Утром: отдать в
   признак жизни состояние по каждой группе отдельно.

8. **Устаревшие числа в двух строках описания bloom_own_send.**
   `потолок_группы` и `можно_ещё` в тексте описания всё ещё говорят "0.2 и
   0.15 SOL" -- это числа до слова владельца 25.09 (вечер). Константы верные
   (ПОТОЛОК_СУТОК_СКОРОСТИ_SOL = 1.0, СТОП_УБЫТОК_СКОРОСТИ_SOL = 0.5) и
   признак жизни показывает 1.0/0.5, то есть поведение правильное, врёт
   только текст. Правка описания -- вместе с п. 7, отдельного деплоя не
   стоит.

9. **SC1 launch tracker: потолок 30 минут убьёт каждый прогон.** На рабочей
   ветке у job стоит `timeout-minutes: 30` (поставлен после зависания
   24.09), а нормальный прогон занимает ~2 ч 50 мин (00:24->03:12,
   06:21->09:15, 18:20->21:19 -- все success). На ветке-носителе потолка ещё
   нет, поэтому сейчас прогоны идут. Как только носитель получит эту версию,
   трекер начнёт падать по таймауту. Утром: поднять потолок до 4 часов ИЛИ
   разобраться, почему честный прогон идёт три часа, и ускорить -- но не
   оставлять 30 минут.

### Как ночь защищена до починки (слово владельца 25.09, 21:3xZ)

* Прогон `run_vps_bloom_kill_nl.yml` (новый, облачный, только файл
  `/etc/bloom-executor/KILL`, служб не касается, продажи не трогает):
  `mode=status` проверено живьём в 21:36Z -- KILL нет, KILL_BY_TELEGRAM нет,
  KILL_SELL нет, служба видит `kill_active: false`.
* Две получасовые проверки (площадка не даёт чаще часа на одну рутину):
  `trig_019u6CdNcduE5J65xUWwHE9t` в :44 и `trig_01VeRY9Xe9jmVNZVw6qK4azt` в
  :14. Каждая считает пары с 20:07Z и сумму балансов 4s87RRC2 + 21DqHDDP;
  порог -- пар >= 50 ИЛИ сумма <= 1.54 SOL (-1.0 от старта 2.5398; владелец поднял
  порог с -0.75 до -1.0 в 21:5xZ), при
  пересечении ставит KILL прогоном и пишет строку в Telegram.
* ЧЕГО НЕ ХВАТАЕТ В КОДЕ: отдельного выключателя только для Bloom. Файл KILL
  общий: полоса сверяется с ним раньше своего KILL_OWN_SEND
  (`bloom_own_send.py:1323, 2359, 2477`), поэтому рубильник остановит и её.
  УТРОМ (прямое поручение владельца 25.09, 21:5xZ): развести на ДВА файла --
  свой KILL для Bloom и свой для полосы, чтобы остановка одного не глушила
  другое.

### Утром первым пунктом (поручение владельца): учёт realized_sol

Звать `add_pnl` из сторожа продаж и из исполнителя:
* сторож знает `sol_delta_net` по каждой продаже -- это `realized_sol`;
* исполнитель знает потраченное на покупку -- это `spent_sol`, плюс счётчики
  `buys`/`sells`;
* после этого `BLOOM_DAILY_LOSS_SOL` начнёт работать, и строка `/status`
  перестанет врать нулями.
Регрессия: отдельная самопроверка на то, что после разобранной продажи
`pnl()["realized_sol"]` меняется, и что гейт КОД_ДНЕВНОЙ_УБЫТОК закрывает
покупки при пределе. Деплой -- при нуле открытых позиций.

### BAM: ответ на вопрос "есть ли наши кошельки в потоке" (сделано 25.09, 21:4xZ)

Считано локально, без хоста: журнал контроля
`data/triton/preconfs_bam_20260925T193215Z.jsonl` (11 317 транзакций, 120
слотов, 19:32:20-19:36:59Z) против хвоста журнала решений
`data/triton/decisions_tail.jsonl` (он как раз доходит до 19:37:05Z).

* Транзакций наших источников в окне: 98 (16 источников, 83 слота); из них в
  слотах, где BAM-ams отдавал preconf -- **13**; из этих 13 пришли в поток
  **0**.
* Только покупки источников: 35 в окне, в BAM-слотах 6, в потоке 0.
* Наших подписей в потоке вообще (без условия на слот): 0.

Вывод: BAM-ams отдаёт транзакции тех же слотов, но ни одной нашей -- это не
ошибка нашего фильтра по слотам, а отсутствие наших кошельков в том, что
отдаёт узел. Harmonic не открывать.

## Снято ночью (сделано, не нужно утром)

* Двухшаговая тень: кэш ног выключен в коде (BLOOM_SHADOW_LEGS_OFF=1 по
  умолчанию), бюджет 0. Причина: 2 сигнала из 41 977 за сутки.
* Зонд BAM: расписания у него нет и не было, никакой прогон его не зовёт --
  "снять" означает просто не запускать; Harmonic не открывать.
