# Стенд Bloom: контролируемая проверка детектора и исполнителя

Смысл стенда — не ждать органических сигналов, а получить все нужные
случаи по заказу, с кошелька владельца, и сверить ожидаемое с фактическим
по подписям.

**Тестовый источник:** `DNJeTYni5QQVCwNNt1SrHu1GjYR4ak5Zw7LeuFsKYmXX`
(кошелёк владельца). Порог входа у него **0.05 SOL-эквивалента**, все
остальные фильтры — как у BATCH-5.

**Изоляция.** Каждое решение по этому источнику несёт `test_source: true`.
Такие сигналы **не входят** в живую сверку с DBot, в счётчик 30 сигналов
и в пары А/Б — они идут отдельным разделом журнала (`by_code_test_source`)
и отдельным разделом отчёта.

**Порядок покупок владельца:** по одной, с паузой **не меньше 60 секунд**,
с сообщением времени каждой. Сверка идёт по подписям в цепи.

---

## Проход A — dry-run: все 10 пунктов

Исполнитель подключён, но `BLOOM_LIVE_BUY` и `BLOOM_LIVE_TEST` не заданы:
ни один POST в Bloom не уходит, пишутся только решения и намерения.

| # | Покупка владельца | Ожидаемый код | Что пришло | слот наш − слот источника | время ответа Bloom | продажа |
|---|---|---|---|---|---|---|
| 1 | Fomo, USDC, свежий для него токен, прямой пул к SOL, без налога | `BUY` | | | — (dry-run) | — |
| 2 | Fomo, USDC, тот же токен ещё раз | `SKIP_TARGET_INCREASE_POSITION` | | | — | — |
| 3 | Fomo, USDC, токен Token-2022 с налогом, прямой пул есть | `BUY` + `taxed: true`, `tax_bps` | | | — | — |
| 4 | Pump.fun (Bloom / Photon / pump-портал), SOL, токен на кривой | `BUY`, программа pump в `dex_programs` | | | — | — |
| 5 | Терминал (Photon/Axiom), SOL, токен на Raydium или Meteora | `BUY`, программа пула | | | — | — |
| 6 | Jupiter, SOL, токен только с пулом к USDC | `INTERMEDIATE_ROUTE` | | | — | — |
| 7 | Покупка ниже порога 0.05 | `TARGET_AMOUNT_OUT_OF_RANGE` | | | — | — |
| 8 | Продажа любого из купленных | `NOT_A_BUY` | | | — | — |
| 9 | Две покупки подряд с разницей в секунды | оба `BUY`, дедуп не путает | | | — | — |
| 10 | Перевод токена себе с другого кошелька без покупки | `TOKEN_RECEIVED_NOT_BOUGHT` | | | — | — |

**Правило прохода A:** любое расхождение ожидаемого с фактическим —
разбор до начала прохода B. Не «посмотрим потом».

---

## Проход B — live-test на 0.01 SOL: пункты 1, 3, 4, 5, 9

`BLOOM_LIVE_TEST=1`. Покупки только по тестовому источнику; сигналы
реальных источников в этом режиме идут **исключительно в журнал** (код
`LIVE_TEST_SKIP_REAL_SOURCE`). Лимиты, оба рубильника и сторож действуют
полностью.

Размер — **0.01 SOL**. Если Bloom отвергнет тело по минимуму
(`INVALID_REQUEST` со ссылкой на минимум), делается **один** повтор на
**0.02 SOL**, и первый код ошибки идёт в доклад. На любом другом коде
повтора нет: 200 у Bloom означает «принято», и слепой повтор способен
купить второй раз.

| # | наша подпись | слот по цепи | позиция в блоке | t получения → t вызова Bloom → t ответа | слот посадки | авто-ордер 28.8 с | сторож |
|---|---|---|---|---|---|---|---|
| 1 | | | | | | | |
| 3 | | | | | | | |
| 4 | | | | | | | |
| 5 | | | | | | | |
| 9 | | | | | | | |

**Первый вызов прохода B — это и есть тестовый вызов с
`target_value: 28.8`.** При `INVALID_REQUEST` на нём переходим на `29` —
без потерь, это цена одного запроса.

---

## Гейт live на реальных источниках

Все четыре условия одновременно:

1. все пункты A и B совпали с ожиданием;
2. ноль ложных покупок;
3. все тестовые позиции закрыты;
4. сверка при старте (`bloom_reconcile`) чистая.

Первый live — размер **0.05 SOL**. Переход на 0.2 — при **30 органических
сигналах** с «DBot купил, мы нет» = 0.

**Оговорка, которую нельзя терять:** пары А/Б при 0.05 SOL идут в зачёт
**слотов**, но не в разницу результата — размер входа другой, и сравнивать
выручку с DBot на 0.5 SOL было бы подменой. В отчёте это помечается.

---

## До старта стенда — проверено по цепи

| что | порог | чем проверяется |
|---|---|---|
| нет чужой активности на `4s87RRC2…` | ни одной подписи, которой мы не делали | `bloom_reconcile`, `getSignaturesForAddress` |
| баланс кошелька | ≥ **0.3 SOL** (владелец пополнил на 0.35) | `bloom_reconcile`, `getBalance` |
| рубильники | читаются службой **от имени `bot`** и выключены | `bloom_reconcile`, `kill_readable` |
| позиции dry-run | отложены с меткой | `bloom_reconcile --archive` |
| журнал | не смешан из двух форматов | `bloom_reconcile` |

Отказ узла при проверке чужой активности **не** считается её
отсутствием: результат «неизвестно» блокирует старт так же, как найденная
чужая активность.
