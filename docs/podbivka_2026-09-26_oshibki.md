# Подбивка 26.09: ошибки, найденные по ходу

Правило владельца (26.09 вечер): найденное по ходу — в этот список; проход (а)
по 543 кошелькам не перезапускается, чинится после первого полного результата.

| # | что | как нашлось | состояние |
|---|---|---|---|
| 1 | Ключ gRPC-фида (`GRPC_FEED_TOKEN`) — не ключ RPC Shyft: 401 Invalid API key | проба 14:36Z | владелец добавил `SHYFT_API_KEY` |
| 2 | Shyft отдаёт транзакции только ~2.5 суток (с 23.09 вечера); `getFirstAvailableBlock` ~6.5 ч назад | проба глубины по дням | правило: с 24.09 — Shyft, старше — Helius |
| 3 | Shyft не принимает `before` с чужой подписью (0 подписей) | та же проба | история пула на Shyft листается от вершины (предел 150 страниц) |
| 4 | `maxSupportedTransactionVersion: 0` → `-32015 version (1) not supported`: в пробе пропадало до 89 % окна | диагностика покрытия | в модулях подбивки-2 версия 1; **код контроля `podbivka.py` остался на 0** — вероятная причина 15 отказов «узел: RuntimeError» в контроле (контроль принят, не трогал) |
| 5 | Курс USD/SOL «из самой сделки» брал чужое плечо у маршрутов лидера; медиана часа — тоже | 1б: DBot купил на 500–1471 USD, наш отбор — «ниже 2 SOL-экв» | с 24.09 — курс детектора (GeckoTerminal) из истории `data/vps_health_nl.txt`, ряд `data/podbivka/kurs_sol_usd.json`; старше — опорный пул через Helius. Проход (а) идёт уже на этом курсе; порог дополнительно пересчитывается пост-фактум при сборке таблиц |
| 6 | Режим «цена свопа» (DLMM и др.) брал цену из пылевой транзакции: +2·10¹¹ п.п. | проба на 10 кошельках | точка цены — своп с котировкой ≥ 0.01 SOL, скачок > 1000× от S+0 отбрасывается с флагом |
| 7 | Маркер запуска не изменился → план его не видит, задача молча не стартует | 1б не посчитался в прогоне 3 | маркер меняется при каждом перезапуске |
| 8 | Pump AMM: выдача 0.77–0.997 от x·y=k при полной раскладке сборов по событию (LP 20, протокол 5, создатель 85–95 б.п.) | 7 сохранённых транзакций | не объясняется по цепи; калибровка по продажам, строка «необъяснённое» в отчёте |
| 9 | У лидера 96 % первых покупок — в пулах с котировкой «другое» (не SOL/USDC): симулятор 0.5 SOL даёт отказ | 1б | в отчёте долей; сверка чисел 1б только на 7 парах |
| 10 | Шаги прогона, ходящие на боевой хост (выгрузка сделок, чтение журнала по ssh), заблокированы и запрещены владельцем | классификатор автозапуска | «факт» Bloom/полосы — только из выгрузок в репозитории (296 сделок, итог по SOL у 46); сторож 429 — по `vps_health_nl.txt` через git (обновляется ~раз в час) |
| 11 | Покупка ниже порога в прогоне не сохраняется (только счётчик `ниже_порога`) — пост-фактум их не вернуть | правило пост-фактум пересчёта | ограничение прохода (а); в отчёте счётчиком |
| 12 | Курс по опорному пулу SOL/USDC (эпоха Helius) в минуту 19.09 21:44Z дал завышенное значение: покупки лидера на 796 и 1194 USDC вышли «ниже 2 SOL-экв», DBot их купил | 1б на курсе детектора (19:26Z) | не чинено (правило: после первого полного результата); кандидат — курс детектора и для эпохи Helius, если ряд удастся продлить назад, или сверка опорного курса с соседними минутами |
| 13 | Скользящая глубина Shyft: контроль 18:46Z уже не получил 6 сделок 24.09 ~00:07 (было 80 сверено → 75) | повтор контроля на версии 1 | грань «сейчас − 2.4 суток» для всех новых прогонов; проход (а) идёт на старой грани — недоданные Shyft покупки первых часов окна → «досчитать» |
| 14 | `podbivka.факт_покупки` читал слот до проверки «узел не отдал» — на версии 1 пустой ответ ронял контроль | повтор контроля | исправлено одной строкой |
