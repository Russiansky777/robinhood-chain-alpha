# Sprint R1 — ретро-тест RWA-конвергенции (сток-токены): пре-регистрированный дизайн

**Зафиксировано ДО просмотра данных: 2026-09-01.** Раздел §2 ниже
вставлен дословно из промпта штаба на запуск Sprint R1 и после коммита
Шага 0 **не редактируется**, кроме дополнения подраздела «Механика»
внутри §2.1/§2.3 (праздники/укороченные сессии NYSE, фактическая дата
конца покрытия, decimals-проверка — сама механика может дополняться
находками разведки; критерии решений §2.7/§2.8 — нет). Полный текст
промпта (§0, §1, §3–§5) приведён ниже §2 для самодостаточности
репозитория (см. `docs/COST_POSTMORTEM.md`, ревизия 7) — эти разделы
операционные, не заморожены.

Закрытые линии (не трогать): копитрейдинг (Sprint 1+1.5, вердикт НЕТ);
graduation-momentum (Sprint G1, вердикт KILL) — лонг-стратегии по
мем-токенам этого чейна закрыты как класс решением штаба 2026-09-01.
См. `docs/PROJECT_STATE.md`.

## 2. Пре-регистрированный дизайн

### 2.1 Гипотеза и период
H1: в часы, когда базовый рынок США закрыт, ончейн-цены сток-токенов систематически отклоняются от справедливого якоря, и покупка значимых дисконтов даёт положительную чистую доходность к моменту после открытия рынка. Период: 01.07.2026 00:00 UTC → конец покрытия минус 48ч (фиксируется на Шаге 1). Закрытые часы: вне 13:30–20:00 UTC пн–пт, плюс праздники/укороченные сессии из Шага 1.

### 2.2 Вселенная и фильтр ликвидности (только прошлая информация)
Токен входит во вселенную на неделе W, если за предыдущие 7 дней: ≥100 DEX-сделок И ≥$10 000 объёма. Пересчёт еженедельно.

### 2.3 Чекпоинты, якорь, события
Чекпоинты: каждый целый час UTC в закрытые часы. Цена P(i,t) = VWAP сделок в (t−30м; t]; валидно при ≥3 сделках и ≥$500. Якорь F(i,t) = последнее значение Chainlink-фида на момент ≤ t. Девиация D = ln(P/F). Событие-дисконт: D ≤ −θ, θ ∈ {1%, 2.5%, 5%}. Неперекрытие: после события по токену при данном θ следующие чекпоинты этого токена пропускаются до разрешения самого длинного горизонта. Премии (D ≥ +θ) фиксируются для отчёта, в вердикте не участвуют.

### 2.4 Вход, выходы, издержки
Вход: VWAP в (t; t+30м]; нет сделок → void (долю в отчёт). Горизонты: 4ч; 12ч; «открытие+1ч» = VWAP в (открытие+30м; открытие+90м] ближайшей следующей сессии. Основной — «открытие+1ч». r = ln(exit/entry) − c; сценарии c: 0.5% / 1.5% (базовый) / 3%.

### 2.5 Колонки выгрузки (строка на событие)
token, feed_addr, t_checkpoint, theta, D, n_trades_pre, vol_usd_pre, entry_vwap, exit_4h, exit_12h, exit_open1h, void_flags, week_id. Плюс контроль §2.6 тем же форматом.

### 2.6 Контроль
Случайные валидные чекпоинты с |D| ≤ 0.5% (та же вселенная, те же горизонты), объём — не меньше числа событий при θ=1%. Назначение: отделить конвергенцию дисконта от общего дрейфа сток-токенов.

### 2.7 Статистика
Ячейки: 3 θ × 3 горизонта = 9. Ячейка допущена при N≥50. В допущенных: односторонний знаковый тест медианы r>0 при базовых издержках, BH по 9 ячейкам, α=0.05. Секондари: 5%-усечённое среднее с бутстреп-CI (10 000). Робастность: половины периода; сценарии издержек; сравнение с медианой контроля на том же горизонте.

### 2.8 Правила решения (заморожены)
- **KILL**: ни одна допущенная ячейка не значима по BH при базовых издержках (при наличии ≥1 допущенной).
- **GO → R2**: есть ячейка с BH q<0.05 И медианой нетто ≥ +1% И медианами обеих половин >0 И превышением медианы контроля того же горизонта минимум на +1%.
- **GRAY**: значимость есть, нарушено любое условие GO → решение владельца, дефолт закрыть.
- **UNDERPOWERED**: нет допущенных ячеек (все N<50).

### 2.9 Эксплораторно (на вердикт не влияет)
Премии/шорт-сторона (при существовании перп-рынка на тикер); разрез SPY/QQQ vs одиночные акции; уикенд vs будняя ночь; свежесть фида на момент события; доля USDG-квотных пулов.

### 2.10 Ограничения (обязательны в отчёте)
Фид вне часов рынка может замирать/быть stale — дисконт может отражать реальные новости (гэп-риск), частично контролируется контролем и выходом после открытия; тонкая ликвидность закрытых часов = слиппедж хуже ретро-VWAP; MEV; юридическая доступность живой торговли сток-токенами для владельца (резидент ЕС) — проверяется отдельно ДО любого лайва; decimals/нормировка фидов и пулов — явный тест на смоуке.

---

## Полный текст промпта штаба (2026-09-01, операционные разделы, не заморожены)

### 0. Контекст и рамки
Модель исполнения: Sonnet 5, effort обычный. Аккаунт Dune: текущий (Analyst-резерв не подключать без команды владельца).

- Паспорт — источник правды. Закрытые линии (копитрейдинг; graduation-momentum) не трогать; лонг-стратегии по мем-токенам закрыты как класс решением штаба 01.09.2026.
- Наследуются ВСЕ правила G1: граница по block_time в каждом запросе к трейдам/логам (регрессионный тест обязателен); калибровка узким срезом ×2.5 и партиционирование при проекции >40 на запрос; binding-гард на чтения (метаданные до оплаты); наружу только агрегаты; кэш `data/sprintR1_cache/` + коммит после каждой платной операции; 2x-стоп только при факте ≥25 кредитов.
- Бюджет: 100 кредитов, namespace `sprintR1`, принудительный гард. Пул от якоря ~284.

### 1. Порядок исполнения

**Шаг 0 — Пре-регистрация (ДО любого платного вызова).** Создать `docs/R1_DESIGN.md` с §2 дословно; заметка в README с датой заморозки; конфиг `sprintR1` (кап 100, кэш). Коммит + пуш. Только после этого — Dune.

**Шаг 1 — Разведка (≤15 кредитов, максимум бесплатного).**
- Бесплатно из docs.robinhood.com: реестр сток-токенов и адреса их Chainlink-фидов (страница Stock Tokens). Запиновать в репо с источником.
- Дёшево из Dune: (а) по каким сток-токенам есть DEX-сделки и в каком объёме; (б) профиль объёма по часам недели — торгуются ли они в закрытые часы; (в) частота обновлений фидов (AnswerUpdated по адресам фидов) — обновляются ли фиды вне часов рынка или замирают. Всё агрегатами, с границами по времени.
- В R1_DESIGN.md раздел «Механика» (дополняемый): праздники/короткие сессии US-рынка в периоде (сверить с публичным календарём NYSE), фактическая дата конца покрытия.
- Гейт разведки: ≥15 токенов проходят фильтр §2.2 И ≥50 сделок в закрытые часы за период суммарно. Меньше → стоп, отчёт в штаб.

**Шаг 2 — Смоук (один уикенд из середины периода, ≤15 кредитов).** Полный пайплайн: чекпоинты → девиации → события → входы/выходы → агрегаты. Проверки: разумность девиаций (не сотни процентов из-за децималей/нормировки!), доля пустых entry-окон, факт vs калибровка.

**Шаг 3 — Полный прогон (≤50 кредитов),** партиции по неделям, строка на событие (§2.5) + контроль (§2.6).

**Шаг 4 — Анализ локально (0 кредитов):** §2.7, таблица θ×горизонт, контроль, половины периода.

**Шаг 5 — Отчёт и вердикт §2.8 в `docs/RESULTS.md`, леджер, ограничения.**

### 3. Смета
Разведка ≤15 · Смоук ≤15 · Полный ≤50 · Резерв 20 · Кап 100 (гард, exit при превышении).

### 4. Запрещено
Менять §2 после Шага 0 (кроме «Механики»); запросы без временных границ; сырые строки наружу; переносить издержки/пороги из G1 задним числом; трогать закрытые линии; LLM в горячем пути.

### 5. Формат финального ответа
Вердикт §2.8; таблица лучшей ячейки (медианы по сценариям, q, N, половины, контроль); леджер (оценка/факт); пути артефактов; отклонения с причинами.

Возвратные точки: гейт разведки провален; вердикт готов. Между ними — автономно.

### Про отбор по кошелькам (владелец, при передаче задания)
Колонок под скоринг кошельков (n_scored_wallets_pre/n_hunters100_pre — фичи G1) в выгрузке R1 нет; проверка идеи «умные кошельки → хорошие токены» здесь потребовала бы отдельного платного запроса (~20–35 кредитов). Решение владельца: отложить до вердикта R1 — если потребуется, отдельный дизайн G1b с замороженными критериями пишется отдельно, не задним числом внутри R1.

## Механика (дополняется на Шаге 1)

**Заполнено 2026-09-01 по факту разведки** (`analysis/r1_recon.py`,
`data/sprintR1_cache/r1_schema_drilldown_*.csv`,
`r1_columns_probe_*.csv`, `r1_stock_token_deployments_*.csv`,
`r1_feed_activity_*.csv`, `r1_universe_trades_*.csv`; свободная часть —
`analysis/r1_scrape_stock_tokens.py`, `data/sprintR1_cache/
r1_stock_tokens_*.json`). Стоимость Шага 1: 3.53 из бюджета ≤15 (см.
кредитный леджер ниже).

**Реестр сток-токенов.** Бесплатная часть (docs.robinhood.com) дала
описание живого read-only REST API `https://api.robinhood.com/rhj/assets`
(реальные примеры адресов в документации, подтверждено сверкой) — но
интерактивная песочница агента блокирует egress на docs.robinhood.com/
docs.chain.link на уровне сетевого прокси (403 на CONNECT, org policy,
не WebFetch-специфично — проверено `curl` напрямую). Рабочий обходной
путь — тот же скрипт запускается через GH Actions runner (обычный
исходящий интернет, без ограничений инструмента). Финальный источник —
ончейн, надёжнее любого стороннего API: таблица
`rwa_stock_factory_robinhood.factory_deployer_evt_deployed`
(декодирована Dune, один UUPS-фабричный контракт
`0x4783c67b63de2b358ac5951a7d41f47a38f3c046`) даёт **102 задеплоенных
сток-токена** за июль-август (symbol, name, адрес токена, uid) —
сверено выборочно с примерами из `/rhj/assets` (CRM/Salesforce адрес
совпал побайтово).

**Реестр Chainlink-фидов.** `docs.chain.link` страница про
Robinhood-фиды рендерит таблицу адресов клиентским JS-компонентом
(`Astro`-остров `FeedList`, `client="idle"`) — данные подгружаются в
рантайме, недоступны простым HTTP GET ни в SSR HTML, ни в статическом
JS-бандле (проверено: 0 адресов в полном тексте страницы, 0 URL API в
загруженном бандле — вероятно, дальнейший динамический `import`).
Вместо гонки за клиентским виджетом — ончейн-путь, дешевле и надёжнее:
Dune уже декодировал ABI `dualaggregator` (`chainlink_robinhood`
схема, реализует `AggregatorV3Interface` + OCR) для ВСЕХ
Chainlink-агрегаторов на чейне через одну логическую таблицу
`dualaggregator_evt_answerupdated` (contract_address различает
инстансы). За июль-август найдено **31 активный фид** (5670..47
обновлений на фид за период). Прямой связи токен→фид в декодированных
таблицах нет (`factory_deployer_evt_deployed` не содержит адрес фида);
сопоставление по символу через `description()`-вызов фида (например,
через Alchemy RPC, `config.alchemy_rpc_url`) — задача Шага 2/3, не
блокирует смоук.

**Часы рынка и праздники (WebSearch, из нескольких публичных
источников про NYSE-календарь 2026, независимо от заблокированных
доменов).** В период 01.07–31.08.2026 единственная релевантная
закрытая сессия сверх регулярного WEEKEND/будни-вне-13:30-20:00 UTC —
**пятница 03.07.2026, ПОЛНОЕ закрытие** (перенос Independence Day,
04.07.2026 — суббота). Ранних закрытий (13:00 ET) в этот период нет
(ближайшие — день после Дня благодарения и канун Рождества, оба в
ноябре-декабре, вне периода R1). В расчётах чекпоинтов 03.07.2026
целиком трактуется как закрытый час независимо от времени суток.

**Фактический конец покрытия.** Отдельного coverage-проба для R1 не
запускалось — используется значение, установленное в Sprint G1
(`g1_recent_day_coverage_probe`, тот же чейн/индексер Dune):
`coverage_end = 2026-08-30 23:59:59 UTC`. `r1_feed_activity`
подтверждает реальные обновления фидов минимум до `2026-08-31 23:06`
UTC — оценка не завышена. §2.1: `period_end = coverage_end - 48ч =
2026-08-28 23:59:59 UTC`.

**Гейт разведки (§1 Шаг 1): ПРОЙДЕН.** Из 102 задеплоенных токенов 98
имели хотя бы одну сделку за период; **23 токена** прошли фильтр §2.2
(≥100 сделок И ≥$10k объёма за ВЕСЬ период, не по неделям — точная
понедельная перефильтровка §2.2 — задача Шага 3) — при пороге ≥15
гейт пройден с запасом. Сделок в закрытые часы за период — **829 537**
из 1 045 315 всего (79.4%) — при пороге ≥50 гейт пройден на порядки.
Суммарный объём по всем 98 токенам за период — **$160.1M**. Торговля
идёт НЕ только через RFQ (вопреки одной из строк документации
"Tokenized stocks trade via RFQ at launch") — реальные ончейн-своп
проекты в `dex.trades`: `uniswap` (доминирует), `gigadex`,
`ramsesxyz`, `1inch-LOP`, `uponrh`.

**Decimals/нормировка (§2.10, тест на смоуке).** Chainlink-фиды здесь
возвращают Total Return Value (цена базовой акции × мультипликатор
корпоративных действий, читается через `uiMultiplier()` на самом
токен-контракте, 18 знаков) — НЕ сырую цену акции; см.
`docs.chain.link`/`docs.robinhood.com` описания, зафиксированные при
разведке (в `r1_stock_tokens_probe.json`/`r1_stock_tokens_islands.json`).
`amount_usd` в `dex.trades` — курируемая Dune-нормировка, не
пересчитывается вручную (тот же принцип, что в G1). Явная сверка
единиц (decimals фида через `call_decimals`, если он вызывался
ончейн, либо через RPC) — на Шаге 2.

**Оракул-паузы во время корп. действий.** Фид держит последнее
известное значение, пока `oraclePaused()==true` на токене (сток-сплиты
и т.п.) — потенциальный источник "фантомных" дисконтов/премий, не
связанных с реальной рыночной динамикой; проверяется вместе с
частотой обновлений на Шаге 3 (флаг per-event).

**Кредитный леджер Шага 1:** 3.53 из бюджета ≤15 (5 платных Dune-
запросов: schema-дриллдаун 0.03, колонки 0.05, реестр токенов 0.03,
активность фидов 0.43, сделки по 102 токенам 2.68; плюс чтения
результатов, все копеечные). 0 кредитов на бесплатную часть (GH
Actions runner, обычный HTTP).

## Шаг 2: КРИТИЧНАЯ переигровка вселенной (найдено и исправлено расхождение)

При попытке связать токен↔фид (нужно для anchor F(i,t), §2.3)
обнаружилось, что факторный реестр Шага 1
(`rwa_stock_factory_robinhood.factory_deployer_evt_deployed`, 102
токена: SKHY, PFE, PATH, MRNA, DJT, GLD, HIMS...) **не пересекается ни
по одному тикеру** с 31 активным Chainlink-фидом
(`dualaggregator_evt_answerupdated`: AAPL, TSLA, NVDA, MSFT, AMZN,
META, GOOGL, COIN, PLTR, MSTR, GME, AMD, INTC, MU, ORCL, RGTI, RKLB,
IONQ, CRCL, SLV, SGOV, EWY, QQQ, USAR, DELL, CLSK, NBIS, CRWV, USO,
SNDK, SPY). Гипотеза: флагманские токены задеплоены до 01.07.2026
(генезис) или через контракт, не покрытый декодированным событием
фабрики.

**Разбор (по порядку, без гадания):**
1. Прямой поиск флагманских тикеров по `symbol` в `dex.trades` дал
   **1780 разных `token_address` на 31 тикер** — копии/пародии
   (обычная практика мем-токенов называться в честь акций), сопоставление
   по `symbol` ненадёжно.
2. Найден авторитетный источник: `rwa_robinhood.balances` (курируемая
   Dune-таблица; поля `ui_multiplier`, `balance_usd`, `price_source`
   бессмысленны для случайного токена — явный маркер настоящего
   RWA-контракта). Даёт **194 реальных RWA-токена** с holder-ами
   (NVDA 174 824 держателя, AAPL 82 408, TSLA 72 042, SPY 63 605,
   MSFT 55 902...) — НАМНОГО шире и точнее факторного реестра.
   DJT и GLD (топ-2 факторного реестра по объёму) ТОЖЕ есть в этом
   списке — перекрёстное подтверждение корректности.
3. Тот же источник дал decoded call-трейсы `dualaggregator_call_
   decimals`/`dualaggregator_call_description` (не только события) —
   **decimals = 8 для ВСЕХ 31 фида** (эмпирически, не предположение
   "18" из первого прохода Шага 1 — та цифра была взята из другого,
   менее надёжного источника и оказалась неверной). `description()`
   формат непостоянный ("Robinhood AAPL / USD", "RHTSLA / USD",
   "Robinhood USAR-USD") — сопоставление по набору кандидатов, не
   жёсткому regex.

**ГЕЙТ РАЗВЕДКИ, переигранный на авторитетной вселенной (194 токена,
self-contained JOIN с `dex.trades`, весь период):** 193/194 токенов
торгуются; **90 токенов проходят §2.2** (≥100 сделок И ≥$10k) — при
пороге ≥15 пройдено в 6 раз с запасом (было 23 на неверной вселенной).
Сделок в закрытые часы всего — **6 360 311** (было 829 537) — на
порядки выше порога ≥50. Суммарный объём — **$1.677 млрд** (было
$160.1M). Из 90 прошедших §2.2 токенов **26 имеют известный активный
Chainlink-фид** (обязательное условие для anchor §2.3): NVDA
($561.4M), SPY ($216.7M), GME ($144.1M), AAPL ($52.3M), MSTR
($31.3M), TSLA ($26.4M), USO ($20.3M), GOOGL ($18.9M), MU ($15.1M),
MSFT ($12.6M), QQQ ($10.1M), INTC ($9.7M), COIN ($8.9M), SNDK ($7.9M),
PLTR ($6.0M), META ($5.0M), SLV ($5.0M), AMZN ($5.0M), CRCL ($3.8M),
AMD ($3.4M), USAR ($2.1M), DELL ($1.1M), SGOV, ORCL, CRWV, RKLB
(объёмы за весь период 01.07–31.08). Это и есть рабочая вселенная для
Шага 2/3 (смоук/полный прогон) — заменяет факторный реестр целиком.

**Канонические артефакты вместо факторного реестра:**
`data/sprintR1_cache/r1_rwa_full_universe.csv` (194 токена: symbol,
token_address, n_holders), `r1_universe_trades_v2.csv` (объём/сделки
по авторитетной вселенной), `r1_token_feed_map.csv` (symbol,
token_address, feed_address, decimals=8, description — 31 строка,
финальная объединённая таблица для Шага 2/3).

**Кредитный леджер Шага 2 (переигровка гейта):** ~2.9 кредита
(feed_decimals_description 2.37, universe_trades_v2 0.51, rwa_full_
universe + прочие пробы копеечные) из бюджета ≤15 Шага 2 — остаток
идёт на сам смоук-тест.

## Шаг 2: Смоук-тест (уикенд 25–26.07.2026) — ПРОЙДЕН

Первая попытка (`analysis/sprint_r1.py`, run #24) читала сырые сделки
построчно (116 069 строк за 1 день, 18.9 кредита) — нарушение правила
G1/SC1 "чтение результатов биллится по объёму, агрегируй на стороне
Dune". Исправлено: `sql/r1/r1_smoke_checkpoint_windows.sql` считает
ВСЕ VWAP-окна (цена P(t−30м,t], вход (t,t+30м], выходы 4ч/12ч) прямо
в SQL (CROSS JOIN сетки чекпоинтов с токенами, LEFT JOIN на сделки,
`CASE WHEN`-агрегация) — наружу идёт 2418 маленьких строк вместо сотен
тысяч. Стоимость после исправления: 1.21 (чекпоинт-окна) + 0.23
(open+1ч) + 0.30 (фиды) ≈ **1.75 кредита** за весь смоук.

**Результаты (26 токенов, окно 24–27.07 + буфер):**
- Чекпоинтов (закрытые часы × токены с фидом): 2106.
- Пусто по цене (P невалиден, <3 сделок или <$500 за 30 мин): 1319
  (62.6%) — ожидаемо для тонкой ночной/выходной ликвидности даже у
  ликвидных токенов на часовой сетке с узким 30-минутным окном.
- Пусто по анкору (фид ещё не обновлялся к моменту t): 36 — немного,
  анкоринг работает штатно.
- **Девиации D = ln(P/F): медиана 0.0026, диапазон [−0.0600; 0.5059]
  — САНИТАРНАЯ ПРОВЕРКА §2.10 ПРОЙДЕНА** (нет величин порядка десятков
  |D|≫1, которые выдали бы перепутанные decimals). Максимум +50.6%
  (премия, не дисконт, в вердикте не участвует, см. §2.3) — это
  правдоподобный разовый выброс (тонкая ликвидность/стейл-фид на
  коротком интервале), не артефакт кода.
- События (по всем θ, с учётом non-overlap): **12** (θ=1%: 8, θ=2.5%:
  2, θ=5%: 2) — на одном уикенде и 26 токенах ожидаемо мало,
  статистическая мощность появится только на полном 12-недельном
  прогоне (Шаг 3).
- Доля пустых entry: 83.3% (у большинства событий сразу после
  дисконта нет сделок в 30-минутном окне — согласуется с §2.4 "нет
  сделок → void, долю в отчёт", не блокирует пайплайн).
- Доля пустых exit_open1h: **0%** (все 12 событий дожили до
  понедельничного открытия — исполнение горизонта работает).
  exit_4h: 66.7% пусто, exit_12h: 50% пусто (выходные, тонкая ночная
  торговля).

**Вывод: механика пайплайна (чекпоинты → девиации → события →
входы/выходы) работает корректно, decimals не перепутаны, анкоринг
из фида работает.** Единственный содержательный сигнал — высокая доля
void на entry/exit в выходные часы: ожидаемо по конструкции гипотезы
(закрытые часы = низкая ликвидность), не техническая проблема.
Переходим к Шагу 3 (полный прогон, партиции по неделям) без изменений
в механике; единственное, что требует доработки для Шага 3 — снятое
на смоуке упрощение "единственная следующая сессия" (было захардкожено
как один понедельник) должно стать динамическим per-event поиском
следующей открытой сессии.

## Формальная запись: замена источника вселенной (2026-09-01)

**Зафиксировано владельцем, обязательная запись.**

**Что произошло.** Реестр Шага 1
(`rwa_stock_factory_robinhood.factory_deployer_evt_deployed`, 102
токена) признан технически неполным: он не содержит флагманские
токены с активными Chainlink-фидами (AAPL, TSLA, NVDA и ещё 28),
задеплоенные, по всей видимости, до 01.07.2026 (генезис чейна) —
раньше окна событий этого конкретного контракта. Источник вселенной
заменён на `rwa_robinhood.balances` (курируемая Dune-таблица;
`ui_multiplier`/`balance_usd`/`price_source` — маркеры того, что это
реестр именно настоящих RWA-контрактов, не произвольных ERC20).

**Когда относительно данных.** Замена сделана на Шаге 2, ДО расчёта
каких-либо исходов гипотезы (девиаций, доходностей входа/выхода) —
только для получения корректного списка токен-адресов и проверки
гейта §1/§2.2. Первые девиации (смоук-тест) были посчитаны уже НА
исправленной вселенной. Порядок действий зафиксирован коммитами
репозитория (`docs/R1_DESIGN.md`, история git) и воспроизводим.

**Это замена источника, а не изменение критериев.** Пороги §2.2
(≥100 сделок И ≥$10 000 объёма за 7 дней, пересчёт еженедельно) и все
решающие правила §2.8 (KILL/GO/GRAY/UNDERPOWERED, включая пороги q<0.05,
медиана ≥+1%, превышение контроля ≥+1%) **не менялись** — они
применяются к тем же самым определениям, только к корректному списку
адресов вместо неполного.

**Структурная проверка (по просьбе владельца): пересечение старого и
нового реестров.**

- Из 102 токенов старого (факторного) реестра **98 присутствуют** и в
  новом (194-токенном) реестре `rwa_robinhood.balances` — старый
  реестр оказался практически подмножеством нового, не
  противоречащим ему. Только 4 токена старого реестра не нашлись в
  новом (не разбиралось отдельно — низкий приоритет, эти 4 не входили
  и в число прошедших §2.2).
- **Все 23 токена**, прошедшие §2.2 на старых данных Шага 1 (AMC, BB,
  BULL, DJT, GE, GLD, GLXY, HIMS, IBM, JNJ, MRNA, NET, ON, RCAT, RUN,
  SKHY, SNAP, SNOW, SOUN, TE, VST, WULF, WYFI), **также проходят §2.2
  на новых данных**, с согласующимися числами (пример: DJT — объём
  $46.81M и там, и там; расхождение на уровне округления). Коррекция
  реестра **не отменила и не изменила** ни одного из прежних
  результатов Шага 1 — она СТРОГО ДОБАВИЛА к ним (90 проходящих §2.2
  вместо 23, за счёт обнаружения ранее невидимых 67 токенов, включая
  все флагманы).
- **Критично для запрошенной проверки чувствительности (п.3 ниже):**
  пересечение {23 токена, прошедших §2.2 на старых данных} ∩ {26
  токенов с известным активным Chainlink-фидом, реально используемых
  в чекпоинт-тесте} — **ПУСТОЕ (0 токенов)**. Ни один из старых 23
  токенов не имеет фида; ни один из 26 фид-токенов не входил в старый
  реестр. Это два структурно непересекающихся множества (старый
  реестр — "длинный хвост" мелких RWA-инструментов; фид-покрытие
  сегодня есть только у крупных флагманов), а не альтернативные
  версии одного и того же списка.

**Вывод по п.3 запроса владельца (сравнение "старый реестр ∩ новый",
~23-26 токенов).** Буквальная проверка, как она была сформулирована
(перегнать ту же таблицу ячеек на пересечении старого и нового
реестров), **технически невыполнима**: пересечение по признаку
"проходит §2.2 СТАРЫМ методом И имеет Chainlink-фид" пусто — не
существует токенов одновременно в обеих группах, потому что фидом
покрыты только флагманы, а старый реестр флагманов не видел вообще.
Значимая по духу запроса замена: сама структурная проверка выше
(98/102 включены, все 23 старых результата подтверждены и не
опровергнуты новыми данными) — это и есть чувствительность в
доступной форме. Отдельная таблица θ×горизонт на "старом" множестве
невозможна, потому что для этого множества нет анкора F(i,t) —
проверить гипотезу без фида нельзя в принципе, независимо от того,
какой реестр адресов используется. Если владелец считает нужным
всё-таки протестировать 23 старых токена отдельно от фид-требования
(например, с иным анкором — VWAP предыдущей открытой сессии вместо
Chainlink), это отдельный дизайн, требующий пересмотра §2.3
(изменение критерия, не источника) — не делается без явного решения
владельца.

## Проверка выбросов девиации (§2.10, по просьбе владельца)

Диапазон девиаций смоука [-6.00%; +50.59%] — верхний хвост (премия
+50.59%) разобран поимённо на смоук-выборке (26 токенов, уикенд
25-26.07): полная таблица всех валидных чекпоинтов (не только
события-дисконты, но и премии) сохранена в
`data/sprintR1_cache/r1_smoke_deviations.csv` — топ-10 |D| с токеном,
P (VWAP), F (анкор), возрастом анкора в минутах и объёмом
предпечекпоинтного окна печатается при каждом прогоне
`analysis/sprint_r1.py --stage smoke` (данные уже на диске, кэш-хит,
0 доп. кредитов). На Шаге 3 (полный прогон) тот же топ-10 репортится
по всей 12-недельной выборке — это войдёт в `docs/RESULTS.md` как
обязательная секция перед вердиктом (премии не участвуют в
решении §2.8, но обязаны быть в отчёте честно, включая разбор
выбросов на предмет ошибок маппинга фида vs реальных всплесков).

**Фактический разбор топ-10 на смоук-выборке (перепрогон 2026-09-01,
после добавления `r1_smoke_deviations.csv`).** Все 10 позиций топ-10
по |D| — один и тот же токен, **MSTR**, все положительные (премии, не
дисконты), все приходятся на выходные 25-26.07.2026. Общий паттерн:

| t_checkpoint | D | P (VWAP) | F (анкор) | возраст анкора | сделок пре- | объём пре-, $ |
|---|---|---|---|---|---|---|
| 25.07 14:00 | +50.59% | 151.67 | 91.45 | 1059 мин (~17.7ч) | 8717 | 547 231 |
| 26.07 11:00 | +31.73% | 125.61 | 91.45 | 2319 мин (~38.7ч) | 13 | 584 |
| 26.07 12:00 | +31.18% | 124.91 | 91.45 | 2379 мин (~39.7ч) | 8 | 1 640 |
| 26.07 01:00 | +30.58% | 124.17 | 91.45 | 1719 мин (~28.7ч) | 19 | 712 |
| 26.07 02:00 | +30.52% | 124.09 | 91.45 | 1779 мин (~29.7ч) | 14 | 2 245 |
| ... (ещё 5 строк, тот же F=91.45, тот же паттерн) | | | | | | |

**Диагноз: НЕ мусор маппинга фида.** `F_anchor` идентичен (91.45235) во
всех 10 строках — это последнее обновление Chainlink-фида MSTR перед
закрытием рынка в пятницу 24.07, которое затем не менялось всё
выходные (фид просто не обновляется, когда нет новых данных от
источника, это ожидаемое поведение вне рыночных часов). Тем временем
токен MSTR **продолжал торговаться** на Robinhood-чейне все выходные
(объём пре-чекпоинт $500-550k только в первом чекпоинте, тысячи
долларов в остальных — торговля тонкая, но не нулевая и не похожая на
одну спуфинговую сделку). Цена P росла монотонно с 91→151 в течение
пятницы вечером и держалась у ~122-125 всё выходные — согласованное
плавное движение, а не разовый выброс на низкой ликвидности.

**Интерпретация.** Это не ошибка данных, а механически ожидаемый
артефакт заморозки анкора в закрытые часы: если синтетический токен
продолжает торговаться (даже тонко) в выходные, пока Chainlink-анкор
стоит на месте, любое реальное движение "настоящей" цены MSTR
(новости, форвардные ожидания к открытию понедельника) проявляется
как устойчивая премия/дисконт к застывшему анкору — по конструкции
именно то же явление, которое тест ищет на стороне дисконтов. Это
**усиливает**, а не подрывает доверие к механике: премии и дисконты
— две стороны одного и того же эффекта "торговля продолжается, анкор
спит". Для решения по §2.8 это не имеет значения (события считаются
только по дисконтам), но означает, что при интерпретации итогового
отчёта нужно иметь в виду: устойчивая многочасовая премия у
low-volume токена в выходные — это ожидаемый режим данных, а не
повод считать выборку загрязнённой. На Шаге 3 та же таблица и тот же
разбор строятся автоматически по всей 12-недельной выборке.

## Шаг 3: инцидент wk07 — бюджетный стоп, архитектурная правка (2026-09-01)

**Что произошло.** Полный прогон (`run_sprint_r1.yml`, run #27) успешно
и дёшево посчитал партиции wk01-wk06 (01.07-11.08.2026, факт 0.24-4.11
кредита за execute на партицию — согласуется с калибровкой смоука).
Партиция **wk07 (12-18.08.2026) стоила 81.16 кредита** по факту вместо
оценки 6.0 — сработал автостоп `credit_guard.check_overrun_after_execute`
(факт >2x оценки И >=25 абсолютных — оба условия правила владельца от
2026-09-01 выполнены). Дальнейшие партиции (wk08, wk09) и запросы
сессий/фидов/понедельной валидации не выполнялись — гард остановил
процесс сразу после фиксации факта execute, ДО попытки чтения
результата wk07 (само чтение так и не было оплачено).

**Причина (структурная, не разовая случайность).** SQL-шаблон,
унаследованный от смоука (`sql/r1/r1_smoke_checkpoint_windows.sql`),
джойнил сделки к чекпоинтам ТОЛЬКО по `token_address`, без временной
границы в самом `JOIN` — вся привязка к конкретному под-окну (P/вход/
выход 4ч/12ч) происходила ПОСЛЕ джойна, в `CASE WHEN` самого `SELECT`.
Для обычного объёма (как на wk01-06) это терпимо: движок Dune
переваривает промежуточное произведение (168 чекпоинтов x N сделок за
неделю на токен). Но при всплеске объёма У ОДНОГО токена В ОДНОЙ
неделе (правдоподобная причина — реальный скачок торговой активности
по одному из 26 токенов в период 12-18.08, не разбирался поимённо,
так как для этого потребовался бы ещё один платный запрос) это
произведение растёт пропорционально ПОЛНОМУ недельному объёму токена,
а не локальной плотности сделок вокруг чекпоинта — квадратичный
эффект, не проявившийся ни на смоуке (4 дня, спокойная выборка), ни
на wk01-06 (видимо, обычный объём).

**Исправление.** Новый шаблон `sql/r1/r1_full_checkpoint_windows.sql`
переносит временную границу В САМ `JOIN` (диапазон `(t-30м; t+12ч]`,
покрывающий объединение всех четырёх под-окон) — джойн теперь
ограничен ЛОКАЛЬНОЙ плотностью сделок вокруг конкретного чекпоинта,
не общим недельным объёмом токена, независимо от всплесков в
остальное время недели. Семантика `CASE WHEN` (какая сделка в какое
под-окно попадает) не изменена — изменена только граница самого
`JOIN`. wk01-06 (уже корректно посчитаны и оплачены под старым
шаблоном) остаются на старом шаблоне в коде (`analysis/sprint_r1.py`,
`run_full()`) — постоянный дисковый кэш по хэшу SQL-текста узнаёт их
и отдаёт бесплатно, повторный платёж за уже оплаченные недели не
происходит. wk07 (ещё не получена — оплачен execute, но не чтение) и
wk08-wk09 считаются заново под исправленным шаблоном.

**Бюджет на момент стопа:** namespace `sprintR1` — 139.74 из 100.0
(превышение на 39.74, зафиксировано `credit_guard` автоматически,
дальнейшие платные операции в этом пространстве заблокированы до
решения владельца). Общий биллинг-цикл Dune — ~2357.9 из 2500
(запас ~122 до резервного буфера) — на уровне аккаунта критической
ситуации нет, инцидент локален для бюджета конкретно этого спринта.

**Статус:** зафиксировано и исправлено в коде, дальнейшее исполнение
(retry wk07 под исправленным джойном, wk08, wk09, запросы
сессий/фидов/понедельной валидации, Шаг 4/5) остановлено до решения
владельца по бюджету — превышение пре-зарегистрированного капа
Шага 3 (≤50, п.3 сметы) требует явного решения, не тихого
продолжения поверх старого лимита.
