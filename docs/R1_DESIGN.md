# Sprint R1 — ретро-тест RWA-конвергенции (сток-токены): пре-регистрированный дизайн

**Зафиксировано ДО просмотра данных: 2026-09-01.** Раздел §2 ниже
вставлен дословно из промпта штаба на запуск Sprint R1 и после коммита
Шага 0 **не редактируется**, кроме дополнения подраздела «Механика»
внутри §2.1/§2.3 (праздники/укороченные сессии NYSE, фактическая дата
конца покрытия, decimals-проверка — сама механика может дополняться
находками разведки; критерии решений §2.7/§2.8 — нет). Полный текст
промпта (§0, §1, §3–§5) приведён ниже §2 для самодостаточности
репозитория (см. `docs/COST_POSTMORTEM.md`, ревизия 7) — эти разделы
операционные, не заморожены.

Закрытые линии (не трогать): копитрейдинг (Sprint 1+1.5, вердикт НЕТ);
graduation-momentum (Sprint G1, вердикт KILL) — лонг-стратегии по
мем-токенам этого чейна закрыты как класс решением штаба 2026-09-01.
См. `docs/PROJECT_STATE.md`.

## 2. Пре-регистрированный дизайн

### 2.1 Гипотеза и период
H1: в часы, когда базовый рынок США закрыт, ончейн-цены сток-токенов систематически отклоняются от справедливого якоря, и покупка значимых дисконтов даёт положительную чистую доходность к моменту после открытия рынка. Период: 01.07.2026 00:00 UTC → конец покрытия минус 48ч (фиксируется на Шаге 1). Закрытые часы: вне 13:30–20:00 UTC пн–пт, плюс праздники/укороченные сессии из Шага 1.

### 2.2 Вселенная и фильтр ликвидности (только прошлая информация)
Токен входит во вселенную на неделе W, если за предыдущие 7 дней: ≥100 DEX-сделок И ≥$10 000 объёма. Пересчёт еженедельно.

### 2.3 Чекпоинты, якорь, события
Чекпоинты: каждый целый час UTC в закрытые часы. Цена P(i,t) = VWAP сделок в (t−30м; t]; валидно при ≥3 сделках и ≥$500. Якорь F(i,t) = последнее значение Chainlink-фида на момент ≤ t. Девиация D = ln(P/F). Событие-дисконт: D ≤ −θ, θ ∈ {1%, 2.5%, 5%}. Неперекрытие: после события по токену при данном θ следующие чекпоинты этого токена пропускаются до разрешения самого длинного горизонта. Премии (D ≥ +θ) фиксируются для отчёта, в вердикте не участвуют.

### 2.4 Вход, выходы, издержки
Вход: VWAP в (t; t+30м]; нет сделок → void (долю в отчёт). Горизонты: 4ч; 12ч; «открытие+1ч» = VWAP в (открытие+30м; открытие+90м] ближайшей следующей сессии. Основной — «открытие+1ч». r = ln(exit/entry) − c; сценарии c: 0.5% / 1.5% (базовый) / 3%.

### 2.5 Колонки выгрузки (строка на событие)
token, feed_addr, t_checkpoint, theta, D, n_trades_pre, vol_usd_pre, entry_vwap, exit_4h, exit_12h, exit_open1h, void_flags, week_id. Плюс контроль §2.6 тем же форматом.

### 2.6 Контроль
Случайные валидные чекпоинты с |D| ≤ 0.5% (та же вселенная, те же горизонты), объём — не меньше числа событий при θ=1%. Назначение: отделить конвергенцию дисконта от общего дрейфа сток-токенов.

### 2.7 Статистика
Ячейки: 3 θ × 3 горизонта = 9. Ячейка допущена при N≥50. В допущенных: односторонний знаковый тест медианы r>0 при базовых издержках, BH по 9 ячейкам, α=0.05. Секондари: 5%-усечённое среднее с бутстреп-CI (10 000). Робастность: половины периода; сценарии издержек; сравнение с медианой контроля на том же горизонте.

### 2.8 Правила решения (заморожены)
- **KILL**: ни одна допущенная ячейка не значима по BH при базовых издержках (при наличии ≥1 допущенной).
- **GO → R2**: есть ячейка с BH q<0.05 И медианой нетто ≥ +1% И медианами обеих половин >0 И превышением медианы контроля того же горизонта минимум на +1%.
- **GRAY**: значимость есть, нарушено любое условие GO → решение владельца, дефолт закрыть.
- **UNDERPOWERED**: нет допущенных ячеек (все N<50).

### 2.9 Эксплораторно (на вердикт не влияет)
Премии/шорт-сторона (при существовании перп-рынка на тикер); разрез SPY/QQQ vs одиночные акции; уикенд vs будняя ночь; свежесть фида на момент события; доля USDG-квотных пулов.

### 2.10 Ограничения (обязательны в отчёте)
Фид вне часов рынка может замирать/быть stale — дисконт может отражать реальные новости (гэп-риск), частично контролируется контролем и выходом после открытия; тонкая ликвидность закрытых часов = слиппедж хуже ретро-VWAP; MEV; юридическая доступность живой торговли сток-токенами для владельца (резидент ЕС) — проверяется отдельно ДО любого лайва; decimals/нормировка фидов и пулов — явный тест на смоуке.

---

## Полный текст промпта штаба (2026-09-01, операционные разделы, не заморожены)

### 0. Контекст и рамки
Модель исполнения: Sonnet 5, effort обычный. Аккаунт Dune: текущий (Analyst-резерв не подключать без команды владельца).

- Паспорт — источник правды. Закрытые линии (копитрейдинг; graduation-momentum) не трогать; лонг-стратегии по мем-токенам закрыты как класс решением штаба 01.09.2026.
- Наследуются ВСЕ правила G1: граница по block_time в каждом запросе к трейдам/логам (регрессионный тест обязателен); калибровка узким срезом ×2.5 и партиционирование при проекции >40 на запрос; binding-гард на чтения (метаданные до оплаты); наружу только агрегаты; кэш `data/sprintR1_cache/` + коммит после каждой платной операции; 2x-стоп только при факте ≥25 кредитов.
- Бюджет: 100 кредитов, namespace `sprintR1`, принудительный гард. Пул от якоря ~284.

### 1. Порядок исполнения

**Шаг 0 — Пре-регистрация (ДО любого платного вызова).** Создать `docs/R1_DESIGN.md` с §2 дословно; заметка в README с датой заморозки; конфиг `sprintR1` (кап 100, кэш). Коммит + пуш. Только после этого — Dune.

**Шаг 1 — Разведка (≤15 кредитов, максимум бесплатного).**
- Бесплатно из docs.robinhood.com: реестр сток-токенов и адреса их Chainlink-фидов (страница Stock Tokens). Запиновать в репо с источником.
- Дёшево из Dune: (а) по каким сток-токенам есть DEX-сделки и в каком объёме; (б) профиль объёма по часам недели — торгуются ли они в закрытые часы; (в) частота обновлений фидов (AnswerUpdated по адресам фидов) — обновляются ли фиды вне часов рынка или замирают. Всё агрегатами, с границами по времени.
- В R1_DESIGN.md раздел «Механика» (дополняемый): праздники/короткие сессии US-рынка в периоде (сверить с публичным календарём NYSE), фактическая дата конца покрытия.
- Гейт разведки: ≥15 токенов проходят фильтр §2.2 И ≥50 сделок в закрытые часы за период суммарно. Меньше → стоп, отчёт в штаб.

**Шаг 2 — Смоук (один уикенд из середины периода, ≤15 кредитов).** Полный пайплайн: чекпоинты → девиации → события → входы/выходы → агрегаты. Проверки: разумность девиаций (не сотни процентов из-за децималей/нормировки!), доля пустых entry-окон, факт vs калибровка.

**Шаг 3 — Полный прогон (≤50 кредитов),** партиции по неделям, строка на событие (§2.5) + контроль (§2.6).

**Шаг 4 — Анализ локально (0 кредитов):** §2.7, таблица θ×горизонт, контроль, половины периода.

**Шаг 5 — Отчёт и вердикт §2.8 в `docs/RESULTS.md`, леджер, ограничения.**

### 3. Смета
Разведка ≤15 · Смоук ≤15 · Полный ≤50 · Резерв 20 · Кап 100 (гард, exit при превышении).

### 4. Запрещено
Менять §2 после Шага 0 (кроме «Механики»); запросы без временных границ; сырые строки наружу; переносить издержки/пороги из G1 задним числом; трогать закрытые линии; LLM в горячем пути.

### 5. Формат финального ответа
Вердикт §2.8; таблица лучшей ячейки (медианы по сценариям, q, N, половины, контроль); леджер (оценка/факт); пути артефактов; отклонения с причинами.

Возвратные точки: гейт разведки провален; вердикт готов. Между ними — автономно.

### Про отбор по кошелькам (владелец, при передаче задания)
Колонок под скоринг кошельков (n_scored_wallets_pre/n_hunters100_pre — фичи G1) в выгрузке R1 нет; проверка идеи «умные кошельки → хорошие токены» здесь потребовала бы отдельного платного запроса (~20–35 кредитов). Решение владельца: отложить до вердикта R1 — если потребуется, отдельный дизайн G1b с замороженными критериями пишется отдельно, не задним числом внутри R1.

## Механика (дополняется на Шаге 1)

**Заполнено 2026-09-01 по факту разведки** (`analysis/r1_recon.py`,
`data/sprintR1_cache/r1_schema_drilldown_*.csv`,
`r1_columns_probe_*.csv`, `r1_stock_token_deployments_*.csv`,
`r1_feed_activity_*.csv`, `r1_universe_trades_*.csv`; свободная часть —
`analysis/r1_scrape_stock_tokens.py`, `data/sprintR1_cache/
r1_stock_tokens_*.json`). Стоимость Шага 1: 3.53 из бюджета ≤15 (см.
кредитный леджер ниже).

**Реестр сток-токенов.** Бесплатная часть (docs.robinhood.com) дала
описание живого read-only REST API `https://api.robinhood.com/rhj/assets`
(реальные примеры адресов в документации, подтверждено сверкой) — но
интерактивная песочница агента блокирует egress на docs.robinhood.com/
docs.chain.link на уровне сетевого прокси (403 на CONNECT, org policy,
не WebFetch-специфично — проверено `curl` напрямую). Рабочий обходной
путь — тот же скрипт запускается через GH Actions runner (обычный
исходящий интернет, без ограничений инструмента). Финальный источник —
ончейн, надёжнее любого стороннего API: таблица
`rwa_stock_factory_robinhood.factory_deployer_evt_deployed`
(декодирована Dune, один UUPS-фабричный контракт
`0x4783c67b63de2b358ac5951a7d41f47a38f3c046`) даёт **102 задеплоенных
сток-токена** за июль-август (symbol, name, адрес токена, uid) —
сверено выборочно с примерами из `/rhj/assets` (CRM/Salesforce адрес
совпал побайтово).

**Реестр Chainlink-фидов.** `docs.chain.link` страница про
Robinhood-фиды рендерит таблицу адресов клиентским JS-компонентом
(`Astro`-остров `FeedList`, `client="idle"`) — данные подгружаются в
рантайме, недоступны простым HTTP GET ни в SSR HTML, ни в статическом
JS-бандле (проверено: 0 адресов в полном тексте страницы, 0 URL API в
загруженном бандле — вероятно, дальнейший динамический `import`).
Вместо гонки за клиентским виджетом — ончейн-путь, дешевле и надёжнее:
Dune уже декодировал ABI `dualaggregator` (`chainlink_robinhood`
схема, реализует `AggregatorV3Interface` + OCR) для ВСЕХ
Chainlink-агрегаторов на чейне через одну логическую таблицу
`dualaggregator_evt_answerupdated` (contract_address различает
инстансы). За июль-август найдено **31 активный фид** (5670..47
обновлений на фид за период). Прямой связи токен→фид в декодированных
таблицах нет (`factory_deployer_evt_deployed` не содержит адрес фида);
сопоставление по символу через `description()`-вызов фида (например,
через Alchemy RPC, `config.alchemy_rpc_url`) — задача Шага 2/3, не
блокирует смоук.

**Часы рынка и праздники (WebSearch, из нескольких публичных
источников про NYSE-календарь 2026, независимо от заблокированных
доменов).** В период 01.07–31.08.2026 единственная релевантная
закрытая сессия сверх регулярного WEEKEND/будни-вне-13:30-20:00 UTC —
**пятница 03.07.2026, ПОЛНОЕ закрытие** (перенос Independence Day,
04.07.2026 — суббота). Ранних закрытий (13:00 ET) в этот период нет
(ближайшие — день после Дня благодарения и канун Рождества, оба в
ноябре-декабре, вне периода R1). В расчётах чекпоинтов 03.07.2026
целиком трактуется как закрытый час независимо от времени суток.

**Фактический конец покрытия.** Отдельного coverage-проба для R1 не
запускалось — используется значение, установленное в Sprint G1
(`g1_recent_day_coverage_probe`, тот же чейн/индексер Dune):
`coverage_end = 2026-08-30 23:59:59 UTC`. `r1_feed_activity`
подтверждает реальные обновления фидов минимум до `2026-08-31 23:06`
UTC — оценка не завышена. §2.1: `period_end = coverage_end - 48ч =
2026-08-28 23:59:59 UTC`.

**Гейт разведки (§1 Шаг 1): ПРОЙДЕН.** Из 102 задеплоенных токенов 98
имели хотя бы одну сделку за период; **23 токена** прошли фильтр §2.2
(≥100 сделок И ≥$10k объёма за ВЕСЬ период, не по неделям — точная
понедельная перефильтровка §2.2 — задача Шага 3) — при пороге ≥15
гейт пройден с запасом. Сделок в закрытые часы за период — **829 537**
из 1 045 315 всего (79.4%) — при пороге ≥50 гейт пройден на порядки.
Суммарный объём по всем 98 токенам за период — **$160.1M**. Торговля
идёт НЕ только через RFQ (вопреки одной из строк документации
"Tokenized stocks trade via RFQ at launch") — реальные ончейн-своп
проекты в `dex.trades`: `uniswap` (доминирует), `gigadex`,
`ramsesxyz`, `1inch-LOP`, `uponrh`.

**Decimals/нормировка (§2.10, тест на смоуке).** Chainlink-фиды здесь
возвращают Total Return Value (цена базовой акции × мультипликатор
корпоративных действий, читается через `uiMultiplier()` на самом
токен-контракте, 18 знаков) — НЕ сырую цену акции; см.
`docs.chain.link`/`docs.robinhood.com` описания, зафиксированные при
разведке (в `r1_stock_tokens_probe.json`/`r1_stock_tokens_islands.json`).
`amount_usd` в `dex.trades` — курируемая Dune-нормировка, не
пересчитывается вручную (тот же принцип, что в G1). Явная сверка
единиц (decimals фида через `call_decimals`, если он вызывался
ончейн, либо через RPC) — на Шаге 2.

**Оракул-паузы во время корп. действий.** Фид держит последнее
известное значение, пока `oraclePaused()==true` на токене (сток-сплиты
и т.п.) — потенциальный источник "фантомных" дисконтов/премий, не
связанных с реальной рыночной динамикой; проверяется вместе с
частотой обновлений на Шаге 3 (флаг per-event).

**Кредитный леджер Шага 1:** 3.53 из бюджета ≤15 (5 платных Dune-
запросов: schema-дриллдаун 0.03, колонки 0.05, реестр токенов 0.03,
активность фидов 0.43, сделки по 102 токенам 2.68; плюс чтения
результатов, все копеечные). 0 кредитов на бесплатную часть (GH
Actions runner, обычный HTTP).
