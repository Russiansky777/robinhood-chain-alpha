# PROJECT_STATE — Robinhood Chain Alpha

## Цель и путь

Заработок на Robinhood Chain (L2 Arbitrum, mainnet 01.07.2026, chain id 4663). Путь: ретро-валидация стратегии → execution-модуль с гардами → микро-лайв ~$100 → лестница депозита ($100 → $500–1000 → рабочий размер). Теневой режим отменён решением владельца.

## Ветки репозитория

**Основная (дефолтная) ветка репозитория на GitHub — `claude/robinhood-copytrading-hypothesis-awjj0g`**, НЕ `main` (проверено `git ls-remote --symref origin HEAD`, 2026-09-01; `main` существует, но не является дефолтной). Это важно операционно: `workflow_dispatch` через GitHub API принимает только воркфлоу, уже присутствующий на дефолтной ветке — воркфлоу, добавленные на других ветках (например, `claude/sc1-verdict-3jaxoy`, ветка этой сессии), можно запускать только через `push`-триггер (см. `docs/P3_GUARD.md`, «Как запускалось на практике», и тот же паттерн в `.github/workflows/run_p4_lighter.yml`) до тех пор, пока эти ветки не смержены в дефолтную.

## Закрытые линии

Копитрейдинг — ЗАКРЫТ (Sprint 1 + 1.5, вердикт НЕТ). Персистентность размера прибыли отсутствует: медиана августовского PnL топ-когорты июля = $0 (как у контроля), Mann-Whitney p=0.68. Факт-прибыльность персистентна (лифт 3.89–5.73x во всех 4 ячейках sensitivity, Fisher p<0.0001), но немонетизируема копированием сделок. Критерии были зафиксированы до данных; повторное тестирование линии запрещено. Детали: docs/RESULTS.md.

Graduation-momentum — ЗАКРЫТ (Sprint G1 v2, вердикт KILL, решение штаба 2026-09-01). N аналитическое=513 (гейт N≥200 пройден), ни один из 10 горизонтов (30с…24ч) не значим по BH (q≥0.05) при базовой стоимости 3%, медианы глубоко отрицательны на обеих половинах периода и при всех сценариях издержек (1/3/5%). Распад после градуации реален и велик по амплитуде, но не монетизируем доступным инструментарием (нет шорт-инструмента на чейне) — решением штаба лонг-стратегии по мем-токенам этого чейна закрыты как класс, не только эта формулировка. Критерии были зафиксированы до данных (docs/G1_DESIGN.md §2.7); повторное тестирование линии запрещено. Детали: docs/RESULTS.md.

RWA-конвергенция (сток-токены) — ЗАКРЫТ (Sprint R1, вердикт KILL, 2026-09-01). 189 событий (θ=1%: 130, θ=2.5%: 38, θ=5%: 21) на 26 ликвидных сток-токенах с известным Chainlink-фидом, 12-недельный период (01.07–28.08.2026). Из 9 ячеек (3θ × 3 горизонта) допущена только одна (θ=1%, «открытие+1ч», N=61) — не значима (p=1.00, q=1.00 по BH), медиана нетто r=-1.49% (базовые издержки 1.5%), хуже медианы контроля (-1.28%) на -0.21%. Остальные 8 ячеек UNDERPOWERED (N от 5 до 48) — событий на более широких θ и горизонтах 4ч/12ч физически мало из-за non-overlap правила §2.3. Сенситивность на пересечении старого/нового реестра токенов технически невыполнима (пересечение пусто — старый реестр не видел фид-покрытые флагманы, см. docs/R1_DESIGN.md, «Формальная запись»); структурная проверка подтвердила, что коррекция реестра на Шаге 2 не отменила ни одного результата Шага 1. Топ-10 |D| по всей выборке разобран поимённо: доминирующий кластер (7/10, GME 22-23.07) — застывший анкор + большой реальный объём (сотни тыс.–млн $), похоже на подлинное движение цены в закрытые часы; 2-3 отдельных выброса (GME 20.07, MSFT 16.07, TSLA 09.08) — тонкая ликвидность (3-32 сделки, объём у порога §2.3 ≥$500) даёт статистически шумные VWAP, не ошибка маппинга фида, но наблюдение для возможного ужесточения порога §2.3 в будущих спринтах этого типа. Критерии зафиксированы до данных (docs/R1_DESIGN.md §2); повторное тестирование линии запрещено. Инцидент бюджета (Шаг 3, партиция wk07: 81.16 кредита вместо оценки 6.0, диагностирован и исправлен — джойн без временной границы; кап namespace поднят владельцем 100→180, первый из двух разрешённых подъёмов) — см. docs/R1_DESIGN.md. Итог: 160.67 из 180 кредитов namespace `sprintR1`. Детали: docs/RESULTS.md.

## Активная линия

Нет активной линии (копитрейдинг, graduation-momentum, R1 — все закрыты выше). Следующая линия — по решению владельца, не автоматически.

## P3 / P4 / депеги — статус (решение владельца, 2026-09-01)

**P3 (кросс-версионная дислокация сток-токенов, v3↔v4) — переведён в WATCHLIST.** Причины (решение владельца): латентностная гонка (конкурентное преимущество в этой идее определяется скоростью исполнения, а не аналитическим эджем — типичный признак идеи, где розничный/скриптовый участник проигрывает специализированным ботам), регим-брейк 29.09.2026 (конец 90-дневного gas waiver сети меняет экономику всех latency-чувствительных стратегий на чейне, см. docs/G1_DESIGN.md), бюджет (текущий биллинг-цикл Dune уже значительно израсходован по факту R1/G1/SC1/1.5, см. ниже). Запущен один дешёвый гард без Dune (RPC/Blockscout через Alchemy, узкий срез ×2.5, GH Actions runner — не интерактивная сессия, egress которой блокирует все внешние домены кроме github.com): доля топ-3 адресов в кросс-версионных (v3↔v4) закрытиях дислокаций топ-5 сток-токенов за последние 7 дней; если >80% — P3 закрывается окончательно. Методология и критерий зафиксированы до результата — см. `docs/P3_GUARD.md`. **Заблокирован на кредах, не на коде.** Код готов и протестирован; сделано 3 реальных прогона на GH Actions 2026-09-01 (не в интерактивной сессии — её egress блокирует все внешние домены кроме github.com), все три уткнулись в отсутствие рабочего RPC-ключа: `ALCHEMY_API_KEY` не настроен секретом в этом репозитории (тот же вывод, что уже был у Sprint R1 — `analysis/r1_feed_match.py` тоже никогда не оставил результата), а бесплатный публичный Blockscout eth-rpc для Robinhood Chain конкретно вернул 403 — по официальной документации (`blockscout/docs`, `robinhood-api.mdx`) этот чейн обслуживается через платный «PRO API» гейтвей, требующий Bearer-токен даже на бесплатном тире (регистрация на `dev.blockscout.com`). Подробный разбор всех трёх попыток — `docs/P3_GUARD.md`, "Блокер выполнения". Нужно от владельца: секрет `ALCHEMY_API_KEY` **или** зарегистрированный на dev.blockscout.com ключ как `BLOCKSCOUT_API_KEY` — любой из двух разблокирует гард (агент не регистрируется на внешних сервисах самостоятельно).

**P4 (фандинг/базис на перп-рынках сток-токенов, Lighter/Aster) — приоритет №1 по итогам внешнего исследования 01.09; Aster — резерв.** Разведка выполнена без Dune, только публичные API/документация — см. `docs/P4_RECON.md`. Обе площадки реально листят перпетуалы на токенизированные акции и документируют публичные (без ключа) REST-эндпоинты для списка рынков и истории funding (подтверждено первоисточником — официальные репозитории `asterdex/api-docs` и `elliottech/lighter-python` на GitHub). Явная оговорка: интерактивная сессия не смогла выполнить НИ ОДНОГО живого API-вызова к самим площадкам напрямую (сетевая политика окружения блокирует egress к `fapi.asterdex.com`, `apidocs.lighter.xyz`, `mainnet.zklighter.elliot.ai` и т.д. — проверено эмпирически) — актуальный список тикеров, текущие ставки funding и точные лимиты плеча/позиции в `docs/P4_RECON.md` вторичны (новости/агрегаторы) там, где не подтверждены отдельно. `docs/P4_DESIGN.md` сознательно НЕ создан — по указанию владельца дизайн замораживается в штабе после того, как разведка прочитана, не автоматически этой сессией.

Kill-критерии P4 заморожены ДО данных — `docs/P4_KILL.md` (<30 дней истории фандинга по всем сток-перпам → KILL; медианный годовой фандинг в пользу шорта минус издержки хеджа <8% → KILL; глубина ±0.5% меньше планируемой ноги → KILL по ёмкости — два последних критерия не полностью параметризованы владельцем, см. явную оговорку в файле). Стадия `p4_lighter_markets` (`analysis/p4_lighter_markets.py`, `.github/workflows/run_p4_lighter.yml`) выполнена реально (не только код — 4 живых прогона на GH Actions 2026-09-01, публичный REST Lighter, без ключа и без Dune): **231 перп-рынок всего на Lighter, 45 сток-/RWA-подобных** (совпадение по `market_type` и/или символу с реестром 194 токенов Sprint R1) — реальное подтверждение, что Lighter действительно листит сток-перпы в этом масштабе, не только принимает акции как обеспечение. Максимальный охват истории фандинга — 62.42 дня (**критерий №1 ПРОЙДЕН** — есть перпы с ≥30 дней истории). Явные незакрытые вопросы, не додуманные вместо проверки: (а) знак фандинга — поле `direction` API Lighter оказалось не тем, что задокументировано в SDK ("long"/"short", не "long_pays_short"/"short_pays_long"), и какое направление благоприятно для шорта **не подтверждено официальной докой** — опубликованные цифры годового фандинга (медиана ~350% почти везде) идут БЕЗ инверсии знака, см. явную оговорку в `docs/P4_RECON.md`; (б) глубина книги (±0.1%/±0.5%) занижена — упёрлась в лимит уровней/сторона почти на всех рынках.

**Дозапрос владельца (2026-09-01, 5-й прогон), Q3–Q5:** пересчёт единиц `rate` (сверка с `docs.lighter.xyz` — "funding payments distributed over 8 hours", "expressed in bps" — и внешним ориентиром loris.tools, сравнимые крипто-рынки max ~95%/год) — **36 из 36 успешно опрошенных рынков (9/45 упёрлись в rate-limit 429, честно исключены, не додуманы) показывают >100%/год после пересчёта → по прямой инструкции владельца это подозреваемый баг/аномалия, НЕ подтверждённый факт рынка**, разрешение требует либо доступа к официальной доке напрямую, либо реальной калибровки по счёту (`analysis/p4_funding_calibration.py`, заготовка, НЕ запускается автономно — только по команде владельца после открытия позиции). Q4: эмпирически найден рабочий лимит глубины книги — **200 уровней/сторона** (выше ранее известных 100, точный документированный максимум не найден); добавлен часовой объём (`daily_quote_token_volume/24`, 0 доп. запросов) как временный прокси ёмкости. Критерии №2–3 P4_KILL по-прежнему не применены к вердикту — ждут подтверждения знака `direction`/единиц `rate` и ввода владельца (издержки хеджа, размер ноги).

**Дозапрос владельца, 2-й раунд (2026-09-01), п.3:** рынок для калибровки — **SNDK (market_id=139)**, максимальный часовой объём (прокси) среди всех опрошенных — **$933 214.74/час**, почти втрое выше второго места (SPCX). Точечный прогон (`analysis/p4_current_snapshot.py`, GH Actions [run 33559991638](https://github.com/Russiansky777/robinhood-chain-alpha/actions/runs/33559991638)) — последняя закрытая часовая запись на 2026-09-01T21:00:00Z: `rate=0.0004`, `direction=long` (аннуализация **~~350.4%/год~~ — цифра устарела, см. исправление ниже**). За последние 48 часовых записей 46/48 — `direction=long`/`rate=0.0004` стабильно, 2/48 — выброс (`direction=short`, rate в 20–90 раз выше) — `direction` НЕ константа на этом рынке, но семантика payer/receiver по-прежнему не подтверждена без реальной калибровки по счёту (п.5, ниже). П.4 (сама калибровка `p4_funding_calibration.py` по открытым позициям) — **НЕ запущена**, ждёт явной команды владельца «позиции открыты» (её не было в этом сообщении).

**Дозапрос владельца, 3-й раунд (2026-09-01), п.3 — ИСПРАВЛЕНА единица `rate`:** три независимых реальных доказательства (клэмп официальной доки `docs.lighter.xyz/perpetual-futures/funding` ±0.5%/час — реальная запись `rate=0.0361` нарушала бы его как доля, но не как процент; эмпирика `value=(rate/100)×price` на всех 48 записях SNDK, точно восстанавливающая реальную ценовую траекторию; декомпозиция `base_interest_rate=0.0032 / 8 = 0.0004`, ровно медиана всех 36 рынков) сошлись: **`rate` — уже процент, не доля.** Скриншот UI от владельца не понадобился. Скорректированная годовая аннуализация (`rate×24×365`, без лишнего ×100): **медиана 3.504%/год у всех 36 рынков — 0 из 36 превышают 100%/год** (было 36/36 при старой трактовке). Единственное исключение на уровне p90 (не медианы) — MRNA, 118.08%. `analysis/p4_lighter_markets.py` и `analysis/p4_current_snapshot.py` исправлены (формула + `bps_per_8h` ×100 вместо ×10000); опубликованные ранее абсолютные цифры (~350%/год) не переписаны задним числом, помечены устаревшими. Критерий №2 P4_KILL по-прежнему ждёт ввода владельца (издержки хеджа), но при 3.5%/год он, скорее всего, ближе к KILL, чем предполагала более ранняя картина — наблюдение, не вердикт. Полный разбор — `docs/P4_RECON.md`.

**Дозапрос владельца, 4-й раунд (2026-09-01), п.2 — критерий №2 применён, линия переформулирована.** `3.504%/год − издержки хеджа < 8%` истинно при ЛЮБЫХ неотрицательных издержках → **критерий №2 P4_KILL: НЕ ПРОЙДЕН**, зафиксировано в `docs/P4_KILL.md`. Экономика поинтов LIT (программа `Lighter on Robinhood Chain Points`, живёт с 10.08.2026, еженедельные дропы по пятницам с 21.08, пул объявлен как $11M→$25M по разным источникам — противоречие не сглажено) — **понедельные LIT/объём для недель 21.08 и 28.08 конкретно НЕ НАЙДЕНЫ** ни в одном источнике → LIT на $1 объёма НЕ посчитан (владелец: «если не публикуется — не оценивать», не додумано). Линия P4 переформулирована: **«дельта-нейтральный фарм LIT»** — доход не сам фандинг (KILL), а поинты/LIT программы при захеджированной позиции, фандинг здесь — издержка хеджа. Добавлен критерий-заготовка №4 («USD за $1 дневного объёма после стоимости хеджа ≥ [порог владельца]») — НЕ параметризован, ждёт порога от владельца и реального источника понедельных чисел. `analysis/p4_funding_calibration.py` (Q5) — **отменён** (докстринг помечен явно): семантика `rate` разрешена без открытия позиции, скрипт остаётся в репозитории на будущее.

**Депеги (сток-токен ↔ цена базового актива) — только мониторинг.** Не активная линия и не кандидат следующего спринта на данный момент (решение владельца, 2026-09-01). Отдельного гарда/разведки в этом проходе не запрашивалось и не выполнялось.

## Побочный замер SC1 — ЗАВЕРШЁН (вердикт GO, V1-эра)

Sprint SC1 (юнит-экономика конвейерного запуска токенов, `docs/SC1_NOTE.md`) — отчёт готов (2026-09-01). Вердикт: **GO** по замороженному критерию (медианный конвейер 48 кластеров ≥50 запусков заработал $4 812 комиссий против $374 полных издержек launchFee+газ — 14.5x против порога 2x; концентрация топ-5% кластеров 78.65% < порога лотереи 90%). Явная оговорка: вывод привязан к V1-эре (01–12.08.2026), применимость к текущим V2-запускам — качественная, не количественная (см. отдельный абзац в SC1_NOTE.md). Потрачено ~22 из 40 кредитов namespace `sprintSC1` (лимит поднят владельцем 20→40 разово). Повторное тестирование этой линии — по решению владельца, не автоматически. Детали: docs/RESULTS.md.

**Дозапрос владельца, 2-й раунд (2026-09-01):** (1) «потолок конвейера» — топ-10 серийных кластеров (≥20 запусков) по net/$/день (0 Dune, из уже оплаченного кэша, `days_active` по факту таймстампов запусков, пол 1 день): медиана топ-10 **$14 239.93/день (V1)**, **$9 967.95/день (V2, ×0.7)** — по явному решению владельца БЕЗ порога GO/KILL, чисто описательно; это ЛУЧШИЙ наблюдённый случай, не типичный конвейер (медиана по всем 125 серийным кластерам — $72.99/токен, см. SC1_NOTE.md). (2) Реальная V2-транзакция создания токена (газ в ETH/USD) — **недоступно, доложено, не посчитано**: ни `BLOCKSCOUT_API_KEY`, ни `ALCHEMY_API_KEY` не заданы (тот же блокер, что у P3-гарда).

**Инцидент документации (обнаружен и исправлен 2026-09-01, при доведении SC1 до вердикта):** секция SC1 отсутствовала в docs/RESULTS.md — автоматика Шага 5 Sprint R1 (`analysis/sprint_r1.py`) писала файл через `write_text()` целиком вместо append, из-за чего прогон R1 молча стёр секции Sprint 1, Sprint 1.5 и Sprint G1 v2 (они шли до R1 по истории коммитов: `4bf0d72`, `b220fce`, `caf5912`/`79f0262`). Восстановлено дословно из истории git (коммит `79f0262`, последний перед перезаписью) + дописана секция R1 (без изменения чисел, только понижен уровень заголовков под общую структуру) + добавлена секция SC1. `analysis/sprint_r1.py` исправлен на read-existing+append с маркером-гардом от повторной дупликации (по образцу `sprint_g1.py`, который эту схему всегда использовал корректно) — повторный прогон Шага 5 R1 больше не сотрёт файл.

## Правила работы с Dune (оплачены ~1400 кредитами ошибок)

1. Сырые данные не покидают Dune: наружу через API — только агрегаты (тысячи строк максимум). Чтения результатов биллятся по объёму: чтение 1.2M строк стоило 164 кредита.
2. Санитарная проверка ДО исполнения: оценка >40 кредитов ИЛИ множественное сканирование тяжёлых CTE (UNION ALL по сырым свопам) = жёсткий стоп.
3. Отклонение факта от оценки >2x = немедленный стоп сессии.
4. Смоук-тест на одном дне данных до любого полнопериодного прогона.
5. Постоянный кэш data/sprint*_cache/ с коммитом сразу после каждой оплаченной операции; эфемерному кэшу Actions не доверять (гибнет при ненулевом exit).
6. Бюджетный гард: data/credits_spent.json, проверка до КАЖДОЙ платной операции (execute И чтения), лимит на спринт в конфиге, превышение = exit. Лимит поднимается только явным решением владельца, максимум дважды.
7. Состояние цикла (обновлено 2026-09-01, владелец -- внешний якорь, см. docs/COST_POSTMORTEM.md ревизия 5): потрачено 2138.84 из 2500 (реальный пул, не 2450), сброс 14.09.2026. Резерв 20 неснижаемый. Мультиаккаунты запрещены.
8. Пороги в деньгах, не в голых кредитах (владелец, 2026-09-01): кредит ≈ несколько центов на текущем плане. Операции дешевле ~$1 (порядка 30–40 кредитов) не требуют ни остановки, ни доклада штабу -- просто выполняются. Автоматические стопы (кап пространства спринта, >2x при факте ≥25 кредитов, санитарная проверка >40 на один запрос) остаются в силе как жёсткие технические ограничители -- этот пункт про то, что НЕ обязательно докладывать о штатных дешёвых шагах, не про отмену самих стопов.

## Протокол микро-лайва ($100)

Проверяет механику, НЕ прибыльность (позиции $5–15 не дают статистики PnL). Логировать обязательно: полная стоимость раунд-трипа (газ входа+выхода, комиссия пула, налог токена); слиппедж «цена при сигнале → цена исполнения»; задержки (сигнал→отправка, отправка→блок); каждый ханипот и реверт; отказы фильтра (какие токены забраковал и почему); несостоявшиеся сигналы с причиной; ежедневная сверка балансов бот↔чейн. С первого дня: лимит дневного убытка, кап позиции, стоп по серии фейлов транзакций, стоп по расхождению факт/модель.

## Дисциплина моделей и ролей

Штаб (чат Claude): стратегия, гейты, интерпретация — Fable/Opus + расширенное мышление. Исполнение (Claude Code): Sonnet 5, effort обычный. Рантайм-классификация (будущее): Haiku, батчи. LLM в горячем пути исполнения сделок запрещена. Любой бюджет сопровождается механизмом принудительного стопа, не обещанием.

## Ключевые артефакты

docs/RESULTS.md — результаты спринтов; docs/COST_POSTMORTEM.md — разбор перерасходов и ревизий гарда; docs/README.md — методологии; data/credits_spent.json — счётчик кредитов; data/sprint15_cache/ — оплаченные результаты.
