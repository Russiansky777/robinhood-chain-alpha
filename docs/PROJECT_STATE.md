# PROJECT_STATE — Robinhood Chain Alpha

**Этот файл — единственный источник контекста при переходе в новый чат.**
Обновлён 2026-09-03 перед переходом. Короткий файл-компас — `docs/HANDOFF.md`
(что делать первым) — читать вместе с этим, не вместо.

## Цель и путь

Заработок на Robinhood Chain (L2 Arbitrum, mainnet 01.07.2026, chain id 4663). Путь: ретро-валидация стратегии → execution-модуль с гардами → микро-лайв ~$100 → лестница депозита ($100 → $500–1000 → рабочий размер). Теневой режим отменён решением владельца.

## Ветки репозитория

**Ветка этой live-работы (P5) — `claude/sc1-pons-v2-launcher-efdbyt`.** Дефолтная ветка на GitHub исторически `claude/robinhood-copytrading-hypothesis-awjj0g` (не `main`) — воркфлоу, добавленные вне дефолтной ветки, запускаются только через `push`-триггер (маркер-файл в `data/p3_guard_cache/`), не через `workflow_dispatch` из API, пока не смержены.

---

# АКТИВНАЯ ЛИНИЯ: P5 — хеджированная LP ETH/USDG на Lighter

**Статус: ЖИВАЯ ПОЗИЦИЯ ОТКРЫТА.** Реальные деньги, реальный чейн, реальная биржа. Это первый полноценный прогон — с него считается kill-порог 30% годовых.

## 1. Текущая живая позиция (реальная, на момент обновления файла)

**ОБНОВЛЕНО 2026-09-05:** P5 (tokenId `1000756`, таблица ниже) **реально закрыта** (Гейт 1, владелец, итог +$1.26 — см. RESULTS.md §1-4), cron `:06` остановлен. P6 (tokenId `76445294`, Aerodrome Slipstream, Base, USDC/cbBTC) — реально открыта (Гейт 2, RESULTS.md §8), затем **реально ЗАКРЫТА в тот же день** после трёх read-only проверок (RESULTS.md §8 продолжение, docs/PROJECT_STATE.md #17): реальный fee APR пула (11–14%/год из GT-объёма) на порядок ниже заявленного DefiLlama (178–548%) — DefiLlama непригоден для оценки этого пула, плюс структурная поправка на staked/unstaked-механику Aerodrome. Итог закрытия: **-$0.044** по обеим ногам (хедж + LP fees/IL − газ Base-стороны), полный разбор с tx-хэшами — RESULTS.md §9. Cron `:36` остановлен и остановка **подтверждена реальным чтением crontab до/после** (GH Actions run 33959758020, `run_vps_remove_p6_cron_nl.yml`): ДО — обе строки (`:21` funding, `:36` p6) присутствовали; ПОСЛЕ — только `:21` funding, явные проверки `OK: строки P6 нет` и `OK: funding-строка на месте` обе прошли. `:21` (funding) и SSH-доступ не тронуты. **Живых позиций хеджированного LP сейчас нет — ЛИНИЯ ЗАКРЫТА (владелец, 2026-09-05, окончательно, см. `## Закрытые линии`).** Честный пересчёт скринера по реальному GT-объёму и реальной 30-дневной σ (правило #18, баг #22) дал 0 из 21 реально посчитанных кандидатов с `ratio_to_lvr≥2` — согласуется с обоими живыми замерами (P5 1.3–1.47, P6 ~0.08) и с литературой по Uniswap-v3-стилю на крупных парах. Переоткрывается только новым источником объёма, не новым пулом. Таблица ниже — P5, оставлена как историческая запись реального открытия/входа P5 (не текущее состояние).

| | |
|---|---|
| **Открыта** | 2026-09-03T22:06:36Z |
| **LP tokenId** | `1000756` (NFPM `0x73991a25c818bf1f1128deaab1492d45638de0d3`, Uniswap v3, fee 0.01%) |
| **LP-нога** | 0.037725338 ETH + 102.378373 USDG |
| **Диапазон** | ±10% вокруг цены входа: [$2258.91, $2760.90], `tick_lower=-199094`, `tick_upper=-197087` |
| **Хедж-нога** | 0.0377 ETH шорт на Lighter (market_index=0, аккаунт 22012) @ $2506.43 |
| **Итоговая дельта** | ≈0.0000253 ETH (~$0.06) — практически ноль |
| **Плечо** | 3.0003× (подтверждено чтением до и после открытия, не предположено) |
| **Маржа** | collateral $59.799088, available $28.274608 → **свободно 47.3%** (буфер ≥25% соблюдён) |
| **Ликвидация** | оценка формулой 61.348360064171544% роста цены ETH — **совпадает с реальным полем `liquidation_price` позиции ($4044.08) с точностью до последнего знака**, разница = 0.0 (подтверждено дважды на двух разных реальных прогонах, см. §5) |
| **Газ на открытие** | 0.000181740564552 ETH (~$0.46), 4 tx, все success |

Полный сырой результат: `data/p3_guard_cache/p5_live_step1_result.json` (перезаписывается на каждом запуске `p5_live_step1.py` — актуален только СРАЗУ после реального прогона, не для последующего чтения "что сейчас открыто"). **Для стартовых значений и времени открытия, переживающих последующие запуски** — отдельный файл `data/p5_live_position_state.json` (см. §1 таблицу выше — все цифры оттуда): `opened_at_utc`, обе ноги входа, плечо, маржа, газ, `kill_threshold_annual=0.30`, `capital_at_risk_usd_entry`. Часовой/событийный журнал всех open/close-событий — `data/p5_live_log.jsonl` (append-only, одна JSON-строка на событие).

Предыдущая LP этого же скрипта (tokenId `999556`, открыта на 2×-плече, партиями закрывалась/хеджировалась 4 неудачные попытки) — **закрыта владельцем вручную** (LP выведена в токены, хедж на Lighter закрыт), подтверждено реальным чтением `NonfungiblePositionManager.positions(999556)` перед новым открытием: `liquidity=0`, `tokensOwed0=0`, `tokensOwed1=0`.

**ПОД ПРОВЕРКОЙ 04.09.2026 (базовая доходность пула, входит в основу kill-порога 30%/год): витрина GeckoTerminal показывала владельцу TVL $8.96M и объём 24ч $79.13M (144.87K транзакций) — расхождение до 10× по объёму с изначально записанными $823M/сутки (`docs/P5_HEDGED_LP.md`). Записанные $823M не бьются ни с этим единственным пулом при новом наблюдении, ни (по порядку величины) со всем Uniswap V3 на Robinhood Chain ($522.37M/сутки, если эта цифра верна отдельно). Реальный API-запрос `GET /networks/robinhood/pools/{addr}` в ЭТОТ ЖЕ день (2026-09-03T22:32:27Z, `data/p3_guard_cache/gt_pool_verify_probe_result.json`) дал ТРЕТЬЮ цифру: объём 24ч $787.08M, TVL $24.43M — количественно БЛИЖЕ к исходным $823M/$21.1M, чем к наблюдению владельца $79.13M/$8.96M. Три наблюдения одного пула расходятся на порядок, не сведены — см. полный разбор и гипотезы расхождения в `docs/P5_HEDGED_LP.md`. Если подтвердится нижняя оценка ($79M) — базовая доходность пула примерно втрое ниже записанной, и kill-порог 30%/год нужно пересчитывать заново против реальной цифры, не одного высокого наблюдения.**

**Природа объёма пула — выяснена 2026-09-04 (Dune, `uniswap_v3_robinhood.uniswapv3pool_evt_swap`, реальные события на наш адрес пула, 21 837 100 строк).** Адрес `0x65050a9b7e5075a2ba5ced7b1b64ee66262c40dc` — доминирующий участник: **5 324 324 свопа за 7 дней (52.5% ВСЕХ свопов пула по количеству)**, одновременно топ-1 по объёму и как `sender`, и как `recipient` (в конкретных транзакциях sender=recipient — торгует сам с собой, вызывает пул напрямую, не через роутер), средний размер свопа ~$150, медианный своп по всему пулу — $76.87. Признак инсентивного/бот-объёма — **самоторговля и зеркальность sender/recipient**, а не формальная доля в деньгах (топ-3 по деньгам — 42.8%, ниже предрегистрированного порога 50%). **Владелец, 2026-09-04: вопрос закрыт, для LP такой объём — ПЛЮС** (адрес платит комиссию на каждый своп, не двигает цену диапазона) — не требует дальнейшего расследования как угроза, JIT-запрос (запрос 2) по этой же причине отменён. **Доп. проверка:** у `0x65050a` — **0 Mint-событий в этом пуле** (не LP, чистый трейдер/бот), и по недельной динамике именно его объём вырос с $169.6M (неделя 24.08, ранг 2) до $689.7M (неделя 31.08, ранг 1) — ровно на неделе общего скачка объёма пула, т.е. скачок в существенной части объясняется одним этим адресом, а не широким органическим ростом. Полные данные — `data/p3_guard_cache/dune_query1_volume_result.json`.

**Вердикт по P5 на текущем масштабе капитала (владелец, 2026-09-04):** реальная доходность даёт **~18%/год на полном капитале — ниже kill-порога 30%/год**. Решение: **не масштабировать**, позицию 1000756 держать как есть до истечения/явного решения о закрытии, **демон под конкретно этот пул не писать**. Ряд `data/p5_fee_accrual.jsonl` — добрать до 24 точек и на этом протокол P5 в текущем виде закрыть (не отдельная новая линия, а завершение уже начатой). **Если демон когда-либо будет написан** — делать пул-агностичным, с параметрами диапазона/порогов из конфига, не хардкодить под ETH/USDG 0.01%.

## 2. Инфраструктура

**Два VPS, один SSH-ключ (`VPS_SSH_KEY` секрет GH Actions, тот же ключ `extravm_dallas` на обоих):**

- **Даллас, США, `216.146.25.26`** — первый хост. **Непригоден для хеджа на Lighter в принципе** (не техническая проблема дата-центра) — ToS Lighter блокирует США как юрисдикцию целиком (см. §3). Остаётся в строю параллельно, не отключён.
- **Нидерланды, `45.59.170.76`** — рабочий хост, реально используется для всех Step 1 прогонов. Пользователь `bot` (без sudo), репозиторий в `/home/bot/robinhood-chain-alpha`, venv там же (`pip install -r analysis/requirements.txt` + `pip install git+https://github.com/elliottech/lighter-python.git`). ufw (22/tcp only) + fail2ban, `PasswordAuthentication no`.

**Секреты — НЕ через чат, только по SSH stdin в GH Actions job.** Лежат на VPS в `/etc/bot/env` (владелец `root:bot`, права `640`): `PRIVATE_KEY_NOX`, `LIGHTER_API_KEY_PUBLIC`, `LIGHTER_API_KEY_PRIVATE`. Кошелёк `0x893f4a7eADBa18c2f8aA1e0E23e11eCF66208e75`.

**systemd-юнит `/etc/systemd/system/p5-live-bot.service` — УСТАНОВЛЕН, НЕ включён** (`systemctl daemon-reload` выполнен, `enable`/`start` — нет). `ExecStart=.../venv/bin/python .../analysis/p5_live_bot.py` — **этот файл ещё не написан**, юнит будет падать при попытке запуска до тех пор. `Restart=always`, `RestartSec=10`, `StartLimitBurst=5`/`StartLimitIntervalSec=600`, `User=bot`, `NoNewPrivileges=true`. logrotate по размеру (`/etc/logrotate.d/p5-live-bot`, `size 200M rotate 5 compress`) на `/home/bot/robinhood-chain-alpha/logs/*.log` — настроен, проверен (`logrotate -d`).

**Почему НЕ GH Actions для самого демона:** платформенный потолок job'а — 6 часов; демону нужен непрерывный 60-секундный цикл наблюдения 24ч+ (цена пула, дельта, решение о ребалансе, предохранители) — между прогонами job'ов неизбежен зазор в мониторинге захеджированной позиции. GH Actions остаётся правильным инструментом для ВСЕГО ОСТАЛЬНОГО (разовые/точечные скрипты, деплой/bootstrap VPS, диагностика, сам Step 1 открытия).

**Исходящая сеть интерактивной сессии агента (перепроверено фактически 2026-09-04, прежняя запись «нет TCP-доступа» была неверна):** исходящий TCP из сессии открыт, в том числе напрямую без прокси (проверено `socket.connect` на `github.com:443` и на произвольный `8.8.8.8:443` — оба прошли). Отфильтрован конкретно **порт 22** — симптом `Connection timed out` (SYN уходит, ответа нет), воспроизведено на разных хостах (в т.ч. `github.com:22`), т.е. это фильтр в рантайме самой агентской песочницы, а не блокировка какого-то конкретного хоста. **Это ограничение — сознательный барьер, не баг, и его не обходят**: доступ к VPS остаётся ТОЛЬКО через маркер → GH Actions, ради аудируемости (каждое действие с хостом = коммит + лог), даже после того как выяснилось, что порт 22 технически можно было бы обойти сменой порта sshd. Разобранный случай, почему это не делается — §4, последний пункт.

Подробный план (изначальный, частично уже реализован в реальности иначе — см. выше): `docs/P5_LIVE_DEPLOY.md`.

## 3. Lighter — специфика площадки

- **Реальный торговый API-хост: `api.rh.lighter.xyz`.** `robinhoodchain.lighter.xyz` — это ФРОНТЕНД (веб-виджет), не API; не использовать для запросов из кода.
- Аккаунт **22012**, `l1_address = 0x893f4a7eADBa18c2f8aA1e0E23e11eCF66208e75`.
- **ToS блокирует список юрисдикций целиком** (blanket-запрет, не просто фильтр по одному эндпоинту): United States of America, Canada, United Kingdom, China, North Korea, Russia, Ukraine, Cuba, Iran, Venezuela, Sudan, Belarus, Myanmar, Syria. Найдено по факту (`analysis/lighter_jurisdiction_probe.py`, реальный `WebFetch` `lighter.xyz/terms`) после того, как реальная попытка `create_market_order` с США-хоста вернула `code=20558 "restricted jurisdiction"`. Публичные/signing-only вызовы (account/margin read, `check_client()`, `create_auth_token_with_expiry()`) с США-IP проходили весь день — блок проявляется именно на торговом пути.
- **Владелец подтвердил, что не резидент ни одной из перечисленных юрисдикций** — релокация сервера в разрешённую юрисдикцию (Нидерланды) легитимна, не обход персональных ограничений.

**Hyperliquid — юрисдикционные ограничения (для будущего логгера спреда фандинга Lighter↔Hyperliquid, п.0 задания владельца 2026-09-04).** Реальный текст `app.hyperliquid.xyz/terms`, "Terms of Use", last updated June 15, 2026 (получен рендерингом через Playwright/Chromium на GH Actions runner — `requests.get` без JS вернул только шелл SPA, см. `analysis/hyperliquid_jurisdiction_probe.py`):
  - **§1.6 "Restricted Persons" — НЕ отдельный список стран, а формула:** (a) лица/организации, проживающие/находящиеся/зарегистрированные в **США или Онтарио (Канада)**; (b) лица/организации в юрисдикциях под экономическими/торговыми санкциями или экспортным контролем ("Restricted Territories", без пофамильного перечисления на этой странице); (c) граждане Restricted Territories независимо от текущего местонахождения. Restricted Persons "строго запрещено" получать доступ к Interface.
  - **§1.7 "Excluded Persons"** — отдельная, ýже категория, применяется только к outcome-маркетам (предсказательные рынки), не ко всей платформе.
  - **Живое подтверждение блока, не только текст ToS:** сам фронтенд (`app.hyperliquid.xyz`) при заходе с IP GH Actions runner'а (вероятно США) показал баннер "You are accessing our products and services from a restricted jurisdiction" — то есть гео-блок реально применяется на уровне веб-интерфейса. **НЕ проверено отдельно**, распространяется ли то же самое на сам read-only `info` REST API (`/info`, `metaAndAssetCtxs`) — по аналогии с Lighter (см. выше) публичные/read-only эндпоинты нередко ходят даже с заблокированных для трейдинга IP, но это предположение, не факт для Hyperliquid — требует отдельной проверки перед тем, как полагаться на неё в логгере.
  - Диспуты — арбитраж LCIA в Лондоне, иск в суд США подавать нельзя (п.11.5).
  - `hyperliquid.xyz/terms` (без `app.`) вернул 403 при обоих методах (requests и Playwright) — недоступен для проверки этим способом, не влияет на вывод выше (тот же контент дублирован на `app.` поддомене).
- **Лот/шаг/минимальный нотионал ETH-перпа** (реальное чтение `GET /api/v1/orderBookDetails`, не из документации — владелец, задача расширения отчёта, 2026-09-04, снято `analysis/p5_live_position_snapshot.py`, `data/p3_guard_cache/p5_live_snapshot_1000756_result.json`, 2026-09-03T22:54:10Z): `min_base_amount = 0.0050 ETH`, `size_decimals = 4` ⇒ **шаг лота = 0.0001 ETH**, `min_quote_amount = $10.00` (минимальный нотионал ордера). Наш текущий хедж 0.0377 ETH (~$94 нотионал на момент снятия) далеко выше обоих минимумов — единственное ограничение на мелкость будущего ребаланса — шаг 0.0001 ETH, не сам минимальный лот.

## 4. РЕЕСТР НАЙДЕННЫХ БАГОВ (самое ценное — чтобы не наступить снова)

1. **`canceled-margin-not-allowed`** — реальная причина 0%/27%/0%/0% филла на 4 первых попытках хеджа (подтверждено аутентифицированным чтением `GET /api/v1/accountOrders`, не гипотеза). Корень: `p5_live_precheck.py` считал требуемую маржу по ЗАШИТОМУ допущению `MAX_LEVERAGE = 3.0`, а РЕАЛЬНОЕ плечо аккаунта было 2× (`initial_margin_fraction="50.00"`) — формула молча считала маржи достаточно, когда её не было. Опровергнуты по пути (реальными данными, не рассуждением) три другие гипотезы: Volume Quota Program (Premium-only, не относится к Standard-аккаунту), post_only/лимитный ордер (SDK не поддерживает такой параметр в этом пути), тонкая глубина книги (реально 412 ETH на 0.10% против запроса 0.0365 ETH). **Фикс:** `real_eth_leverage()` — плечо ВСЕГДА читается с аккаунта (`GET /api/v1/account`, публичный эндпоинт), допущение убрано из формулы совсем.
2. **`err=None` НЕ доказывает исполнение ордера.** `create_market_order`/`create_market_order_limited_slippage` может вернуть `200 OK, err=None` с реальным `tx_hash`, но без фактического филла (тело ответа содержит `"ratelimit": "didn't use volume quota"` — не ошибка, но и не подтверждение). **Фикс:** `_verify_hedge_filled()` — до 4 попыток свежего чтения РЕАЛЬНЫХ `positions()` после отправки, порог ≥85% запрошенного размера (не 50% — частичный 50-85% филл всё ещё существенная голая экспозиция).
3. **`actions/checkout@v4` shallow-clone ломал confirm-гейт.** С дефолтным `fetch-depth=1` `git log -1 --name-only` на корневом коммите (нет родителя) печатает ВСЕ файлы в дереве, не только реально изменённые в пуше — если CONFIRM-маркер хоть раз существовал где-то в истории, ЛЮБОЙ последующий push (в т.ч. чистый dry-run) детектился как РЕАЛЬНЫЙ. Воспроизведено локально (`git clone --depth 1`), эмпирически проверен фикс отдельным dry-run прогоном перед тем, как снова доверять гейту. **Фикс:** во всех confirm-гейтед воркфлоу — режим определяется ТОЛЬКО существованием файла-маркера (`[ -e MARKER ]`, без диффа по git log), маркер удаляется (`git rm -f --ignore-unmatch`) сразу после использования в шаге "Commit results" — не может "протухнуть" и сработать повторно.
4. **Gas-race.** Один `eth_gasPrice()` на весь пакет последовательных tx — base fee успевает подрасти, пока ждём квитанцию предыдущей tx, следующая падает `max fee per gas less than block base fee`. **Фикс:** `send_with_gas_retry()` — свежий `eth_gasPrice()` на КАЖДОЙ попытке + эскалирующийся буфер `[1.15, 1.4, 1.75]`, несколько попыток прежде чем жёсткий стоп.
5. **Across доставляет WETH (в одном реальном случае — через `MulticallHandler`-контракт, который сжигает WETH через `Transfer→address(0)` без сопутствующего `Withdrawal`-события, т.е. не канонический `WETH9.withdraw()`, механизм до конца не расшифрован, но handler в итоге не удерживает ни WETH, ни native ETH — не зависло), не гарантированно native ETH.** Сайзер читал только native-баланс кошелька, недооценивая реальный доступный капитал, сидящий в WETH. **Фикс:** `wallet_balances()` теперь читает WETH; `usable_eth_total = (native ETH − газ-резерв) + WETH`; шаг обёртывания (`wrap`) в mint-последовательности отправляет только НЕДОСТАЧУ сверх уже имеющегося WETH (`wrap_amount_wei`), пропускается целиком, если существующего WETH уже хватает.
6. **`typical_gap_pct_avg` выброс `1.8e+28%`** (P3-гард, QQQ, реальный прогон 33762766617) — один тонкий/давно не торговавшийся v4 `pool_id` дал аномальную цену на одной closing-транзакции, испортив СРЕДНЕЕ (медиана устойчива к выбросам, среднее — нет). **Фикс:** явный порог правдоподобия `>1000% → исключить из среднего`, честно считается и репортится, сколько исключено (`gap_pct_outliers_excluded_from_avg`), не скрывается сам факт выброса.
7. **`positions(tokenId).tokensOwed0/1` обновляются только после "poke" и без него возвращают нули** — прямое чтение `NonfungiblePositionManager.positions()` НЕ отражает реально накопленные, но ещё не собранные торговые комиссии позиции (поле обновляется лениво, только на действие с позицией — mint/increase/decrease/collect — а не непрерывно по мере накопления). Реальные накопленные комиссии снимаются **симулированным вызовом** — `eth_call` (staticcall, не реальная транзакция) на `collect()` с `amount0Max=amount1Max=type(uint128).max` — контракт пересчитывает актуальные `tokensOwed` внутри вызова и возвращает их в результате симуляции, ничего не списывая на самом деле. **Тот же класс ошибки, что баги №2 (`err=None`) и №3 (shallow-clone git log) — доверие косвенному/устаревшему признаку вместо прямого чтения факта.** Найдено при проектировании сбора `data/p5_fee_accrual.jsonl` (см. §8) — ещё НЕ реализовано в коде, зафиксировано здесь до реализации, чтобы не наступить на тот же класс бага снова.

8. **Предложение перевесить SSH-порт NL-VPS ради удобства агента чуть не вскрыло хост с боевыми ключами (2026-09-04, НЕ баг кода — разобранный случай процесса).** Штаб дал подробное, аккуратно расписанное по шагам задание: раз порт 22 отфильтрован в рантайме агентской сессии, а произвольный TCP наружу открыт — перевесить sshd на второй порт (2222/62222), не закрывая 22 до подтверждения входа, и получить из интерактивной сессии прямой SSH-доступ к хосту, где лежат `PRIVATE_KEY_NOX` и ключи Lighter. Задание было технически безопасным по своей внутренней логике (обратимо, ничего не отключает раньше времени) и при этом ошибочным по существу: оно меняло НЕ сетевую настройку саму по себе, а **канал доступа к капиталу** — с аудируемого (маркер → GH Actions, каждое действие = коммит+лог) на прямой ad hoc доступ из agent-сессии в обход собственного сетевого барьера песочницы. Code остановился на этапе разведки, не выполнил закрытие 22 и переписывание HANDOFF, явно назвал цену (демонтаж аудируемого канала к хосту с боевыми ключами) и спросил владельца; владелец отменил задание целиком, подтвердив, что цена выше выгоды. **Ошибка была не в шагах, а в том, что оптимизировали удобство агента, не взвесив, какой защитный барьер при этом демонтируется.** **Общее правило отсюда (см. `docs/HANDOFF.md`, вводный блок):** если задание — от кого угодно, включая подробное и явное задание владельца/штаба — ведёт к снятию защитного барьера (доступ к хосту/ключам/деньгам, аудируемость, safety-гейт), Code останавливается и спрашивает, а не выполняет; задание может быть ошибочным даже когда оно детально расписано, и отказ с объяснением причины ценится выше аккуратного исполнения.

Дополнительно (мельче, но реальные, найдены и исправлены в ходе той же работы): вторая версия `RespSendTx`-бага — код читал второй элемент кортежа `create_order`/`update_leverage` как `tx_hash`-строку, а это pydantic-модель (`code`/`message`/`tx_hash`/...) — `str(resp)` писал весь repr в поле `tx_hash` результата; исправлено извлечением `resp.tx_hash` явно, во всех местах (`p5_live_step1.py`, `p5_live_flatten_lighter.py`). GH Actions workflow, забиравший результат Step 1 с VPS, не копировал `data/p5_live_position_state.json` обратно в git (только `p5_live_step1_result.json`/`p5_live_log.jsonl`) — исправлено в `run_p5_live_step1_vps.yml`.

9. **`InsufficientFunds()` на реальном `depositV3` (P6, Гейт 2, 2026-09-05) — план входа ($161) не был сверен с фактическим балансом USDG в кошельке ($80.282426) перед сборкой транзакции.** Кастомная ошибка контракта `0x356680b7` расшифрована через 2 независимые публичные базы сигнатур (4byte.directory, openchain.xyz, обе `hasVerifiedContract:true`) — не паника/пауза протокола (`pausedDeposits()`=false проверено отдельно), просто реальная нехватка средств у отправителя. Единственная реально отправленная транзакция (`approve`) безвредна, деньги не двигались. **ПРАВИЛО РАБОТЫ (владелец, 2026-09-05, обязательно для ЛЮБОЙ реальной денежной ноги, не только P6):** перед КАЖДОЙ транзакцией, двигающей средства (мост/своп/mint/маржа под шорт и т.п.) — реальное чтение баланса НУЖНОГО токена/коллатерала на НУЖНОЙ сети И явная сверка с суммой транзакции, СТОП с точной цифрой при нехватке, никогда не полагаться на ранее посчитанный план без переподтверждения чтением непосредственно перед отправкой. **Реализовано** как `preflight_balance()` в `analysis/p6_live_step1.py` — вызывается перед мостом (USDG+ETH на Robinhood Chain), перед свопом (USDC на Base), перед mint (USDC+cbBTC на Base), перед шортом (коллатерал на Lighter, пересчитан свежим mark price). Тот же класс дисциплины, что баги №1/№2/№7 выше (доверие плану/косвенному сигналу вместо прямого свежего чтения факта) — теперь явное общее правило, не привязанное к одному протоколу.

10. **`LOW_BRIDGE_LIQUIDITY` (P6, Гейт 2, реальный вход 2026-09-05) — реальный `400` от Across API на газовом ETH-мосте (0.0006 ETH), тело ответа `{"code":"LOW_BRIDGE_LIQUIDITY","message":"Amount is higher than available liquidity."}`.** Подтверждено повторным идентичным запросом (не транзиентно) — реальная нехватка ликвидности релеера на тонком/новом маршруте Robinhood Chain→Base для нативного ETH, не баг кода. **Фикс:** `across_quote()` теперь печатает реальное тело ответа при не-200 статусе ДО `raise_for_status()` (иначе тело ошибки терялось); `BRIDGE_ETH_AMOUNT_ETH` снижен с 0.0006 до 0.00025 (чуть выше реального `minDeposit` для этого маршрута, ~0.0002036 ETH).

11. **Транзиентный лаг RPC-реплики `mainnet.base.org` — `eth_estimateGas` реально падал с revert сразу после только что замайненного `approve` на ТОМ ЖЕ кошельке.** Реальная перепроверка (`analysis/p6_debug_swap_stf.py`): allowance/баланс на самом деле были в порядке, повторный `eth_estimateGas` через несколько секунд сразу проходил без изменений в состоянии — вывод: конкретный backend-узел за публичным RPC ещё не увидел предыдущий блок (несколько узлов за одним доменом, не идеально синхронны). **Фикс:** `eth_estimate_gas_with_retry()` — ретрай с паузой (5с, 15с) перед тем, как считать revert реальным и останавливаться.

12. **Неполный idempotency-guard при возобновлении реального входа после сбоя (P6, run 11→12).** Первая версия guard'а проверяла только «мост уже сделан» (по балансу USDC на Base) — при возобновлении ПОСЛЕ уже успешно сделанного свопа (который реально уменьшил баланс USDC ниже порога) guard не срабатывал, скрипт пытался мостить ЗАНОВО. Реально остановлено БЕЗОПАСНО существующим `preflight_balance()` (баг №9) — реального USDG на кошельке больше не было, ни цента не потеряно, но попытка не продвинулась. **Фикс:** guard расширен — проверяется реальный баланс cbBTC на Base ДО шага моста; если есть (свыше пыли ~$0.8) — значит и мост, и своп уже сделаны в прошлой попытке, оба пропускаются целиком, скрипт идёт прямо к mint с текущими реальными балансами. **Урок:** idempotency-guard должен резюмировать с ЛЮБОЙ реально достигнутой точки прогресса, а не только с первого шага — частичный guard хуже, чем его отсутствие (создаёт ложное чувство защищённости).

13. **`PSC` (`LiquidityManagement.sol`: `require(amount0>=amount0Min && amount1>=amount1Min, "PSC")`) на `mint` (P6, run 11 и 13) — ДВЕ разные реальные причины подряд.** (а) Первая: `amount0_desired`/`amount1_desired` считались как ВЕСЬ имеющийся баланс обоих токенов (а не оптимум под тик-диапазон), `amount_min` = 95% от него — при малейшем несовпадении реального соотношения с требуемым для диапазона одна нога уходит ниже 95%. Фикс по паттерну P5 (`expected0`/`expected1` из `get_liquidity_for_amounts`) — считать ОПТИМАЛЬНЫЕ суммы под диапазон, не брать весь баланс. (б) Тот же фикс реально упал ПОВТОРНО: оптимум считался по **номинальным** границам ±10% (`p0*(1±0.10)`), а РЕАЛЬНЫЙ ончейн-диапазон — это границы, округлённые до `tickSpacing` этого пула (2000) — при `1.0001**2000 ≈ 1.221` один шаг тика даёт ~22% цены, так что округление реально увело границы далеко от номинала (проверено на реальных числах: реально $60 165–$89 754 против номинальных $71 732–$87 672, т.е. -16%/+2.4%). Пул считает amount0/amount1 СТРОГО по реальным (округлённым) границам, не по номиналу. **Фикс:** `tick_to_usd_price()` — точная обратная функция к `usd_price_to_tick()`, даёт реальную USD-цену на границе КОНКРЕТНОГО округлённого тика; оптимум для mint теперь считается по НЕЙ, не по номиналу. `MINT_SLIPPAGE` увеличен с 0.05 до 0.10 (запас на остаточный сдвиг цены за реальное время двух approve-транзакций между расчётом и самим mint). **Урок:** при большом `tickSpacing` (широкие "квантованные" ценовые шаги) НИКОГДА не использовать номинальный процентный диапазон напрямую в математике размера позиции — всегда выводить реальные границы из округлённых, отправляемых on-chain тиков.

Все 4 находки — за один реальный вход (P6, Гейт 2, 2026-09-05, RESULTS.md §8), исполненный на 4-й попытке (run 10/11/13/14) без потери средств на неудачных попытках (только газ, ≈$0.01 суммарно) — каждая остановка была безопасной (либо `preflight_balance()`, либо реальный revert ДО отправки/списания через `eth_estimateGas`).

14. **`tickSpacing=2000` на пуле P6 (Aerodrome Slipstream USDC-CBBTC, Base) делает номинальный диапазон ±10% НЕДОСТИЖИМЫМ — минимальный шаг диапазона (1 tickSpacing) даёт ~22% цены.** `1.0001**2000 ≈ 1.2214` — один шаг тика на этом пуле меняет цену почти на четверть, поэтому ЛЮБОЙ реальный диапазон на этом пуле квантован крупными скачками ~22%, не плавно. Прямое следствие бага №13(б). **Реально посчитанные (не смоделированные) значения k-концентрации диапазона** (формула `1/(1-sqrt(Pa/Pb))`, `analysis/p6_reposition_dryrun.py::position_concentration_k`, реальные тиковые границы через `tick_to_usd_price()`):
    - Фактически исполненный вход (диапазон `[-68000,-64000]`, 2 шага tickSpacing, реальные границы $60 165–$89 754) — **k=5.517**. Заметно НИЖЕ номинала, потому что реальный диапазон оказался вдвое шире задуманного (округление в mint-математике на момент входа не давало выбрать более узкий диапазон без ручной перестройки логики шага диапазона).
    - Номинальный ±10% (`p0·0.9`…`p0·1.1`), недостижимый на этом `tickSpacing` напрямую — **k=10.475** (для сравнения, это была подразумеваемая цель).
    - Предложенная перестановка на 1 шаг tickSpacing (`[-68000,-66000]`, реальные границы $73 485–$89 754, dry-run 2026-09-05, `data/p3_guard_cache/p6_reposition_dryrun_result.json`) — **k=10.509**, почти ТОЧНО совпадает с недостижимым номиналом ±10% — то есть узкий (однотиковый) диапазон реально даёт ту концентрацию, которая изначально подразумевалась под "±10%", просто не в виде симметричного процента, а в виде одного шага сетки тиков.
    - Пуловый (не позиционный) `k_concentration` (другая метрика — концентрация ВСЕЙ активной ликвидности пула относительно TVL, `compute_k_concentration` в `p6_hourly_snapshot.py`) — реально наблюдался в диапазоне **9.88–10.10** на разных снимках 2026-09-05. Владелец называл **10.6** (реально) и **21.5** (модель) — первое совпадает по порядку величины с пуловым k выше (вероятно, та же метрика на другом снимке/со своим источником); **"21.5" НЕ найдено нигде в репозитории как задокументированный собственный расчёт** — зафиксировано здесь СО СЛОВ владельца, не переподтверждено независимо (см. `никогда не выдумывай данные` — не реконструирую задним числом гипотезу, откуда взялось 21.5, вместо этого рядом приведены реально пересчитанные числа с явной формулой и источником).
    - **Пересчёт ожидания доходности:** т.к. `lvr_annualized_frac = k·σ²/8` и `fee_apr` растут (в первом приближении) пропорционально `k` при прочих равных, фактический вход с k=5.517 даёт примерно ВДВОЕ меньшую концентрацию (следовательно, при прочих равных — примерно вдвое меньшую долю комиссий пула на доллар капитала) относительно того, что предполагал номинал ±10% (k=10.475) или предложенная перестановка (k=10.509). Kill-порог №1 (`fee_lvr_ratio<3`) и ожидания APY, calibровавшиеся под k~10-21, стоит перепроверять именно против РЕАЛЬНОГО k=5.517 для уже открытой позиции, пока она не переставлена.
    - **Реальный ранний сигнал (2026-09-05, снимок №3, 36 минут после входа):** `kill_flag_1_fee_lvr_ratio_lt_3` = **true** (`fee_lvr_ratio=0.880`, `sigma_realized_annualized=0.149`, `k_concentration=9.877`) — ПЕРВЫЙ раз, когда флаг реально стал вычислимым (нужно было ≥3 точки своего ряда для sigma). Тот же класс нестабильности, что документирован в P5 (`p5_live_position_snapshot.py`: "fee_lvr_ratio НЕУСТОЙЧИВ, sigma на малой выборке крайне шумная -- для решений не используется") — 3 точки за 36 минут дают статистически ненадёжную аннуализированную sigma, флаг НЕ считается решающим сигналом на этой стадии, но заслуживает отслеживания по мере накопления часовых точек.

15. **Честная сверка формулы `k` (владелец, 2026-09-05) — подтверждено чтением кода: k пула (скринер + снимок) и k диапазона (dry-run перестановки) это РАЗНЫЕ формулы, отвечающие на разные вопросы, не взаимозаменяемые.**
    - `pool_screener_concentration.py::compute_k` И `p6_hourly_snapshot.py::compute_k_concentration` — ОДНА И ТА ЖЕ формула: `k_pool = L_active_human / L_full_human`, где `L_active_human` = реальная ончейн `liquidity()` (суммарная активная ликвидность ВСЕХ LP пула прямо сейчас), `L_full_human = sqrt(amount0_full·amount1_full)` при гипотетическом депозите ВСЕГО TVL 50/50 на весь диапазон цен. Отвечает "насколько концентрирован ВЕСЬ пул сейчас" — НЕ параметризована каким-либо одним диапазоном (у пула их много, у разных LP).
    - `p6_reposition_dryrun.py::position_concentration_k` — ДРУГАЯ формула: `k_range = 1/(1-sqrt(Pa/Pb))` для КОНКРЕТНОГО диапазона `[Pa,Pb]`. Отвечает "во сколько раз этот выбранный диапазон эффективнее full-range для ОДНОГО депозитора" — чистая геометрия, не требует знания TVL/liquidity() пула вообще.
    - **Честный перевод одного в нормировку другого** (владелец: "если нет — пересчитать пул в той же нормировке") — сделан РЕАЛЬНО, не через обратное решение уравнения: `analysis/p6_pool_real_shelf_k.py` реально прочитал `TickBitmap(wordPos)` + `ticks(tick)` (поле `liquidityNet`, порядок полей сверен с реальным исходником `Tick.sol`, aerodrome-finance/slipstream, WebFetch 2026-09-05: `liquidityGross,liquidityNet,stakedLiquidityNet,...`) вокруг текущего тика и нашёл РЕАЛЬНУЮ "полку" постоянной активной ликвидности — **ровно `[-68000,-66000]`**, т.е. буквально ТОТ ЖЕ диапазон, что предложенная перестановка (см. №14). `k_range` для этой реальной полки = **10.509** — практически совпадает и с k_range предложенной перестановки (10.509, тот же диапазон), и по порядку величины с пуловым `k_pool` (9.88–10.10). **Вывод:** перестановка ставит нашу позицию в ТУ ЖЕ концентрацию, которую уже реально использует другая активная ликвидность этого пула прямо сейчас — не гипотетическое улучшение, а подтверждённое реальными ончейн-данными соответствие рыночной норме.
    - Владельческие **10.6**/**21.5** остаются нереконструированными (см. №14) — рядом теперь есть независимая, полностью ончейн-выведенная точка сравнения (10.509 из реальной полки), а не только "недостижимый номинал" (10.475).

16. **Реальная проверка заявленной 30-дневной волатильности (владелец назвал σ=37.47%) — РАСХОДИТСЯ с реально пересчитанной.** `analysis/p6_hist_sigma_30d.py` — реальный часовой OHLCV-скан GT (720 точек, полное покрытие 30 суток, тот же метод лог-доходностей, что `sigma_realized_annualized_from_series`/P5): **σ_realized_30d = 41.66%**, НЕ 37.47% (расхождение ~4.2 п.п., ~11% относительно — возможно другое окно/метод у владельца, не установлено, не реконструировано задним числом). **Используется РЕАЛЬНО ПОДТВЕРЖДЁННОЕ значение (41.66%)**, не озвученное, для `fee_lvr_ratio_at_hist_sigma` в `p6_hourly_snapshot.py` (справочный ряд рядом с шумным по своей истории `kill_flag_1` — сам флаг НЕ переопределён этой величиной, остаётся на короткой реализованной sigma, как решил владелец изначально).

17. **DefiLlama `apyBase` для пула P6 (USDC-CBBTC, Aerodrome Slipstream, Base) РЕАЛЬНО НЕДОСТОВЕРЕН — завышен на порядок относительно реального дохода от торговых комиссий, отдельно от механики staked/unstaked ниже.** (владелец, 2026-09-05, "перестановка отложена, сначала три проверки"; `analysis/p6_fee_mechanism_investigation.py`, `data/p3_guard_cache/p6_fee_mechanism_investigation_result.json`.)
    - **П.1 — реальный fee APR из часового объёма GT (не DefiLlama):** TVL=$7 025 318, объём 24ч=$8 303 802, объём 7д=$45 139 040 (24/168 часовых свечей, полное покрытие). `fee_apr = объём × 0.033% × 365 / TVL` → **fee_apr_24h=14.24%, fee_apr_7d=11.06%**. Сравнение с ПЕРВОНАЧАЛЬНО записанным на скрининге (§"Скринер пулов" выше): `apyBase=548.58%`, `apyBase7d=279.08%`, `apyMean30d=178.84%` — **реальный доход в 12–50 РАЗ ниже даже самой консервативной из заявленных цифр (178.84%)**, устойчиво на всех временных окнах (24ч и 7д дают близкие ~11–14%, не выброс одного дня). **Это НЕ "баг сбора" (гипотеза ~180%) — реальный сбор комиссий пулом в принципе примерно на порядок меньше, чем показывал DefiLlama.** Соответствует исходу "DefiLlama непригоден для оценки Aerodrome Slipstream" из трёх названных владельцем.
    - **П.2 — механика Aerodrome (staked/unstaked) — РЕАЛЬНО ПРОЧИТАН ИСХОДНИК** (`CLPool.sol`, `aerodrome-finance/slipstream`, WebFetch 2026-09-05, не документация): комиссия со свопа делится между **застейканной** (в gauge, `stakedLiquidity`) и **незастейканной** (наша позиция, минченная напрямую через NFPM без стейкинга) ликвидностью; **сверху** с незастейканной доли снимается ДОПОЛНИТЕЛЬНАЯ `unstakedFee()` (per-million, читается напрямую с пула, не предполагается) в пользу того же gauge. Реально на этом пуле СЕЙЧАС: `liquidity()`=1 250 696 499 757, из них `stakedLiquidity()`=1 037 062 751 843 (**82.9% всей активной ликвидности застейкано**), unstaked=213 633 747 914 (17.1%), `unstakedFee()`=100 000/1 000 000 (**10%**). Дальше — `feeGrowthInside`/`position.update()`, ТА ЖЕ механика, что Uniswap v3 core (комиссии реально накапливаются на незастейканной позиции через `tokensOwed`, подтверждено — см. п.3), но от суммы после 10%-го вычета и делённой на (liquidity−stakedLiquidity), а НЕ на весь `liquidity()`. **Это ограничение скринера:** `pool_screener_concentration.py` и вся оценка `apyBase/LVR` (k-поправка включительно) считали доходность от ОБЩЕЙ ликвидности пула — ни DefiLlama, ни скринер не могли учесть (и не учитывали) реальное разделение staked/unstaked, специфичное для Aerodrome/Velodrome-форков с gauge-стейкингом. Для НЕЗАСТЕЙКАННОЙ позиции (любой, открытой напрямую через NFPM, не только нашей) реальный ожидаемый доход систематически НИЖЕ наивной оценки "объём×fee/TVL" ещё и по этой причине, независимо от вопроса о качестве данных DefiLlama выше.
    - **П.3 — наши реальные комиссии (`eth_call collect`-симуляция) против ОЖИДАЕМЫХ по правильной (staked/unstaked-скорректированной) формуле:** реально `fee0=0.000152 USDC, fee1=0 cbBTC` (~$0.000152) за ~2 часа с реальным объёмом пула за этот период $21 684. Ожидаемо по формуле `объём×0.0297%(после 10%-выреза)×наша_доля_unstaked-ликвидности(5.547e-5)` = **$0.000358**. Реальное/ожидаемое = **0.425** — реальный сбор вдвое НИЖЕ скорректированного ожидания, но **не на порядки и не ноль**: при абсолютных суммах в десятые доли цента и всего 1 часовой свече объёма это статистически ОЖИДАЕМЫЙ шум (дискретность реальных свопов/блоков относительно точного момента открытия позиции, округления в фиксированной точке контракта на таких масштабах) — **преждевременно называть это багом сбора**, накопление реально идёт (`tokensOwed`/`feeGrowthInside` реально растут), нужно больше часов данных для надёжного вывода. Отдельно подтверждает: коллекция комиссий на незастейканной позиции РАБОТАЕТ (не нулевая, не сломана), т.е. исход "механизм сбора сломан" (~180%, наша реализация) **реальными данными не подтверждён**.
    - **Итоговый вердикт по трём названным владельцем исходам: подтверждены №2 и №3 одновременно, №1 (сбор сломан) — НЕ подтверждён.** DefiLlama `apyBase` для этого пула реально непригоден (расхождение на порядок, устойчиво по времени) — это самостоятельная, достаточная причина рассмотреть закрытие P6 по предрегистрации (реальная доходность пула ~11–14%/год ДО вычета доли LP, что уже близко к или ниже действующего kill-порога 30%/год до какого-либо дальнейшего вычета доли позиции). Механика Aerodrome (staked/unstaked) — реальное, отдельное структурное ограничение скринера, ухудшающее реальную экономику ЛЮБОЙ незастейканной позиции на форках с gauge-стейкингом сверх и без того завышенной базовой цифры DefiLlama; **скринер (`pool_screener_concentration.py`) для ВСЕХ 23 кандидатов нуждается в пересчёте по реальному GT-объёму вместо DefiLlama `apyBase`**, аналогично сделанному здесь для P6 — не выполнено в этом прогоне (по объёму работы, не по решению), фиксируется как следующий шаг.

18. **ПРАВИЛО РАБОТЫ (владелец, 2026-09-05, обязательно для ЛЮБОГО будущего пула/протокола, не только P6): "APR агрегатора — гипотеза, не факт."**
    - Перед входом в любой пул: `fee_apr = объём(GT, реальные часовые OHLCV, 7д И 30д отдельно) × fee_tier × 365 / TVL`, независимо от `apyBase`/`apyMean` любого агрегатора (DefiLlama и т.п.). **Расхождение агрегатора с этим реальным числом более чем в 2× — СТОП и разбор** (реальная причина: другая методология агрегатора, staked/unstaked-специфика форка, устаревшие данные и т.п. — не входить, пока расхождение не объяснено).
    - **Для gauge-форков** (Aerodrome, Velodrome и однотипные, где есть разделение liquidity на staked (в gauge) / unstaked): реальный доход НЕЗАСТЕЙКАННОЙ позиции = `fee_apr_наивный × (unstaked_liquidity / total_liquidity) × (1 − unstakedFee)`, где `stakedLiquidity()`/`liquidity()`/`unstakedFee()` (или их аналоги под другим именем в конкретном форке) — читаются НАПРЯМУЮ с контракта пула (реальные view-функции, не предполагаются, не берутся из документации протокола) непосредственно перед оценкой/входом, т.к. это динамические, реально меняющиеся во времени величины.
    - Найдено и формализовано после реальной проверки P6 (см. №17 выше): DefiLlama `apyBase` для Aerodrome Slipstream USDC-CBBTC разошёлся с реальным GT-объёмным расчётом в 12–50 раз — правило родилось из конкретного, а не гипотетического инцидента.

19. **`"PS"` (`NonfungiblePositionManager.sol`: `require(amount0>=amount0Min && amount1>=amount1Min, "PS")`, "Price Slippage") на `decreaseLiquidity` при реальном закрытии P6 (2026-09-05) — та же категория ошибки, что баги №5(а)/№13, но в НОВОМ месте (закрытие, не mint), и уже задокументированная РАНЕЕ в `p5_live_close.py` — не перенесена в P6 при написании `p6_live_close.py`.** Реальная причина: raw ончейн `liquidity` (из `positions()`) была скомбинирована со sqrt в "человеческом" домене (`1/p_usd`, тот же, что `get_liquidity_for_amounts`/`v3_amounts` используют для расчёта ЖЕЛАЕМЫХ сумм при mint) — это ДВА РАЗНЫХ домена. Результат — `expected_amount0_usdc=438.38` при реальном депозите ~$43.7 (в 10 раз завышено), `amount_min` считался от этого мусора, реальный `burn()` вернул на порядок меньше — revert. Транзакция НЕ была отправлена (упала на `eth_estimateGas`), LP осталась цела, но **на несколько минут образовалась реально непрохеджированная LP-позиция** (хедж уже был закрыт первым по указанному владельцем порядку) — не критично по размеру (~$19 нотионала), устранено быстрым фиксом и повтором. **Фикс:** raw `liquidity` комбинируется ТОЛЬКО с СЫРЫМ `sqrtPriceX96/2^96` (без price-адаптации) — `sqrt_p_raw = sqrtPriceX96/2**96`, `sqrt_pa/pb_raw = (1.0001**tick)**0.5`, результат сразу в raw wei-единицах, делится на `10**decimals` в конце. Проверено вручную на реальных числах ДО отправки (дало ~43.85 USDC/~0.000244 cbBTC, совпало с фактом после реальной отправки — 43.838428 USDC/0.00024262 cbBTC). **Урок, теперь дважды подтверждённый (P5 и P6):** при закрытии позиции (в отличие от открытия/mint, где всё считается в "желаемом" человеческом домене с нуля) — единственный корректный путь получить amount0/amount1 из реальной ончейн-`liquidity()` — сырой `sqrtPriceX96`-домен; при переносе кода открытия/закрытия между протоколами явно перепроверять, какой домен используется в каждой конкретной формуле, а не полагаться на "похожий" код из другого файла того же проекта.

20. **Гонка чтений "живое vs кэш" дала отрицательный `unstaked_share` (пересчёт скринера по 23 пулам, 2026-09-05, `analysis/pool_screener_gt_recompute.py`, первый прогон, run 33960204306).** Формула staked/unstaked-поправки (правило №18) сравнивала `total_liquidity_raw`, взятый из СТАРОГО кэша (`pool_screener_concentration_result.json`, прогон предыдущего дня), со свежепрочитанным ЖИВЫМ `stakedLiquidity()` — на активных пулах реальная суммарная `liquidity()` меняется между прогонами (LP добавляют/убирают ликвидность), поэтому на 3 из 8 aerodrome-slipstream-пулов `staked > total(устаревший)`, `unstaked_share` вышел отрицательным, `fee_apr_corrected` — тоже отрицательным (бессмысленно). **Тот же класс ошибки, что баг №19** (смешивание данных из разных моментов времени в одной формуле), только на уровне двух РАЗНЫХ eth_call, а не двух РАЗНЫХ доменов величины. **Фикс:** `total_liquidity` теперь читается ЖИВЬЁМ (`liquidity()`) в ТОМ ЖЕ вызове `get_gauge_correction()`, что и `stakedLiquidity()`/`unstakedFee()`, не берётся из кэша другого скрипта/прогона. Перепроверено повторным прогоном (run 33960680496) — все `unstaked_share` реально в `[0,1]`, P6-пул (index 8) остался согласован с ранее подтверждённым числом (`unstaked_share≈0.175`, было 0.171 в баге №17 — реальный небольшой дрейф между двумя прогонами, ожидаемо). **Урок:** при читении НЕСКОЛЬКИХ величин с одного контракта для одной формулы — всегда один и тот же снимок (последовательные `eth_call` в одном прогоне достаточно близки по времени, чтобы считаться консистентными; смешивание с кэшем другого дня/скрипта — нет).

21. **[ПЕРВЫЙ ПРОХОД, ЦИФРЫ НИЖЕ УСТАРЕЛИ — реальная sigma-ошибка найдена и исправлена калибровкой, см. баг №22, актуальная таблица там же] Честный пересчёт скринера по всем 23 кандидатам реальным GT-объёмом вместо DefiLlama `apyBase`, выполнено 2026-09-05 (правило №18)** — `analysis/pool_screener_gt_recompute.py`, `data/p3_guard_cache/pool_screener_gt_recompute_result.json`, воркфлоу `run_pool_screener_gt_recompute.yml` (только публичный RPC + GT API, без VPS/SSH/Lighter — не денежная операция).
    - **Метод:** `fee_tier` читается ЖИВЬЁМ с контракта пула (`fee()` — Uniswap v3 и Aerodrome Slipstream используют ОДНУ сигнатуру, подтверждено WebFetch `CLPool.sol`; для aerodrome-v1 — `factory()`+`stable()` с пула → `getFee(pool,stable)` с фабрики, знаменатель 10000, подтверждено WebFetch `Pool.sol`+`PoolFactory.sol`; uniswap-v4 — метод не применяется, тот же честный отказ, что в `pool_screener_concentration.py`). Объём и close-цены — реальный часовой OHLCV с GT за 30 дней (одно окно на пул), из него: `volume_7d`, `volume_30d`, и РЕАЛЬНАЯ 30-дневная `sigma_realized_annualized` (НЕ старая ~41-дневная sigma из `pool_screener_sigma_lvr_result.json` — честно отдельное число). `fee_apr_gt_Xd = (volume_Xd/X) × fee_tier × 365 / TVL`. Для aerodrome-slipstream — поправка на staked/unstaked (правило №18, п.2), читается живьём в одном снимке (см. баг №20). `ratio = fee_apr_corrected / (k×sigma_30d²/8)`, порог ≥2 (`k` — из `pool_screener_concentration_result.json`, k=9.9 для P6-пула — тот же, что называл владелец).
    - **Итог: 21/23 реально посчитано** (2 uniswap-v4 — N/A, объективная причина). **5 строк проходят порог ≥2**, но 2 из них — ДУБЛИКАТЫ одного и того же физического пула (см. ниже) → **4 РАЗЛИЧНЫХ реальных пула проходят порог честно:**

      | Сеть | Пара | Адрес пула | fee tier | k | `fee_apr_gt_30d` (с поправкой) | ratio к LVR@σ30д | DefiLlama `apyBase` | расхождение (DefiLlama/реальное) |
      |---|---|---|---|---|---|---|---|---|
      | Base | WETH/USDC | `0xd0b53d92...798f224` | 0.05% | 11.31 | **29.0–30.3%/год** (2 листинга DefiLlama, TVL расходится, см. ниже) | 10.5–11.0 | 12.1–51.2% (2 листинга) | 0.4x / 1.77x |
      | Arbitrum | WETH/USDC | `0xc6962004...388e09e8d0` | 0.05% | 8.94 | **23.7%/год** | 9.14 | 38.39% | 1.62x |
      | Arbitrum | WBTC/USDC | `0x0e483131...92dee15b44f` | 0.05% | 12.54 | **18.2%/год** | 6.11 | 32.57% | 1.79x |
      | Arbitrum | WETH/USDC | `0xc473e2ae...b059a3b57c` | 0.30% | 8.40 | **22.0%/год** | 2.81 | 38.34% | 1.74x |

    - **Найдена реальная дубликация в исходных данных DefiLlama:** кандидаты №2 (aerodrome-slipstream WETH-USDC) и №5 (uniswap-v3 WETH-USDC), а также №1 (aerodrome-slipstream WETH-CBBTC) и №3 (uniswap-v3 WETH-CBBTC) резолвятся GT на ОДИН И ТОТ ЖЕ физический адрес пула — т.е. DefiLlama несёт ОДИН реальный ончейн-пул под ДВУМЯ разными записями с разными тегами `project` и (что важнее) РАЗНЫМ TVL (`$8 378 622` vs `$8 751 716` для пары WETH-USDC; `$9 438 483` vs `$8 630 938` для пары WETH-CBBTC). **Объективное подтверждение, что тег `aerodrome-slipstream` для этих двух записей — ошибочный:** реальный `eth_call` на `stakedLiquidity()`/`unstakedFee()` для ОБОИХ этих адресов **ревертит** ("execution reverted", без причины) — на настоящих Aerodrome Slipstream CL-пулах (подтверждено на P6-пуле и на ещё 2 других в этом же прогоне) эти функции реально отвечают. Вывод: физически это Uniswap v3-пулы, дубль-запись с неверным `project` в данных DefiLlama — не баг нашего кода, находка о качестве данных агрегатора. Посчитанные для них числа выше (строка "Base WETH/USDC") корректны как обычный Uniswap v3 (без gauge-поправки, других вариантов и нет).
    - **Систематическая картина расхождения DefiLlama `apyBase` vs реальный GT-объёмный расчёт — на 19 из 21 реально посчитанных пулов DefiLlama ЗАВЫШАЕТ**, обычно в 1.3–2.0x (медианно ~1.6–1.8x), но встречаются экстремумы в обе стороны: **P6-пул (USDC-CBBTC, Aerodrome Slipstream, index 8) — расхождение 352.7x** (DefiLlama 548.58% против реального 1.56%/год с учётом gauge-поправки — прямая причина закрытия P6, баг №17); один из дубль-листингов Base WETH-USDC (тег aerodrome-slipstream, TVL, вероятно, включающий что-то, что второй листинг не включает) — единственный случай, где DefiLlama, наоборот, ЗАНИЖАЕТ (0.4x). **Подтверждает правило №18 на всём наборе, не только на одном пуле P6:** DefiLlama `apyBase` для этого класса пулов (concentrated liquidity, особенно gauge-форки) систематически ненадёжен, использовать реальный GT-объёмный расчёт ОБЯЗАТЕЛЬНО перед любым входом.
    - **Честные методологические ограничения (не подгонка, отдельно обозначено в самих данных):** (а) **aerodrome-v1** (3 кандидата, все НЕ проходят порог) — gauge-поправка НЕ смоделирована (механика классического Solidly-форка другая: per-LP `supplyIndex`, не liquidity-based split, как у CL) — показанные числа `fee_apr` для них являются ВЕРХНЕЙ ГРАНИЦЕЙ без вычета доли gauge; (б) **uniswap-v4** (2 кандидата) — N/A, тот же честный отказ, что в `pool_screener_concentration.py` (адрес от GT — `poolId` singleton-контракта, не сам пул); (в) **2 aerodrome-slipstream-тегированных пула** (см. дубликацию выше) — gauge-функции реально ревертят (это и есть основание считать тег ошибочным), поправка не применена, число = необскорректированный Uniswap-v3-эквивалент (не верхняя граница в узком смысле — фактически корректное число ДЛЯ ЭТОГО пула, раз он реально не gauge-форк).
    - **[УСТАРЕЛО — см. баг №22] Решение по линии "хеджированное LP" из первого прохода:** список НЕ пуст (4 кандидата) — это решение ПЕРЕСМОТРЕНО ниже после фикса реальной ошибки в расчёте sigma, которая систематически ЗАВЫШАЛА ratio именно для WETH/USDC-подобных пар (см. №22).

22. **Реальная ошибка в расчёте 30-дневной `sigma` для #21 — `currency=usd` в OHLCV-запросе GT возвращал цену СТЕЙБЛ-стороны пула, а не волатильной, для пар со стейблом.** Найдено калибровкой (владелец, 2026-09-05, "прогнать ETH/USDG через pool_screener_gt_recompute.py как обычного кандидата, истинное значение 1.3–1.47").
    - **Прямое доказательство (не по итоговому расхождению — по сырым данным):** для пула ETH/USDG (`0x52e65b17...`) `currency=usd` close за 10 реальных часовых точек = `$0.9995`–`$1.0010` (размах 0.15%) — это цена USDG (GT считает её `base_token` для этого пула), НЕ цена ETH. `currency=token&token=quote` за то же окно = `$2449`–`$2458` (реальное движение ETH). Итог: `sigma_30d` через `usd` = **13.49%**, через `token&quote` = **52.58%** (близко к независимо ранее подтверждённым **49.53%**, `pool_screener_calibrate_p5.py` — небольшая разница объясняется разными 30-дневными окнами) — занижение в **3.67×**, что систематически ЗАВЫШАЕТ `ratio_to_lvr` (sigma в знаменателе).
    - **Фикс** (`analysis/pool_screener_gt_recompute.py::determine_volatile_gt_token_side()` + `fetch_hourly_closes_30d()`): для КАЖДОГО пула эмпирически определяется, какая из сторон (GT `base`/`quote`) НЕ стейбл — по реальной GT-цене, ближе к $1.00 или дальше (без хардкод-списка адресов стейблов), и close для sigma запрашивается ИМЕННО для неё через `currency=token&token=<сторона>`, отдельным запросом от объёма. **Объём проверен отдельно и НЕ пострадал** — реальный дневной оборот пула ETH/USDG (~21.8× TVL) внутренне согласован с независимо ранее замеренным реальным числом транзакций этого пула (тысячи buy/sell за 5 минут, `p5_gt_pool_history_result.json`) — это реальная гипер-активность (бот/MEV-трафик на молодом чейне), не единичная ошибка, оставлено через `currency=usd` как было.
    - **Калибровка на P5 после фикса — ПРИНЯТА владельцем.** `ratio_to_lvr` (наша формула, с `k_pool` в знаменателе) = **0.180**; истинное значение живой позиции (`real_fee_lvr_ratio_at_hist_sigma`) = **1.3–1.47**. **Уточнение формулировки (владелец, 2026-09-05): 1.3–1.47 — это отношение УЖЕ ПРИ НАШЕЙ РЕАЛЬНОЙ КОНЦЕНТРАЦИИ (узкий диапазон живой позиции, не full-range k=1) — оно СРАВНИМО СО СКРИНЕРНЫМ ratio НАПРЯМУЮ, на `k_pool` дополнительно делить НЕ нужно** (k_pool уже участвовал бы в формировании этого истинного числа через реальную концентрацию позиции — они одного порядка, не находятся в отношении "полное/k_pool"). Остаточный разрыв **8×** (1.4666/0.180) раскладывается: **~5×** — разница окон (17 часов реальных накопленных комиссий сразу после открытия, очень шумная малая выборка, против нашего сглаженного 30-дневного GT-объёма) и **~1.5×** — разница формул (реально собранные комиссии на факте vs `объём×fee_tier/TVL` наивным способом, разные источники дохода по построению). Признано объяснённым и приемлемым, не новым багом — **k_pool в знаменателе формулы (`ratio = fee_apr_gt_30d/(k_pool×σ²/8)`) остаётся БЕЗ ИЗМЕНЕНИЙ**, ответ на вопрос "входит ли k_pool в знаменатель" (владелец, 2026-09-05) — **ДА**, было так с самого начала (`analysis/pool_screener_gt_recompute.py`: `lvr_30d = k * sigma_30d**2/8`, `ratio = fee_apr_corrected/lvr_30d`, `k` = `k_from_concentration_screener`, отдельная колонка во всех записях, формула `pool_screener_concentration.py::compute_k`).
    - **Урок:** GT `currency=usd` не гарантированно возвращает цену "интересующей" (волатильной) стороны пула — зависит от того, какую сторону GT считает `base_token` для конкретного пула, это не универсальное соглашение (для пула P6, cbBTC/USDC, `usd`-режим случайно указывал на cbBTC — сработал; для P5, ETH/USDG — на USDG, стейбл — сломал sigma). Для sigma по любой паре со стейблом — ВСЕГДА определять волатильную сторону реальной ценой, не полагаться на `currency=usd` вслепую.
    - **ИТОГОВЫЙ честный пересчёт по всем 21 (после фикса, run 33963054674, `data/p3_guard_cache/pool_screener_gt_recompute_result.json`), отсортировано по ratio убыв.:**

      | Сеть | Проект | Пара | fee_apr_30d | fee_apr_7d | σ (30д, реальная) | k_pool | LVR=k×σ²/8 | ratio |
      |---|---|---|---|---|---|---|---|---|
      | Base | uniswap-v3 | WETH-CBBTC | 48.19% | 44.86% | 36.0% | 31.16 | 0.5049 | **0.954** |
      | Arbitrum | uniswap-v3 | WETH-USDT | 15.06% | 13.60% | 53.1% | 4.90 | 0.1731 | 0.870 |
      | Base | aerodrome-slipstream | WETH-CBBTC | 44.07% | 41.02% | 36.1% | 31.16 | 0.5082 | 0.867 |
      | Base | aerodrome-v1 | USDC-AERO | 7.03% | 6.81% | 83.7% | 1.00 | 0.0876 | 0.802 |
      | BSC | uniswap-v3 | ETH-WBNB | 10.52% | 9.94% | 37.8% | 7.63 | 0.1363 | 0.772 |
      | Base | aerodrome-slipstream | WETH-USDC | 30.29% | 31.69% | 53.3% | 11.31 | 0.4015 | 0.754 |
      | Arbitrum | uniswap-v3 | WBTC-USDC | 18.20% | 18.94% | 39.6% | 12.54 | 0.2453 | 0.742 |
      | Arbitrum | uniswap-v3 | WETH-USDC (0.05%) | 23.68% | 23.80% | 53.7% | 8.94 | 0.3225 | 0.734 |
      | Base | uniswap-v3 | WETH-USDC (дубль см. №21) | 29.00% | 30.34% | 53.3% | 11.31 | 0.4015 | 0.722 |
      | BSC | uniswap-v3 | BTCB-WBNB | 11.16% | 9.18% | 30.4% | 13.93 | 0.1604 | 0.696 |
      | Base | aerodrome-v1 | WETH-VVV | 7.70% | 6.54% | 97.3% | 1.00 | 0.1183 | 0.650 |
      | Arbitrum | uniswap-v3 | WETH-USDC (0.30%) | 21.98% | 23.53% | 60.2% | 8.40 | 0.3805 | 0.578 |
      | Base | uniswap-v3 | USDC-CBBTC | 20.97% | 23.38% | 43.7% | 15.31 | 0.3661 | 0.573 |
      | Base | aerodrome-v1 | VVV-DIEM | 1.44% | 1.45% | 49.7% | 1.00 | 0.0309 | 0.466 |
      | Base | uniswap-v3 | WETH-USDC (117M TVL) | 58.63% | 58.93% | 66.6% | 25.83 | 1.4333 | 0.409 |
      | Arbitrum | uniswap-v3 | WBTC-WETH | 13.00% | 10.43% | 36.6% | 21.52 | 0.3607 | 0.360 |
      | Base | uniswap-v3 | WETH-CBBTC (11.8M TVL) | 18.93% | 13.06% | 54.7% | 25.18 | 0.9425 | 0.201 |
      | Base | aerodrome-slipstream | USDC-CBBTC (P6-пул) | 1.54% | 1.77% | 40.3% | 9.90 | 0.2006 | 0.077 |
      | Base | aerodrome-slipstream | USDC-CBBTC (5.5M TVL) | 1.88% | 1.18% | 40.8% | 38.44 | 0.8014 | 0.023 |
      | Base | aerodrome-slipstream | WETH-USDC (7.3M TVL) | 2.37% | 2.28% | 53.2% | 53.09 | 1.8758 | 0.013 |
      | Base | aerodrome-slipstream | WETH-CBBTC (18M TVL) | 1.60% | 1.22% | 44.2% | 141.29 | 3.4480 | 0.005 |

      **0 из 21 проходят порог ≥2.** WETH/USDC-пулы (Base и Arbitrum, все fee-tier) — которые ложно проходили в первом (баговом) прогоне #21 с ratio 6-11 — теперь честно 0.57–0.75, что и ожидалось: реальная 30-дневная σ ETH выросла с ошибочных ~14% до честных ~53-60%, LVR (∝σ²) вырос примерно в 14-17 раз, ratio упал пропорционально. Лучший результат из всех 21 — Base uniswap-v3 WETH-CBBTC, 0.954, вдвое ниже порога.

    - **Отдельный факт (владелец, 2026-09-05): кластер 0.7–0.95 на 21 пуле.** 10 из 21 реально посчитанных пулов (Base uniswap-v3 WETH-CBBTC 0.954, Arbitrum WETH-USDT 0.870, Base aerodrome-slipstream WETH-CBBTC 0.867, Base aerodrome-v1 USDC-AERO 0.802, BSC ETH-WBNB 0.772, Base aerodrome-slipstream WETH-USDC 0.754, Arbitrum WBTC-USDC 0.742, Arbitrum WETH-USDC 0.734, Base uniswap-v3 WETH-USDC 0.722, BSC BTCB-WBNB 0.696) плотно легли в диапазон **0.7–0.95**, независимо от сети (Base/Arbitrum/BSC), протокола (uniswap-v3/aerodrome-slipstream/aerodrome-v1) и fee tier (0.01%–0.3%). **Интерпретация: комиссии ≈ LVR на крупных ликвидных парах** — не случайное совпадение конкретных чисел, а структурная близость к равновесию (LP-доход почти покрывает, но не превышает теоретическую стоимость адверс-селекции арбитражёров), устойчиво воспроизводящаяся на разнородном наборе площадок/сетей. Это самостоятельный вывод о рынке, не зависящий от решения закрыть текущую линию.
    - **Решение по линии "хеджированное LP" (владелец, 2026-09-05, дословно): линия ЗАКРЫТА.** "Два живых замера (P5 1.3–1.47, P6 ~0.08), два прохода скринера, калибровка, и вывод: на 30-дневном окне отношение ≥2 без субсидированного объёма не найдено; это согласуется с литературой по v3 на крупных парах. Линия переоткрывается только новым источником объёма, не новым пулом." Список кандидатов из бага №21 (первый, БАГОВЫЙ прогон) — недействителен, заменён таблицей выше; см. `## Закрытые линии` ниже.

23. **ПРАВИЛО РАБОТЫ (владелец, 2026-09-05, обязательно ПЕРЕД началом ЛЮБОЙ новой задачи/спринта, не только Task 1/2/фандинг): четыре вопроса.**
    1. Данные уже существуют? (реестры/кэши/предыдущие прогоны — искать в `data/`/`docs/` ПЕРЕД тем, как писать новый сбор с нуля — Task 1/2/фандинг в этой сессии переиспользовали реальные реестры и функции Sprint R1/G1/P4/P5, а не начинали заново.)
    2. Ограничение (rate limit, известный баг, ранее найденное искажение) ещё актуально? (не предполагать, что зафиксированное неделю назад ограничение всё ещё держится — реальный дрейф схемы Dune `dex.trades` `token_bought_address` varchar→varbinary между 01-04.09 и 05.09.2026, баг только что найденный в Task 1, прямой пример того, почему это нужно перепроверять, а не полагаться на прошлый результат.)
    3. Число агрегатора сверено с сырым источником? (тот же урок, что баги №17/№22 — DefiLlama `apyBase` и GT `currency=usd` оба оказались систематически неверны для конкретных случаев, обнаружено только прямой сверкой с сырыми данными, не по номиналу агрегатора.)
    4. Метод откалиброван на известном ответе? (тот же принцип, что калибровка `pool_screener_gt_recompute.py` на живой позиции P5 (баг №22) — прогнать метод на случае, где истинный ответ уже известен НЕЗАВИСИМО, ДО того как доверять его выводу на неизвестных случаях.)

24. **Chainlink-оракул сток-токенов НЕ годится как источник Y для Задачи 1 — два реальных факта, найденных калибровкой (владелец, 2026-09-05, "Chainlink как Y — да, после двух проверок").**
    - **Факт 1: фид НЕ обновляется по выходным.** Реальные `AnswerUpdated` для 5 фидов (TSLA/AMZN/GOOGL/SPY/QQQ) в окне пт 20:00 → вс 20:00 ET за 3 последних завершённых выходных — **0 обновлений во всех 15 окнах** (`task1_chainlink_weekend_check_result.json`). Ожидаемо (рынок закрыт), но это прямой факт об инструменте, не техническая деталь: между реальным закрытием пятницы и реальным первым обновлением понедельника фид просто молчит — как источник референса ВНУТРИ тёмного окна он неприменим в принципе, не только для Y на границах.
    - **Факт 2: первое обновление после открытия систематически отстаёт от реальной цены открытия.** Прямая сверка "ближайшее обновление после 9:30 ET" против `yfinance` (реальный, рабочий с GH Actions раннера — НЕ заблокирован, в отличие от Stooq) на тех же 5 тикерах × 3 выходных: close-сторона в порядке (медиана расхождения 0.12%, 3/15 > 0.2%), но **open-сторона проваливает калибровку систематически — 13 из 13 (100%) наблюдений расходятся больше чем на 0.2%**, медиана расхождения **0.40%**, максимум 0.83% (`task1_y_source_crosscheck_result.json`). Причина структурная: оракул обновляется по факту реального движения/интервала, не мгновенно в момент открытия — первая реальная точка после звонка колокола уже успевает уйти от истинной цены открытия.
    - **Решение (владелец, 2026-09-05): Chainlink НЕ используется, Y — напрямую `yfinance`** (закрытие пятницы/открытие понедельника, регулярная сессия — НЕ pre/post-market). Урок для правила №23 (п.4, "метод откалиброван на известном ответе") — калибровка НЕ формальность: именно она поймала реальную непригодность инструмента ДО того, как на нём построили финальный вывод, а не после.
    - **Ручная калибровка пайплайна Задачи 1 на yfinance (владелец: "калибровка на одних выходных вручную перед выводом") — ПРОЙДЕНА, 2026-09-05.** X/Z пересчитаны вручную из того же сырого Dune-кэша (`task1_weekend_windows_*.csv`), Y — вручную через `yfinance_daily`+`friday_monday_gap`, на 5 контрольных тикерах (AAPL/TSLA/SPY/GME/NVDA), выходные 2026-08-28→08-31: **все 15 значений (X,Y,Z×5) совпали с тем, что выдал сам пайплайн** с точностью до 1e-6 (`analysis/task1_manual_calibration.py`, run 33967675900). Пайплайну можно доверять — можно запускать полный прогон по всем 9 выходным.

25. **Задача B (фандинг сток-перпов Lighter) — калибровка на известном ответе (правило №23 п.4) вскрыла, что сырой `rate` НЕ измеряет рыночный перекос (владелец, 2026-09-05).** Первый честный прогон (`analysis/lighter_stock_perp_funding.py`, 37 сток-перпов, история с 05.07): **у ВСЕХ 37 тикеров медиана годовых почти идентична и равна ровно 3.504%/год, стабильна по каждой отдельной неделе БЕЗ ИСКЛЮЧЕНИЙ** (пример: AAPL/MSFT/TSLA/NVDA — медиана каждой недели ровно 3.504%, `frac_positive=1.0`). Это число уже РЕАЛЬНО ЗАДОКУМЕНТИРОВАНО в `docs/P4_RECON.md` (Sprint P4, независимо от текущей задачи) как `interestRateComponent` — фиксированный базовый компонент официальной формулы Lighter `fundingRate = premium/8 + interestRateComponent`, воспроизведённый из `base_interest_rate=0.0032` метаданных рынка (`0.0032/8=0.0004%/час × 8760 = 3.504%/год`) — **применяется механически ко ВСЕМ сток-перпам одинаково, когда премия (реальный рыночный перекос спроса) близка к нулю**, что и есть обычное состояние для перпа, стабильно отслеживающего индекс. **Вывод: медианный годовой `rate` на Lighter для сток-перпов почти всегда измеряет не гипотезу "ретейл структурно лонгует" (реальный сигнал перекоса — это компонент `premium`, отдельно не выделенный в этом прогоне), а фиксированный платформенный тариф.** Это НЕ меняет практический вердикт по предрегистрированному порогу (медиана 3.504%/год << порога 25%/год — "НЕ ПРОХОДИТ" устойчиво даже если премия местами добавляет несколько процентов сверху, `mean` уже выше `median` на части тикеров, напр. NVDA 5.15% против медианы 3.504%, но всё равно далеко от порога) — но означает, что метод в текущем виде НЕ является чистой проверкой гипотезы retail-skew, только её верхней грубой оценкой через суммарный rate. Доложено честно, не решено втихую — метод нуждается в выделении `premium`-компонента отдельно (`fundingRate − interestRateComponent`) для содержательной проверки, если владелец захочет продолжать эту линию.

**Дельта хеджа (ETH-нога LP-позиции при текущей цене P, в диапазоне [P_lower, P_upper]):**

```
amount_ETH(P) = L × (1/√P − 1/√P_upper)
```

Дословно `Uniswap/v3-periphery/contracts/libraries/LiquidityAmounts.sol`, реализовано в `p5_live_precheck.py::v3_amounts()`. Это и есть требуемый размер шорта на Lighter.

**Расстояние до ликвидации (оценка, cross-margin, короткая позиция, единственная открытая позиция в пуле обеспечения):**

```
P_liq = (collateral + size×P0) / (size×(1+mmf))
% роста до ликвидации = P_liq/P0 − 1
```

`collateral` — весь доступный collateral аккаунта (cross-режим пулит обеспечение по всем позициям — корректно ТОЛЬКО когда позиция единственная, гарантируется flat-precondition перед хеджем), `size` — размер шорта (ETH), `P0` — цена входа, `mmf` — `maintenance_margin_fraction` рынка (реальное сырое поле `orderBookDetails`, единицы — `raw/10000`, подтверждено формулой самого SDK `imf=int(10000/leverage)` из `update_leverage()`). Реализация — `p5_live_step1.py::liquidation_distance_pct()`.

**Проверено против факта дважды, на двух РАЗНЫХ реальных открытиях** — обе проверки дали разницу 0.0 между формулой и реальным полем `liquidation_price` позиции:
- Первое живое открытие (tokenId 999556, плечо 3.0003×): вход $2499.94, реальный `liquidation_price=$10046.265582243843` ⇒ implied move 301.8602679361842%; формула независимо дала 301.8602679361843% (разница на уровне последнего знака float, не содержательная).
- Текущее открытие (tokenId 1000756, плечо 3.0003×): вход $2506.43, реальный `liquidation_price=$4044.0837011564145` ⇒ implied move 61.348360064171544%; формула дала ровно то же число.

Точные цифры — `data/p3_guard_cache/p5_live_step1_result.json` (текущее открытие) и история коммитов (предыдущий `p5_live_step1_result.json` перед закрытием 999556).

**Единицы плеча/margin fraction (реальная формула SDK, не догадка):** `imf_raw = int(10_000 / leverage)` ⇒ `leverage = 10_000 / imf_raw`. Строковое поле `positions[].initial_margin_fraction` в ответе `/api/v1/account` — уже ПРОЦЕНТ (`raw/100`), напр. `"33.33"` ⇒ `leverage = 100/33.33 ≈ 3.0003`.

**Эластичность требуемого хеджа по цене (владелец, задача расширения отчёта, 2026-09-04) — производная формулы `amount_ETH(P)` выше:**

```
x(P) = amount_ETH(P) = L × (1/√P − 1/√P_upper)
dx/dP = −L / (2×P^1.5)

(dx/x) / (dP/P) = −(1/(2√P)) / (1/√P − 1/√P_upper)
```

При текущей цене на момент снятия ($2501.15, диапазон [$2258.86, $2760.88], `data/p3_guard_cache/p5_live_snapshot_1000756_result.json`, 2026-09-03T22:54:10Z): **эластичность = −10.37** (независимо посчитана владельцем на близком по времени снимке цены $2495.62: −10.14 — то же порядка величины, различие — реальный дрейф цены между двумя снятиями, не расхождение формул). Смысл: **1% движения цены ETH ⇒ ~10% изменения требуемого размера хеджа** вблизи текущей точки диапазона — и это НЕ константа, эластичность зависит от того, где цена находится внутри диапазона (растёт по модулю ближе к границам, минимальна в середине).

**Отсюда прямое следствие для порога ребаланса (владелец, дословно): "скорость дрейфа «X%/час» — не константа, это одна реализация цены; правильная запись порога — в процентах хода цены, а не хеджа."** Наблюдавшаяся на первой живой точке скорость дрейфа хеджа ≈9.7%/час размера хеджа (см. `data/p5_live_snapshot_1000756_result.json` первого прогона) — артефакт конкретного пройденного пути цены и текущей эластичности (~−10 в этой точке), не универсальная константа самого пула/позиции. **Порог ребаланса, когда будет выбираться, должен фиксироваться в % хода ЦЕНЫ (dP/P), а не в % дрейфа ХЕДЖА** — дрейф хеджа тогда получается автоматически через актуальную эластичность в момент проверки, а не наоборот. Демон (`p5_live_bot.py`) ещё не написан — это регистрация формулы и вывода ДО реализации, не изменение уже принятого решения по диапазону/порогам (§8).

## 6. Реестр рабочих скриптов и воркфлоу

Все воркфлоу — `push`-триггер на маркер-файл в `data/p3_guard_cache/<ИМЯ>_REQUEST` (кроме отмеченных `workflow_dispatch`-only), т.к. ветка `claude/sc1-pons-v2-launcher-efdbyt` не дефолтная. Confirm-гейтед пары (`_DRYRUN_REQUEST`/`_CONFIRM_REQUEST`) — по умолчанию dry-run, реальная отправка только по отдельному CONFIRM-маркеру (см. баг №3 выше про фикс гейта).

**Живой цикл (главные):**
- `analysis/p5_live_step1.py` / `run_p5_live_step1_vps.yml` — открытие: mint LP + хедж. Запускается на VPS-NL по SSH (не GH Actions runner — US IP под geo-блоком). Confirm-гейтед. Все обязательные проверки внутри (плечо-верификация, flat-precondition, `_verify_hedge_filled`, автозакрытие при сбое, буфер маржи 25%, WETH-aware sizing).
- `analysis/p5_live_close.py` / `run_p5_live_close.yml` — закрытие LP (`decreaseLiquidity`+`collect`) с корректной raw sqrt-price математикой. Реюзается изнутри `p5_live_step1.py` для автозакрытия.
- `analysis/p5_live_flatten_lighter.py` / `run_p5_live_flatten_lighter.yml` — экстренное выравнивание остаточной позиции на Lighter (reduce_only рыночный ордер на ФАКТИЧЕСКИЙ текущий размер). Функция `flatten()` реюзается как flat-precondition внутри Step 1.
- `analysis/p5_live_unwrap_weth.py` / `run_p5_live_unwrap_weth.yml` — разворот WETH обратно в native ETH (`WETH9.withdraw()`). Confirm-гейтед.
- `analysis/p5_live_precheck.py` / `run_p5_live_precheck.yml` — предварительный расчёт (только чтение): цена пула, размер LP/хеджа, реальное плечо, `nfpm_position()` (проверка закрытия старой LP), `lighter_account_full()`/`real_eth_leverage()` — общая библиотека, импортируется в `p5_live_step1.py`.

**Диагностика (VPS/US-разница, юрисдикция, подпись):**
- `analysis/p5_live_step0.py` / `run_p5_live_step0.yml` — Шаг 0: публичные балансы/маржа, без подписи.
- `analysis/p5_live_lighter_signcheck.py` / `run_p5_live_lighter_signcheck.yml` — проверка схемы подписи SignerClient без отправки ордера.
- `analysis/lighter_jurisdiction_probe.py` / `run_lighter_jurisdiction_probe.yml` — нашёл реальный список запрещённых юрисдикций.
- `analysis/lighter_depth_fine_probe.py`, `lighter_volume_quota_probe.py`, `lighter_rfq_and_docs_probe.py`, `lighter_order_status_probe.py`, `lighter_order_status_authed_probe.py`, `lighter_order_history_probe.py` — цепочка опровержения гипотез по причине неполного филла (см. баг №1) + поиск реальной причины отмены через аутентифицированный `accountOrders`.
- `analysis/across_handler_probe.py`, `across_relay_topups_probe.py` — трассировка реального пути WETH-пополнений через Across (баг №5).
- `run_vps_bootstrap.yml` (Даллас) / `run_vps_bootstrap_nl.yml` (Нидерланды) — полный bootstrap хоста (ufw/fail2ban/пользователь/venv/секреты/systemd-юнит/logrotate/тест гео-блока с нового IP).
- `run_fetch_vps_position_state.yml` — забрать `data/p5_live_position_state.json` с VPS без повторного запуска Step 1 (использовался один раз для устранения бага, см. §4).

**Разведка/бэктест (не live-путь):**
- `analysis/p5_backtest_10d.py` / `run_p5_backtest_10d.yml` — 10-дневный бэктест варианта 1, kill-порог 30%/год после издержек (`KILL_THRESHOLD_ANNUAL=0.30` — источник цифры в §1 таблицы `kill_threshold_annual`).
- `analysis/mm_p5_setup.py`, `mm_p5_variant3_probe.py` — разведка источников данных для пула ETH/USDG (GeckoTerminal/DexScreener/DefiLlama не годятся, ончейн-скан — единственный путь, калиброван объём свопов).
- `analysis/gt_pool_verify_probe.py` / `run_gt_pool_verify_probe.yml` — перечитывает `GET /networks/robinhood/pools/{addr}` для пула P5 с реальным таймстемпом (найден источник расхождения в §1 "ПОД ПРОВЕРКОЙ").

## 6.1. GeckoTerminal API — реальные ограничения и особенности (для будущей работы над σ пула, §8)

Найдено эмпирически при работе над P5 (реальные вызовы, не документация "как должно быть" — GT публичной OpenAPI-спецификации не даёт):

- **Слаг сети — `robinhood`, НЕ `robinhood-chain`.** Неверный слаг возвращает 404, не подсказывает правильный.
- **Rate limit: 30 запросов/мин без ключа**, ключ не требуется для базовых эндпоинтов (`/pools/{addr}`, OHLCV) — но именно по этой причине бюджетировать вызовы заранее, не бить лимит вслепую сериями.
- **OHLCV-агрегаты** (`GET /networks/{network}/pools/{addr}/ohlcv/{timeframe}`): доступные `timeframe` — `minute` (агрегация `1`/`5`/`15`), `hour` (`1`/`4`/`12`), `day` (`1`). `limit` — максимум **1000** точек за запрос.
- **`include_empty_intervals=true` ОБЯЗАТЕЛЕН** — без этого параметра эндпоинт молча пропускает интервалы без сделок, и таймстемпы соседних точек "едут" (перестают быть равномерным шагом) — для расчёта волатильности по равномерной сетке это критично, не косметика.
- **Пагинация — через `before_timestamp`** (unix-время в СЕКУНДАХ, не миллисекундах) — не через номер страницы/курсор.
- **Ориентация пула `0x52e65B17...` (подтверждено реальным ответом `/pools/{addr}`, `relationships.base_token`/`quote_token`): `base` = USDG, `quote` = WETH.** Для получения цены ETH в USDG использовать `currency=token&token=quote` (цена quote-токена в единицах base, т.е. напрямую ETH/USDG) — **не** конвертировать через `currency=usd`: USD-цена самой USDG на этой сети шумная (владелец, 04.09.2026: наблюдённый суточный диапазон **$0.9374–$1.0095** — конкретная пара цифр получена владельцем, не перепроверена этой сессией отдельным API-вызовом, но направление проблемы понятно и без перепроверки: USDG не идеальный $1-стейбл на этом чейне) — считать по ней σ ETH/USDG значит примешивать шум курса стейблкоина к волатильности самой торгуемой пары, искажая результат.

## 7. P3 (кросс-версионный дислокационный арбитраж v3↔v4) — ЖИВА, НЕ ЗАКРЫТА, в очереди после P5

Гард (`analysis/p3_concentration_and_fragmentation.py`, реальный прогон, `data/p3_guard_cache/p3_concentration_result.json`) НЕ закрыл линию: доля топ-3 адресов в закрывающих транзакциях кросс-версионных дислокаций за 7 дней — **NVDA 8.19%, COST 5.28%, QQQ 6.65%**, все далеко ниже порога закрытия 80% (`close_share_kill_threshold=0.8`, `any_token_over_threshold=false`). Реальная v3-И-v4 ликвидность одновременно подтверждена минимум на 9 токенах (NVDA/QQQ/RDDT/COST/GME/RBLX/LLY/SPY/MSTR), реальный V3 Factory адрес найден (`0x1f7d7550b1b028f7571e69a784071f0205fd2efa`, НЕ стандартный Uniswap Labs CREATE2). Полная история находок (включая опровергнутую первую версию "0 из 24 имеют v3-пул" — баг короткого окна поиска фабрики, не факт рынка) — секция ниже в этом файле, не удалена, только не дублируется здесь. Открытые вопросы: реальная разница цены v3↔v4-нога на снимке, фактическая ёмкость v4-стороны (россыпь мелких `pool_id`, не один большой пул). **Приоритет: после P5**, по решению владельца от 2026-09-03.

## 8. Что НЕ сделано (явно, чтобы новый чат не предположил обратное)

- **`analysis/p5_live_bot.py` — НЕ написан.** Цикл ребаланса, часовое логирование, safety-стопы (Step 2-4 живого задания) не существуют как код. systemd-юнит ссылается на этот файл и будет падать при попытке `systemctl start`.
- **Демон НЕ включён.** `systemctl enable`/`start` не выполнялись — явное решение: демон включается ТОЛЬКО отдельной командой владельца, не автоматически после открытия позиции.
- **Пороги ребаланса НЕ рассчитаны.** Нужна историческая волатильность ETH/USDG на самом пуле (не общерыночная ETH-волатильность) — ончейн-скан свопов за представительное окно, ещё не выполнен (см. §6, `mm_p5_variant3_probe.py` — разведка пути, не сам расчёт).
- **Оптимальный диапазон LP НЕ считался.** ±10% взят как стартовый рабочий диапазон (решение по умолчанию, не результат оптимизации) — обе реально открытые позиции (999556, закрыта; 1000756, живая) использовали именно его.
- **Канал алерта (webhook/email) НЕ выбран** — п.7 `docs/P5_LIVE_DEPLOY.md`, нужен явный ввод владельца прежде чем настраивать `p5-live-bot-alert.service`.
- **Аутентифицированный `accountInactiveOrders`/полная история ордеров с auth** — не запрошены; RFQ-гипотеза (почему ручной фронтенд-ордер владельца заведомо исполнялся полностью) осталась НЕ подтверждена и НЕ опровергнута напрямую — маржа/плечо оказались достаточным объяснением всех наблюдённых партиальных филлов, RFQ-путь больше не расследовался за отсутствием необходимости.
- **`data/p5_fee_accrual.jsonl` — цель 24 точки ДОСТИГНУТА, 2026-09-04T20:06:02Z: 25 точек в git.** Сбор автономный, cron на NL VPS (`bot`, раз в час, минута :06 — см. `run_vps_setup_hourly_cron_nl.yml`), пишет в файл ВНЕ git-репозитория (`/home/bot/p5_live_data/p5_fee_accrual.jsonl`), git на VPS не используется. Забор — тем же маркером `P5_LIVE_SNAPSHOT_REQUEST` через GH Actions (`run_p5_live_position_snapshot.yml`, теперь также забирает `funding_spread.jsonl` тем же прогоном, см. ниже). Старый почасовой Routine (`trig_01CY8c91HNxvp4agsKSxVWkE`) удалён 2026-09-04 после подтверждения cron двумя автономными циклами подряд, дублей при переходе не найдено. Через симулированный `eth_call` на `collect()`, см. баг №7 в §4 — НЕ через прямое чтение `positions().tokensOwed`, которое лениво и покажет 0. **По вердикту владельца выше (§1): цель набрана, дальше — закрытие протокола P5 в текущем виде, демон под этот конкретный пул не пишется.**
- **σ пула (реализованная волатильность ETH/USDG) — НЕ посчитана.** Нужна по OHLCV самого пула (§6.1: `currency=token&token=quote`, НЕ `usd` — избежать шума курса USDG), с кросс-проверкой на ДВУХ таймфреймах (напр. `hour/1` и `hour/4`, или `minute/15` и `hour/1`) — расхождение между таймфреймами сверх разумного было бы сигналом проблемы с данными/агрегацией, не просто эстетика. Без реальной σ пороги ребаланса (предыдущий пункт) считать не на чем.

## Задача 1 (сток-токены по выходным) — ЖИВАЯ находка, 2026-09-05, ждёт решения владельца

**Полный честный прогон по всем 9 выходным с 01.07.2026** (`analysis/task1_weekend_gap.py`, `data/p3_guard_cache/task1_weekend_gap_result.json`, run 33967970061), Y — `yfinance` (после отказа от Stooq/Chainlink, см. баг №24), пайплайн прошёл ручную калибровку на 5 тикерах (см. выше). Фильтр тонких пулов (мин. 3 сделки в каждом из 4 брекетов) оставил **N=197 пар токен×выходные, 42 уникальных тикера, 8 из 9 выходных дали хотя бы одну прошедшую пару**.

**Предрегистрированный вердикт (правило владельца, зафиксировано ДО прогона): ЖИВА.**
- `|corr(X,Y)| = 0.165 < 0.3` (порог для "жива") — движение токена в тёмном окне слабо коррелирует с реальным гэпом акции.
- `N=197 ≥ 100`.
- `Z` систематически противоположен `X`: доля `sign(X) != sign(Z)` = **62.4%** (порог для "систематически" — простое большинство, `>0.5`, как в формуле вердикта).

**Робастность (проверено сразу же, не по требованию — 5 строк с `|X|>0.5`, в т.ч. MU с X=11.8 за один 2-часовой брекет, явный артефакт тонкой ликвидности, несмотря на прохождение фильтра "мин. 3 сделки"): после исключения этих 5 строк (N=192) — `corr(X,Y)=0.131` (ещё слабее), `corr(X,Z)=-0.378`, доля противоположных знаков 62.5%.** Вердикт устойчив, не держится на выбросах.

**Экономическая интерпретация (не проверена независимо, гипотеза для владельца): движение токена в тёмном окне выходных слабо предсказывает реальный гэп открытия ⇒ токен, видимо, недо-/по-разному реагирует на то же событие, что и акция; и после реального открытия рынка (`Z`-окно) движение токена в 62% случаев разворачивается относительно того, что было в тёмном окне — то есть есть намёк на частичный возврат/коррекцию к цене открытия акции, а не продолжение тренда тёмного окна.** Это НЕ то же самое, что "спред узкий и стабилен" (два предыдущих закрытия линий) — здесь именно слабая, но статистически значимая (`p=0.020` в регрессии Y~X) связь плюс систематический разворот, потенциальный реальный источник альфы, а не подтверждение рыночной эффективности.

**Капитал НЕ развёрнут.** Владелец потребовал 4 проверки до любого торгового правила (2026-09-05), все из уже имеющихся данных, капитал не разворачивать.

**Проверка 2 (тайминг) — ВЫПОЛНЕНА, реальный и НЕОЧЕВИДНЫЙ результат (`analysis/task1_z_decompose.py`, `data/p3_guard_cache/task1_z_decompose_result.json`, N=170 после джойна с X).** Z разбит на Z1 (вс 20:00→21:00 ET) и Z2 (21:00 ET→пн 9:30 ET). **Headline на полной выборке: corr(X,Z1)=+0.617, corr(X,Z2)=−0.695 — читалось бы как "разворот живёт поздно, Z1 продолжает тренд".** НО это оказалось НЕ робастно: **корреляция corr(X,Z1) целиком развёрнута ОДНОЙ строкой** (AMC, X=6.40 — тот же класс артефакта тонкой ликвидности, что MU/GLD в основном прогоне). После исключения 4 строк с |X|>0.5 (N=166): **corr(X,Z1)=−0.682, corr(X,Z2)=−0.419 — разворот живёт РАНЬШЕ (Z1), а не позже, и сильнее в Z1, чем в Z2.** Доля противоположных знаков одинакова для Z1 и Z2 (58.4%). **Вывод: headline-число на полной выборке нельзя использовать без проверки на выбросах — тот же урок, что уже был на основном X/Z (rule №23 п.4), здесь поймано сразу же, не по отдельному запросу.** Скрипт обновлён — теперь всегда считает и печатает робастную версию.

**Проверка 3 (экономика) — ВЫПОЛНЕНА, без новых запросов (пересчёт на уже имеющемся `task1_weekend_gap_result.json`).** |X|: медиана≈0.91–0.96%, p75≈2.1–2.4%, p90≈5.0–6.1% (в зависимости от исключения выбросов). Условный медианный |Z| растёт монотонно с порогом |X| (после исключения 5 выбросов |X|>0.5, N=192): при |X|>медианы (N=96) медиана|Z|=1.00% (**$4.98 на позицию $500**); при |X|>p75 (N=48) медиана|Z|=1.62% (**$8.11**); при |X|>p90 (N=20) медиана|Z|=4.04% (**$20.20**). Это ГРУБО (без вычета round-trip издержек — ждёт проверки 1) и БЕЗ учёта того, что распределение остаётся правосторонне скошенным (mean заметно выше median даже после исключения explicit outliers) — типичное для тонких DEX-пулов, доложено как есть.

**Проверка 1 (ликвидность, TVL>$200k, round-trip на $500) — разведка адресов пулов ЗАВЕРШЕНА, реконструкция ещё не построена.** Реальная находка: полный `dex.trades` (не тот усечённый список из 7 колонок, что использовался раньше) реально содержит `project`, `version`, `project_contract_address` (варбинари) — прямой адрес AMC-контракта на сделку (`task1_liquidity_probe2.py`, `task1_pool_addresses_by_token`, 300 строк по топ-объёму). **Uniswap v3 (version='3') — адрес РЕАЛЬНО per-pair** (каждая пара токен/USDG или токен/WETH имеет свой уникальный `project_contract_address`, пример: NVDA/USDG → `0xd4eb21209c4d6093f80b5b84f5c45cc093ea14a3`) — для этих пулов реальный путь есть: живое чтение `fee()` с контракта + резервы (тот же метод, что `pool_screener_concentration.py`/`p6_live_close.py`). **Uniswap v4 (version='4') — адрес НЕ per-pair, это singleton PoolManager** (один и тот же адрес `0x8366a39cc670b4001a1121b8f6a443a643e40951` встречается у SPY/USDG, USDG/AMC, USDG/DJT, USDG/NVDA — разные пары через один контракт) — тот же класс ограничения, что уже задокументирован в §7 (P3, "uniswap-v4 — метод не применяется... россыпь мелких pool_id"), TVL по v4-пулу с этой колонкой напрямую не достать, нужен `pool_id` (не в этой таблице). Проект `ramsesxyz` (version='cl' — concentrated liquidity) и `uponrh`/`gigadex` — отдельные протоколы, не проверены. **Поиск готовой исторической таблицы резервов схем `*robinhood*` (`information_schema.tables`, лимит 300) не дал полезного** — вернул алфавитно первые 300 таблиц одного ERC-4626-vault контракта (`accountable_v1_1_robinhood`), не AMM-пулов; лимит съел выдачу до нужных имён, нужен более узкий фильтр (например, схема + 'swap'/'sync'/'mint' в имени таблицы), не выполнено. **Практический вывод: реальную TVL на МОМЕНТ каждого исторического окна (июль-август) без архивного eth_call пока не получить** — либо строим оценку на v3-подмножестве через ЖИВОЙ (текущий) снимок резервов с честной пометкой "снимок на сегодня, не на момент окна", либо запрашиваем архивный RPC (не проверено, есть ли доступ). Дальше — собрать round-trip-стоимость для v3-подмножества по реальным текущим резервам, доложить какая доля из 197 строк вообще имеет v3-пул.

**Проверка 1 — ФИНАЛ (владелец, 2026-09-05, "v3-подмножество, текущий TVL как приближение с пометкой, round-trip на \$500 = fee×2 + проскальзывание по резервам"), `analysis/task1_liquidity_v3_roundtrip.py`, реальные живые `fee()`+GT (0 новых Dune-кредитов).** Из 42 уникальных тикеров основного прогона только **10 реально имеют v3-пул** (32 — v4/другой протокол — **"не оценено", честно, не угадано**), из них **8 с TVL>\$200k** (текущий снимок GT, явно помечено как СЕГОДНЯШНИЙ, не исторический на момент каждого окна). Round-trip издержка на \$500 (fee×2 + проскальзывание по половине TVL×2, формула — стандартное constant-product-приближение, задокументирована в коде) варьируется от 0.18% (SPCX, TVL \$2.6M) до 4.2% (USAR, TVL \$55k, ниже порога \$200k). **Сравнение с медианным |Z| того же тикера: доля наблюдений, где |Z| > round-trip = 50.9% (все 10 оценённых) / 56.0% (только TVL>\$200k).** Робастность (исключение |X|>0.5, та же граница, что везде): убирает единственную строку AMC (её X=6.40 уже задокументирован как артефакт) — доля почти не меняется, **49.1% / 54.2%** — вывод устойчив к этому конкретному выбросу. **Предупреждение: BE оценён по единственному историческому наблюдению (N=1) — не интерпретировать как надёжный факт.** **Итог: примерно половина исторических движений Z по ликвидным (v3, TVL>\$200k) тикерам превышает реальную оценку издержек round-trip на \$500 — сигнал не съедается издержками систематически, но и не покрывает их с большим запасом; для 32 из 42 тикеров (в основном v4) экономика вообще не проверяема имеющимися методами.** Полная таблица — `data/p3_guard_cache/task1_liquidity_v3_roundtrip_result.json`.

**Сводный вердикт по проверкам 1-3 (владелец, 2026-09-05): эффект подтверждён по направлению (систематический разворот X→Z, не шум), таймингу (разворот живёт и в Z1, и в Z2, сильнее в Z1 — робастно) и монотонности (условный медианный |Z| растёт с порогом |X|) — три независимых угла на одни и те же 197 наблюдений сходятся на одном выводе, не противоречат друг другу.** Экономически: **|Z| > round-trip в 54% случаев на торгуемых тикерах (v3, TVL>\$200k, робастно) — торгуемых тикеров 8 из 42.** Это **экономика на грани при позиции \$500**: чуть больше половины сделок отбивают издержки, остальные — нет; ни явного "съедает всё", ни явного "с большим запасом". Оценка стоимости для v4-пулов (32 из 42 тикеров, недоступных этому методу через `dex.trades`/`project_contract_address`) — реальная следующая задача, но **выполняется ПОСЛЕ проверки 4 (out-of-sample), не сейчас** (владелец, 2026-09-05).

**Проверка 4 (out-of-sample, ≥60% попаданий) — ЗАПЛАНИРОВАНА, живые триггеры на ближайшие выходные 2026-09-04/06/07.** Правило прогноза (механическое, из проверки 2 выше: **predicted_sign(Z) = -sign(X)**, ставка на разворот, без новой гипотезы). `trig_01QBG5NsG32SqeqorGzo9iPJ` — прогноз, воскресенье 2026-09-06 23:55 UTC (19:55 ET), фиксирует X и прогноз ДО начала окна Z. `trig_013XKF7CDHSqnUEoS3iLFY8V` — проверка, понедельник 2026-09-07 14:00 UTC (10:00 ET), считает реальный Z и hit_rate. Оба — одноразовые (`run_once_at`), в свежей сессии (`create_new_session_on_fire`), независимы от текущей сессии. `analysis/task1_oos_check4.py` (predict/verify), `.github/workflows/run_task1_oos_check4.yml`.

Полная таблица 197 строк основного прогона — `data/p3_guard_cache/task1_weekend_gap_result.json`.

---

## Задача C (предикт-маркеты) — разведка API, в процессе, 2026-09-05

Реальные публичные API проверены (`analysis/taskC_prediction_markets_probe.py` и др., только чтение, без ключей):
- **Polymarket**: Gamma API (`gamma-api.polymarket.com/markets`, метаданные + `outcomePrices`, `clobTokenIds`), CLOB API (`clob.polymarket.com/sampling-markets`, живой стакан/токены, `maker_base_fee`/`taker_base_fee`), Data API (`data-api.polymarket.com/trades`, реальная история СДЕЛОК — timestamp/price/size/side/outcome/slug, НЕ история стакана).
- **Kalshi**: `api.elections.kalshi.com/trade-api/v2` — `/markets`, `/events`, и (реально подтверждено) **`/markets/{ticker}/orderbook` отдаёт реальный живой стакан** (`orderbook_fp.no_dollars`: массив [цена, размер]) — лучше, чем только сделки. `/markets/{ticker}/trades` (путь из документации) вернул 404 на конкретном тикере — правильный путь ещё предстоит найти (вероятно `/markets/trades?ticker=...`, не проверено).
- **SX Bet**: `api.sx.bet/markets/active` — реальные спортивные рынки (NFL и др.), только по team/line, без крипто-порогов в выборке.

**Находка (структурная, до любого сопоставления, важна для честной оценки цели "спорт и BTC-часовые"): Kalshi и Polymarket НЕ совпадают по типу и грануляции BTC-рынков.** Kalshi `KXBTC` — **часовые** пороговые рынки (много страйков в час, резолюция по среднему CF Benchmarks BRTI за последние 60 сек часа, реальный пример `KXBTC-26SEP0510-T90299.99`). Polymarket реально нашлись **дневные** пороговые рынки (`bitcoin-above-76k-on-september-5-2026` и т.п., резолюция "on September 5, 2026") и дневной `bitcoin-up-or-down-on-september-5-2026`, а также 5-МИНУТНЫЕ direction-рынки для мемкоинов (`doge-updown-5m-...`, встречен в trades-сэмпле) — но НЕ найден часовой BTC-порог, сопоставимый с Kalshi KXBTC, в первой выборке (100 самых объёмных active-рынков, поиск `search=` не сработал как фильтр — Gamma API просто игнорирует параметр и возвращает топ по активности, найдено эмпирически, не по документации). **BTC-канал — ЗАКРЫТ (владелец, 2026-09-05): "форматы не совпадают."** Kalshi (часовые пороговые страйки) и Polymarket (дневные пороговые + дневной up/down + 5-минутные для мемкоинов, часового BTC-порога не найдено даже в топ-100 по объёму) — разные типы ставок, прямое "да+нет<\$1" не строится без придумывания синтетической конвертации между грануляциями, что было бы подгонкой, не честным арбитражем. **Фокус смещается полностью на спорт.**

**Разведка спортивных форматов (`analysis/taskC_sports_match_probe.py`) — реальные факты.** Kalshi: структурированные тикеры `KXNFLGAME-26SEP21NYGLAR-NYG` (дата+обе команды в event_ticker, конкретная команда в суффиксе рынка), `title` — простое "Team X wins", реальный `yes_bid`/`yes_ask` уже в снимке. **На Polymarket и `tag_slug=`, и ранее `search=` — ОБА не фильтруют как ожидалось** (Gamma API реально игнорирует эти параметры при вызове так, как пробовали, и возвращает те же топ-активные рынки независимо от значения — вероятно нужен `tag_id` (числовой), не `tag_slug`, не проверено). **Рабочий путь, уже подтверждённый на BTC-выборке: bulk-fetch active-рынков (`limit` побольше, сортировка по объёму) + клиентская фильтрация по ключевым словам в `question`/`slug`** — это единственный реально работающий метод сейчас, поиск по названию команд будет строиться на нём, не на параметрах API.

---

## Закрытые линии

Копитрейдинг — ЗАКРЫТ (Sprint 1 + 1.5, вердикт НЕТ). Персистентность размера прибыли отсутствует: медиана августовского PnL топ-когорты июля = $0 (как у контроля), Mann-Whitney p=0.68. Факт-прибыльность персистентна (лифт 3.89–5.73x во всех 4 ячейках sensitivity, Fisher p<0.0001), но немонетизируема копированием сделок. Критерии были зафиксированы до данных; повторное тестирование линии запрещено. Детали: docs/RESULTS.md.

Graduation-momentum — ЗАКРЫТ (Sprint G1 v2, вердикт KILL, решение штаба 2026-09-01). N аналитическое=513 (гейт N≥200 пройден), ни один из 10 горизонтов (30с…24ч) не значим по BH (q≥0.05) при базовой стоимости 3%, медианы глубоко отрицательны на обеих половинах периода и при всех сценариях издержек (1/3/5%). Распад после градуации реален и велик по амплитуде, но не монетизируем доступным инструментарием (нет шорт-инструмента на чейне) — решением штаба лонг-стратегии по мем-токенам этого чейна закрыты как класс, не только эта формулировка. Критерии были зафиксированы до данных (docs/G1_DESIGN.md §2.7); повторное тестирование линии запрещено. Детали: docs/RESULTS.md.

RWA-конвергенция (сток-токены) — ЗАКРЫТ (Sprint R1, вердикт KILL, 2026-09-01). 189 событий (θ=1%: 130, θ=2.5%: 38, θ=5%: 21) на 26 ликвидных сток-токенах с известным Chainlink-фидом, 12-недельный период (01.07–28.08.2026). Из 9 ячеек (3θ × 3 горизонта) допущена только одна (θ=1%, «открытие+1ч», N=61) — не значима (p=1.00, q=1.00 по BH), медиана нетто r=-1.49% (базовые издержки 1.5%), хуже медианы контроля (-1.28%) на -0.21%. Остальные 8 ячеек UNDERPOWERED (N от 5 до 48). Критерии зафиксированы до данных (docs/R1_DESIGN.md §2); повторное тестирование линии запрещено. Детали: docs/RESULTS.md.

**Хеджированное LP (Aerodrome/Uniswap v3-стиль концентрированная ликвидность + шорт-хедж) — ЗАКРЫТ (владелец, 2026-09-05, вердикт KILL по честному GT-объёму).** Два реальных живых замера: P5 (ETH/USDG, Robinhood Chain, `real_fee_lvr_ratio_at_hist_sigma`) — **1.3–1.47**; P6 (USDC/cbBTC, Aerodrome Slipstream, Base, реально открыт и закрыт, RESULTS.md §8-9) — **~0.08** (`ratio_to_lvr_real_30d_sigma`=0.077, таблица ниже). Два прохода скринера по 23 кандидатам (DefiLlama→AMM-фильтр→GT-резолв, §"Скринер пулов"): первый на `apyBase` DefiLlama — методологически неверен (баг №17: DefiLlama завышает fee APR систематически, на P6-пуле в 352.7×); второй, честный (правило №18, реальный fee tier с контракта + реальный GT-объём + реальная 30-дневная σ + k_pool в знаменателе LVR) — **0 из 21 реально посчитанных проходят порог ratio≥2** (полная таблица — баг №22 выше, лучший результат 0.954, вдвое ниже порога). Калибровка на живой позиции P5 — принята (баг №22): формула воспроизводит истинный порядок величины с объяснённым остаточным разрывом (окно ~5×, источник дохода ~1.5×), никакого дополнительного скрытого расхождения не найдено. **Вывод (владелец, дословно): на 30-дневном окне отношение ≥2 без субсидированного объёма не найдено; это согласуется с литературой по v3 на крупных парах.** Критерии (реальный fee tier с контракта, реальный GT-объём 7д/30д, реальная 30-дневная σ, k_pool в знаменателе, порог ≥2) зафиксированы до финального прогона (владелец, «полный пересчёт... формула без изменений»); повторное тестирование на существующем наборе пулов запрещено. **Линия переоткрывается только новым источником объёма (не субсидированным DEX-объёмом, не новым пулом на том же принципе дохода)** — не решением найти ещё пул. Детали: `docs/PROJECT_STATE.md` §4 баги №14-22, `RESULTS.md` §5-9.

**Фандинг между площадками (Lighter × Hyperliquid, кросс-биржевой спред) — ЗАКРЫТ (владелец, 2026-09-05, вердикт KILL по честному историческому бэкфиллу).** Полный бэкфилл 13 пар (`funding_pairs.json`) с реальной earliest точки (2026-07-05) по сентябрь — `analysis/funding_historical_backfill.py`, реальные подтверждённые параметры пагинации (Lighter `/fundings` count_back=750, Hyperliquid `fundingHistory` лимит страницы 500). **primary (3 пары): медиана \|спред\| годовых = 0.44%, доля часов выше безубыточности (1.3бпс/8ч) = 8.3%, доля выше 30%/год = 4.6% — НЕ ПРОХОДИТ** (порог median≥30%/год при доле≥50%). **exploratory (10 пар): медиана 0.44%, доля выше безубыточности 18.5% — НЕ ПРОХОДИТ** (порог median≥60%/год). Устойчиво по неделям — та же медиана ≈0.44%/год почти в каждой из ~9 недель обеих когорт, не единичный выброс (полная понедельная таблица — `data/p3_guard_cache/funding_historical_backfill_result.json`). Сверка с `predictedFundings` (снимок Binance/Bybit, HL внутри не считается независимой проверкой) — реальные числа получены по всем 13, разумного порядка величины. **Вывод: две конкурентно арбитражируемые площадки держат спред у базовой линии почти всё время** — тот же класс структурного равновесия, что уже найден для хеджированного LP (кластер 0.7–0.95, №22) — рынок эффективен там, где обе стороны реально ликвидны и конкурируют. Критерии зафиксированы до прогона; повторное тестирование этого набора из 13 пар запрещено. Cron `:21` (funding_hourly_snapshot.sh) — остановлен, `:51` (turnover_watch) не тронут, см. ниже. Детали: `data/p3_guard_cache/funding_historical_backfill_result.json`, `data/funding_historical_cache/funding_spread_hourly_full.csv`.

**Реальное подтверждение снятия cron `:21` (2026-09-05, `run_vps_remove_funding_cron_nl.yml`, run 33967233700, success).** Прямое чтение crontab на NL-хосте ДО: `21 * * * * .../funding_hourly_snapshot.sh` + `51 9 * * * .../turnover_watch_daily.sh` (обе строки на месте). ПОСЛЕ: только `51 9 * * * .../turnover_watch_daily.sh` — строка funding реально отсутствует и в `crontab -l`, и в сыром файле `/var/spool/cron/crontabs/bot`. Явные проверки `grep` внутри самого SSH-шага подтвердили: "OK: строки funding нет" + "OK: turnover_watch-строка на месте". SSH/остальные cron не трогали.

(Кластер 0.7–0.95 на 21 пуле — см. полное описание в §4 баг №22 выше.)

**Сторожок на переоткрытие (владелец, 2026-09-05, дополнение к закрытию): пулы с TVL≥$2M и суточным объёмом >20× TVL, DefiLlama+GT, без капитала — только ловит, ничего не пересчитывает.** `analysis/turnover_watch.py` — те же два независимых источника, что скринер: DefiLlama `/pools` (новые пороги, не 23-кандидатный набор) + GT-сверка (`pool_screener_resolve_pools.py::resolve_pool()`, реюз) только для кандидатов, уже прошедших фильтр DefiLlama. Результат — append-only `data/turnover_watch.jsonl`, "новые" считаются сравнением с объединением `pool_id` всех предыдущих строк. Первый прогон (2026-09-05) — честный ноль (0 кандидатов над порогом на Base/Arbitrum/BSC/Robinhood).

Механизм сбора (владелец, 2026-09-05, ИЗМЕНЁН в тот же день): изначально — ежедневный self-bound CCR Routine (`trig_01UdBXpkcRqo4Gd1SQoEvtJi`, 09:00 UTC), затем **перенесён на автономный суточный cron на VPS Нидерланды** (правило проекта: сбор не должен зависеть от сессии/чата — тот же принцип, что P5/funding). Routine удалён после подтверждения cron. Реализация — `run_vps_setup_turnover_watch_cron_nl.yml`, ТОЧНО тот же паттерн, что `run_vps_setup_funding_cron_nl.yml`: обёрточный скрипт `/home/bot/bin/turnover_watch_daily.sh` запускает `turnover_watch.py` НАПРЯМУЮ на VPS (публичный интернет VPS, не через GH Actions — DefiLlama/GT доступны с VPS напрямую), накопленный ряд живёт ВНЕ git-репозитория (`/home/bot/turnover_watch_live_data/turnover_watch.jsonl`), git push на VPS НЕ используется (тот же принцип, что funding/P5). Crontab `bot` — ЧЕТВЁРТАЯ строка, минута `:51` (`51 9 * * *`, 09:51 UTC), ДОПИСАНА к существующей `:21` (funding), не заменила её — подтверждено реальным чтением `crontab -l` до/после (GH Actions run 33965384082): ДО — только `:21`; ПОСЛЕ — `:21` и `:51`; тестовый ручной прогон (шаг 5) подтверждён вживую — внешний файл реально пополнился второй строкой. **TODO (как у P5/funding): забор накопленного `/home/bot/turnover_watch_live_data/turnover_watch.jsonl` обратно в git — отдельный маркер-триггер по аналогии с `P5_LIVE_SNAPSHOT_REQUEST`, ещё не реализован** — данные растут ВНЕ git, `data/turnover_watch.jsonl` в репозитории актуален только на момент последнего явного забора (пока — только начальный прогон 2026-09-05 через `run_turnover_watch.yml`, до переноса на VPS-cron).

## P3 / P4 / депеги — история находок (2026-09-01 → 2026-09-03, для справки, актуальный статус P3 см. §7 выше)

**P3 (кросс-версионная дислокация сток-токенов, v3↔v4) — изначально переведён в WATCHLIST** (решение владельца, 2026-09-01): латентностная гонка, регим-брейк 29.09.2026 (конец 90-дневного gas waiver), бюджет Dune. Запущен дешёвый гард без Dune (RPC/Blockscout через Alchemy) — критерий: доля топ-3 адресов в закрытиях дислокаций >80% → закрытие. Методология зафиксирована до результата, `docs/P3_GUARD.md`.

**Обновление 2026-09-01:** блокер по ключу RPC снят — публичный `https://rpc.mainnet.chain.robinhood.com` работает без ключа (rate-limit 429 на быстрых вызовах, ~3/с). `analysis/alchemy_fallback.py` — публичный RPC основной, Alchemy/Blockscout фолбэк.

**Обновление 2026-09-03 (MM, п.5) — ИСПРАВЛЕНА ошибка метода:** первая версия утверждала "0 из 24 сток-токенов имеют v3-пул" — ОШИБКА, не факт. Владелец нашёл вручную 9 реальных пулов (GeckoTerminal), все 9 подтверждены `eth_call`: 7 v3 (NVDA/QQQ/RDDT/COST/GME/RBLX/LLY), 2 v4 (SPY/MSTR). Корень ошибки: `mm_discover.py` искал `PoolCreated` в коротком окне (20 000 блоков, ~30 мин) — фабрика могла не эмитировать событий за это окно, стадия 2 не запускалась вообще (`if v3_factory else []`).

**Уточнение (владелец, 2026-09-03):** реальный вопрос — есть ли токен с ОБЕИМИ ногами (v3 И v4) одновременно. Проверено напрямую (`mm_p3_v4leg_check.py`, GH Actions run 33745293273): **ВСЕ 9 из 9 токенов имеют обе ноги с реальной ликвидностью.** SPY/MSTR (были известны только как v4) — у обоих нашёлся реальный v3-пул, не dust. Реальный V3 Factory — `0x1f7d7550b1b028f7571e69a784071f0205fd2efa`, НЕ совпадает со стандартным Uniswap Labs CREATE2-адресом.

**DELL/USO** — не найдено НИ ОДНОГО пула (ни v3, ни v4) — не дислокация, просто нет ончейн-пары; цена $1.00/капитализация $3.2 млрд на GeckoTerminal, вероятно, дефолтное отображение отсутствия торгового пула.

**Итоговый статус P3 на 2026-09-03 (актуализирован §7 выше реальным гардом): линия НЕ закрыта, доля топ-3 5-8% << 80%, приоритет — после P5.**

**Приоритеты (владелец, 2026-09-03): P5 — приоритет №1. SC1-лаунчер, тайминги Pons, wash-срез, релеер Across — отложены. MM (сток-токены) — приоритет №2, дешёвая часть.**

**P5, разведка (до живого запуска, 2026-09-03):** пул `0x52e65b17fb6e5ba00ed806f37afcd2daa50271ca` подтверждён ончейн (WETH/USDG Uniswap v3, fee 0.01%), цена из `sqrtPriceX96` сходилась с CoinGecko/DexScreener на момент проверки. GeckoTerminal/Uniswap subgraph не поддерживают эту сеть; DexScreener — только текущий снепшот; DefiLlama — обзор объёма есть, дневной ряд не проверен. Полный лог-скан свопов за 30 дней оценён в ~337 минут (×3.7 сверх лимита job'а) — не выполнен, ждёт решения по сужению.

**MM базис (приоритет №2, справочно):** NVDA/SPY/QQQ/GME/MSTR — базис пул↔перп на снимке <0.4% везде, рынки эффективно арбитражированы на момент проверки.

**P4 (фандинг/базис на перп-рынках сток-токенов) — приоритет №1 по внешней разведке 01.09, переформулирован в "дельта-нейтральный фарм LIT-поинтов".** Ключевые находки: 231 перп-рынок на Lighter, 45 сток-подобных; максимум истории фандинга 62.42 дня (критерий №1 пройден); единица `rate` исправлена (уже процент, не доля — 3 независимых доказательства) — медиана годового фандинга после исправления **3.504%/год**, не ~350%; критерий №2 P4_KILL (`3.504% − издержки хеджа < 8%`) истинен при любых неотрицательных издержках → **P4_KILL критерий №2 НЕ ПРОЙДЕН**. Понедельные LIT/объём для реальной оценки поинт-экономики не найдены ни в одном источнике — не посчитано, не додумано. `docs/P4_RECON.md`, `docs/P4_KILL.md`.

**Поинты Lighter — статус 2026-09-04 (частичный прогресс, не закрыто).** Предыдущий тупик (`docs/P4_RECON.md` раздел 4, 2026-09-02): недельные суммы LIT за 21.08/28.08 не нашлись через бесплатные Ethereum RPC — ДВА разных бесплатных шлюза (`cloudflare-eth.com`, `publicnode`/`llamarpc`) отказали на `eth_getLogs` по инфраструктурным причинам (`-32603`, `403 Forbidden`), похоже на блок IP-диапазона GH Actions раннеров как датацентрового трафика — это была стена инфраструктуры, не отсутствие данных. Новый Dune-аккаунт индексирует Ethereum mainnet (не только Robinhood Chain) — тот же путь (Transfer-логи контракта LIT `0x232ce3bd40fcd6f80f3d55a522d03f25df784ee2`) через SQL вместо RPC. **Подтверждено реальным запуском:** обе кандидатные таблицы существуют и содержат данные — `tokens.transfers` (22 колонки) и `erc20_ethereum.evt_transfer` (12 колонок), оба peek реально исполнились и были оплачены (~0.3 кредита суммарно, namespace `lit_points_mozila`, отдельный бюджет 30 из 2000). Инфраструктурная стена снята — путь технически открыт. Реальные колонки восстановлены через `fetch_existing()` на уже оплаченном execution_id (не угаданы) — `tokens.transfers.amount` уже в человекочитаемых единицах (decimals применены Dune).

**Итог агрегации топ-20 отправителей LIT в окнах ±1 день вокруг 21.08 и 28.08 (2026-09-04): явного кандидата на адрес программы поинтов НЕ ВЫДЕЛИЛОСЬ.** В окне 21.08 топ-1 (`0x091d1c97...`, 7.636M LIT, 3 транзакции) и топ-2 (`0x5ee84d30...`, 7.6139M LIT, 3 транзакции) почти идентичны по объёму — паттерн, характерный для разовых крупных переводов (кит/OTC/биржа), а НЕ для еженедельной раздачи множеству разных кошельков. Адрес с ЗАМЕТНО наибольшим числом транзакций (`0x000000000004444c5dc75cb358380d2e3de08a90`, 3363 транзакции за окно) даёт лишь умеренный объём (945.7K LIT) — по адресу с вида (нулевой префикс) похож на DEX-агрегатор/settlement-контракт (например, паттерн CoW Protocol GPv2Settlement), а не на распределитель наград; тот же адрес фигурирует и как получатель в peek-примере выше. В окне 28.08 картина аналогична — топ-адреса сопоставимы по объёму (1.73M/1.53M/1.16M/1.05M/1.03M LIT), без явного отрыва. **Вывод: дальше не гадаем** — ни один адрес не показывает паттерн "одна отправляющая сторона → много получателей → сопоставимый объём каждую неделю", ожидаемый от контракта раздачи поинтов; либо реальная раздача идёт off-chain/через L2-claim-контракт с другим адресом (не найденным этим срезом), либо объём слишком размазан по множеству путей buyback/маркет-мейкинга, чтобы выделиться этим методом. **Статус закрывается как "метод исчерпан на доступных данных"**, не как "найдено" — величина LIT/неделю по программе поинтов остаётся неизвестной. Фактическая стоимость всей серии запросов (первая неудачная попытка + восстановление + финальная агрегация): **0.641 кредита из 30** (namespace `lit_points_mozila`), пренебрежимо мало. Сырые данные: `data/p3_guard_cache/dune_lit_points_weekly_result.json`.

**Логгер спреда фандинга Lighter × Hyperliquid — развёрнут, 2026-09-04.** Пары зафиксированы (`data/funding_pairs.json`: 13 пар, BTC/ETH/LIT primary + 10 exploratory, top-15 по объёму Lighter, список не менять без `force=True`). Сборщик `analysis/funding_spread_hourly_snapshot.py`: Lighter — `/api/v1/fundings` (ТРЕБУЕТ все 5 параметров, включая `start_timestamp`/`end_timestamp` — без них 400 "invalid param", реально проверено `analysis/lighter_funding_endpoint_probe.py`); Hyperliquid — `metaAndAssetCtxs`/`l2Book`. Резилентность: retry с backoff на транспортном уровне, падение одной биржи → `null`+`error` по этой стороне, цикл не пропускается. Тестовый прогон через GH Actions — 13/13 пар, 0 ошибок, mark price между биржами сходится (BTC $79475.8 vs $79483.0, ETH $2447.96 vs $2448.12).

**Единица `rate` у Lighter на api.rh.lighter.xyz — подтверждена ЧЕТВЁРТЫМ, прямым способом (2026-09-04), сверх 3 доказательств в `docs/P4_RECON.md`.** Метод: реальный Δfunding на живой позиции 1000756 (`data/p5_fee_accrual.jsonl`, hedge 0.0377 ETH) за 3 последовательных временных окна сопоставлен с точными историческими записями `/api/v1/fundings` для ETH (market_id=0) за ТЕ ЖЕ часы. Результат (`data/p3_guard_cache/verify_lighter_rate_units_result.json`): если `rate` = процент/час → отношение факт/предсказание = **0.836 / 0.538 / 1.004** (порядок 1, разброс — не совпадение снимков с часовыми границами Lighter); если `rate` = доля/час → отношение = **83.6 / 53.8 / 100.4** (на два порядка мимо). **Вывод: `rate` — процент за час, окончательно подтверждено собственными живыми данными, не только документацией.** Код (`funding_spread_hourly_snapshot.py`) уже использовал это допущение — теперь оно факт, не предположение.

**Единица `funding` у Hyperliquid — ПОДТВЕРЖДЕНА, 2026-09-04, двумя независимыми способами на реальных данных** (не документацией — `docs.hyperliquid.xyz` заблокирован для прямого фетча из этой сессии). `funding` — ДОЛЯ (fraction) за час, домножается на 100. (1) **Абсолютная величина**: сырой кластер ~0.0000125 как доля = 0.00125%/ч = **0.01%/8ч** — стандартная базовая ставка фандинга (процентная компонента при premium≈0), общая конвенция крупных площадок (Binance/Bybit/Hyperliquid); как процент напрямую (0.0000125%/ч) была бы на 2 порядка ниже любой реальной ставки. (2) **Перекрёстная сверка с Lighter** (независимо подтверждён как %, см. выше) на реально собранных точках `data/funding_spread.jsonl`: ETH и BTC — Lighter=0.0012%/ч, HL.raw×100=0.00125%/ч, **совпадение до 3 значащих цифр**, спред ≈0.00005%/ч — ожидаемо, обе площадки арбитражируются к одному рынку. Пометка «неверифицировано» снята из кода (`analysis/funding_spread_hourly_snapshot.py`) и из построчного поля `spread_computation_caveat` (убрано — единицы обеих сторон теперь факт, не допущение).

**Проверка сходимости спреда ETH/BTC — ПРОЙДЕНА, 2026-09-04, на первых 3 реальных автономных циклах cron** (18:21, 19:21, 20:21 UTC — не тестовые ручные прогоны). На всех трёх циклах, для ОБОИХ активов: `spread_hourly_pct_lighter_minus_hyperliquid` = **-0.00005** (0.005 б.п.), стабильно и идентично между ETH и BTC — не случайное совпадение одной точки. Подтверждает калибровку единиц третьим независимым способом (после Δfunding позиции 1000756 и базовой-ставки-кросс-сверки).

**Автономный cron на NL VPS, пользователь `bot`:** ОБЕ строки подтверждены `crontab -l` после установки — `6 * * * * p5_hourly_snapshot.sh` (без изменений) и `21 * * * * funding_hourly_snapshot.sh` (новая, дописана через файл, не заменила P5). Собранный ряд — вне git (`/home/bot/funding_live_data/funding_spread.jsonl`), как у P5. Забор накопленного в git — TODO, по аналогии с `P5_LIVE_SNAPSHOT_REQUEST`.

**Скринер пулов (DefiLlama → AMM-фильтр → GT σ/LVR) — ПЕРВЫЙ ПРОХОД (19/23 прошли) БЫЛ МЕТОДОЛОГИЧЕСКИ НЕВЕРЕН, ИСПРАВЛЕНО 2026-09-04.** DefiLlama (TVL≥$5M, объём≥20%TVL/сутки, BNB/Base/Arbitrum/Robinhood Chain) — 157 кандидатов, 0 AMM на Robinhood Chain (только лендинг). AMM-фильтр (uniswap-v3/v4, aerodrome-slipstream/v1) — 23 кандидата, все резолвлены на реальные адреса через GT (23/23 plausible).

**Калибровка на собственном пуле ETH/USDG (владелец, п.1) вскрыла реальный дефект метода.** Тот же метод скринера (GT 30-дневная σ=49.53%, full-range LVR=σ²/8=3.07%/год, apyBase-эквивалент из реальных пуловых комиссий=153.73%) дал `ratio_screener_method=50.14`. РЕАЛЬНОЕ отношение по живой позиции (`fee_lvr_ratio_at_hist_sigma`, узкий диапазон, уже учитывает фактическую концентрацию) = **1.4666** (не ровно 1.3, как помнил владелец — актуальное значение на 2026-09-04T15:06Z). **Множитель ошибки: 34.18x** — full-range допущение системно и сильно завышает ratio для узкого диапазона. `analysis/pool_screener_calibrate_p5.py`, `data/p3_guard_cache/pool_screener_calibrate_p5_result.json`.

**Поправка на концентрацию (владелец, п.2): k = L_active(RPC slot0+liquidity)/L_full-эквивалент той же стоимости, LVR_pool=k·σ²/8.** `analysis/pool_screener_concentration.py` — реальные ончейн-вызовы (Base/Arbitrum/BSC RPC, chainId проверен перед доверием), 21/23 посчитано (2 uniswap-v4 — метод неприменим, singleton PoolManager архитектурно не даёт slot0()/liquidity() как у v3; 3 aerodrome-v1 — k=1 по определению, некоцентрированный AMM). Первый прогон дал 15/23 ошибок 429 (Base RPC без троттлинга) — исправлено (троттлинг+ретрай), второй прогон чистый (0 ошибок помимо 2 v4, 0 price_check_suspect после фикса сопоставления цен по адресу вместо позиции).

**РЕАЛЬНЫЙ результат после поправки: 3 из 23 проходят порог apyBase/LVR≥2** (было 19 до поправки — ровно то падение "с десятков до 1–3", которое предсказал владелец). Прошли: (1) Base aerodrome-slipstream USDC-CBBTC, k=9.90, ratio_k_adj=**31.57** — это тот же пул с apyBase=548.58%, см. проверку выброса ниже; (2) Base uniswap-v3 WETH-CBBTC, k=31.16, ratio_k_adj=**2.07** — погранично, чуть выше порога; (3) Base aerodrome-slipstream WETH-CBBTC (тот же токен-пара, другой DEX), k=31.16, ratio_k_adj=**2.01** — тоже погранично. Ещё 8 пулов в диапазоне 1.04–1.60 — близко к порогу, но честно не прошли. Остальные — от 0.62 до 0.006 (аэродром-v1 — 0, эмиссия без органики).

**Проверка выброса USDC-CBBTC apyBase=548.58% (владелец, п.4): это НЕ спайк одного дня.** `apyBase7d`=279.08%, `apyMean30d`=178.84% — устойчиво (хоть и снижающийся со временем) экстремальный уровень на ВСЕХ временных окнах, не отбрасывается как шум по критерию владельца. Даже после k-поправки этот пул остаётся сильно выше порога (31.57). Вероятная причина — крайне высокое отношение объём/TVL (объём $11.4M/сутки при TVL $6.94M, >160%/сутки оборота) на низкой базе ликвидности; требует РУЧНОЙ проверки живого пула перед любым капитальным решением (низкая TVL = высокий риск проскальзывания/манипуляции объёмом, wash-объём ещё не проверен). Отдельно — ДРУГОЙ пул (Base uniswap-v3 USDC-CBBTC, изначально apyBase=41.09%) показал признаки настоящего шума: `apyBase7d`=11.66%, отношение сегодня/7д=3.47x — этот кандидат уже не прошёл ни full-range, ни k-adjusted порог, спайк не повлиял на итоговый список прошедших. `analysis/pool_screener_recheck_7d.py`, `data/p3_guard_cache/pool_screener_recheck_7d_result.json`.

**Разделение stable/volatile (владелец, п.3): 0 из 23 — стейбл-пары** (честно по полю DefiLlama `stablecoin`, не предположено) — `stable_candidates` пуст, весь список остаётся волатильным/основным. `data/p3_guard_cache/pool_screener_stable_split_result.json`.

**ФИНАЛ ("3 кандидата" оказались 2 реальными пулами — DefiLlama задваивает Aerodrome Slipstream под меткой uniswap-v3, тот же физический адрес).** Полная таблица (2026-09-04, все цифры реальные, не оценки):

| Пул | Цепь | Fee tier | TVL | apyBase (снимок/сейчас/7д/30д) | σ 30д | k | LVR=k·σ²/8 | ratio | wash_share | Lighter-перп |
|---|---|---|---|---|---|---|---|---|---|---|
| **USDC-CBBTC** (`0x3e66e55e...`, aerodrome-slipstream) | Base | 0.033% | $6.94M | 548.58% / 486.78% / 279.08% / 178.84% | 37.47% | 9.90 | 17.38%/год | **31.57** | НЕДОСТУПЕН через Dune (`aerodrome_slipstream_base` decode'ит только `pools`/`base_liquidity_events`, Swap-события не декодированы под этим проектом) | BTC (id=1) ✅ — CBBTC как прокси, базисный риск к нативному BTC не проверен |
| **WETH-CBBTC** (`0x7aea2e8a...`, один физический пул, задвоен DefiLlama под uniswap-v3 И aerodrome-slipstream) | Base | 0.05% | $8.63M / $9.44M (2 трекера расходятся) | 86.92%/84.55% (2 трекера) | 32.83% | 31.16 | 41.98%/год | **2.01–2.07** | топ-3 sender = 71.6% объёма, НО top_sender ≠ top_recipient (концентрированно, но НЕ классический паттерн самоторговли 0x65050a) | ETH (id=0) ✅ + BTC (id=1) ✅ — обе ноги волатильны, нужен двойной хедж |

**Entry cost (обе на Base):** газ Base ≈0.006 gwei (копейки), мост Across Robinhood Chain→Base **реально поддерживается** (маршруты USDG→USDC/WETH→WETH/ETH→ETH подтверждены API) — котировка на $100: комиссия ≈0.063%, исполнение 2 сек.

**РЕШЕНИЕ ВЛАДЕЛЬЦА, 2026-09-04:**

1. **Рабочее отношение — на 30-дневном apyBase, не на снимке.** `apyBase30d/LVR_k_adj = 178.84% / 17.38% = 10.3`. Снимок 548.58% (и его прямое ratio=31.57) **в решении не используется** — он отражает временный (хоть и не однодневный) пик, а не устойчивый уровень. Реальный тренд — **затухание 548.58% → 486.78% (сейчас) → 279.08% (7д) → 178.84% (30д)**, монотонное снижение на всех трёх окнах. Рабочее число для kill-теста и решения о входе — **10.3**.
2. **WETH-CBBTC — ОТБРОШЕН.** ratio 2.01–2.07 на пороге без запаса, требует хеджа по двум волатильным ногам (ETH И BTC) — риск/сложность не оправданы пограничным числом.
3. **Единственный кандидат — USDC-CBBTC**, ratio=10.3 (30-дневный, рабочий). Дальше — два дешёвых шага перед входом (см. `docs/P6_HEDGED_LP.md`): (а) базис cbBTC/BTC за 90 дней, (б) dry-run входа (ничего не отправляя) с реально прочитанными числами. Спецификация протокола P6 и kill-критерии — заморожены в `docs/P6_HEDGED_LP.md` ДО первого расчёта (тот же режим, что P4_KILL.md/R1_DESIGN.md).

**Шаг 1 (базис cbBTC/BTC) ЗАВЕРШЁН, 2026-09-04.** Максимум |часовой базис| за 90 дней = **5.03%** (2026-06-25, cbBTC=$58 096 vs BTC=$61 171), диапазон [-5.03%, +4.03%], среднее +0.02%. На реальных днях просадки BTC (<-3%/сутки): 2026-08-28 → базис до 1.61%; 2026-07-13 → до 0.98%. `analysis/p6_cbbtc_btc_basis.py`, `data/p3_guard_cache/p6_cbbtc_btc_basis_result.json`.

**Пересчёт базиса + переписанный kill-критерий №3, 2026-09-04 (владелец).** По сырому ряду: **61/2160 часов** (2.82%) с |часовой базис|>1%, максимальная серия подряд — всего **2 часа** (подтверждает предположение владельца: это часовые выбросы тонкого пула, не устойчивый сдвиг). Суточный VWAP-базис (календарные сутки UTC, объёмно-взвешенный) за 90 дней: максимум **2.30%** (2026-08-19), только 1 сутки > 1%, только 1 сутки > 2% — БЕЗ повтора на следующие сутки. **Kill-критерий №3 переписан владельцем**: «суточный VWAP-базис > 2% дольше 24 часов подряд (≥2 суток подряд)» — по этому определению критерий **НЕ СРАБОТАЛ** (`triggered: false`, `max_consecutive_days_gt_2pct: 1`). `analysis/p6_basis_recompute.py`, `data/p3_guard_cache/p6_basis_recompute_result.json`, обновлённый критерий — `docs/P6_HEDGED_LP.md`.

**P5 — dry-run закрытия (ETH-шорт + LP 1000756), 2026-09-04, ничего не отправлено.** Закрытие хеджа: unrealized_pnl=+$2.04, funding уже выплачен (сунк)=$0.027, taker_fee реальный=0% → реализовано сейчас = +$2.04. LP: реальные накопленные комиссии (eth_call-симуляция `collect()`, актуальная сумма, не устаревший tokensOwed) = $0.373; принципал при decrease сейчас (0.04654 ETH + 80.51 USDG) vs известный депозит на входе (0.03773 ETH + 102.38 USDG) → IL = -$0.238; реальный газ на decreaseLiquidity+collect (eth_estimateGas) = $0.508. **Итоговый net PnL при закрытии сейчас = +$1.66.** Освобождается на Lighter (available_balance) = **$30.82** (на аккаунте нет других открытых позиций кроме ETH, проверено по факту). `analysis/p5_close_dry_run.py`, `data/p3_guard_cache/p5_close_dry_run_result.json`.

**P6 — реальное плечо BTC выставлено и подтверждено, 2026-09-04.** Владелец: «плечо BTC не брать по дефолту, а выставить аутентифицированным вызовом (не ордер) и перечитать». Отправлена реальная (единственная мутирующая в этой цепочке) транзакция `update_leverage` на Lighter (аккаунт 22012, рынок BTC, market_id=1, margin_mode=0/cross — то же, что реальная ETH-позиция) → **2.0x**, подтверждено повторным чтением аккаунта (`confirmed_by_account_reread: true`). `analysis/p6_set_btc_leverage.py`, `data/p3_guard_cache/p6_set_btc_leverage_result.json`.

**Шаг 2 (dry-run входа) ПЕРЕСЧИТАН, 2026-09-04 — с реальным подтверждённым плечом (без фолбэка на дефолт).** Пул `0x3e66e55e...`, тик `-66815`, price cbBTC≈$79 723. LP-нога: USDC=$125.00 / cbBTC=0.00142191 (≈$113.36), тики [-68000, -64000]. Шорт BTC на $113.36 при реальном плече 2.0x: требуемая маржа=$56.68. **Сейчас** (P5 ещё открыт): `available_balance`=$31.03 — маржи не хватает без довнесения. **Сценарий после закрытия P5** (реальный freed_on_lighter=$30.82 из dry-run закрытия выше): `available_balance`=$61.86 — **маржи хватает** (`margin_sufficient_after_p5_close: true`); BTC-шорт становится единственной позицией на аккаунте → ликвидация по точной (не изолированно-условной) формуле = **$121 768**. Газ: реальная Mint-транзакция (gasUsed=4 846 063 на этот раз) при `eth_gasPrice`≈0.006 gwei → $0.07. Across на $250: комиссия ≈0.061% (~$0.15), 2 сек. `analysis/p6_dry_run_entry.py`, `data/p3_guard_cache/p6_dry_run_entry_result.json`.

**ГЕЙТ 1 ИСПОЛНЕН, 2026-09-04 — P5 РЕАЛЬНО ЗАКРЫТ (владелец: «Да»).** Порядок: закрыт ETH-шорт на Lighter (reduce_only, `tx=7576705d...`, flattened=true) → decreaseLiquidity+collect на 1000756 (реально, `tx=0x13be1539...`/`0xf2f0a845...`, оба success). **Финальный PnL по всем ногам: +$1.26** (хедж +$2.057289 по коллатералу до/после $59.826→$61.883, LP fees+IL−весь газ открытия/закрытия = -$0.792460). Полная разбивка — `RESULTS.md`. Cron P5 (:06) **остановлен на VPS** (проверено до/после, funding-строка :21 сохранена); ряд `data/p5_fee_accrual.jsonl` не менялся, последняя точка `2026-09-04T22:06:02Z` (27 строк) остаётся как есть.

**P6 — размер пересчитан под правило свободной маржи ≥40%, dry-run, ничего не отправлено.** Реальный collateral после закрытия P5 = $61.883369 (позиций нет). Целевой капитал $250 давал свободную маржу лишь ~8% — по прямому указанию владельца («уменьшить LP, не маржу») капитал подобран итеративно по реальным прогонам до **$161**: LP USDC=$80.50/cbBTC=0.00091619 (≈$73.00), шорт BTC $73.00 при подтверждённом плече 2.0x, требуемая маржа=$36.50, **свободная маржа=$25.38 (41.02%, правило выполнено)**, ликвидация (единственная позиция, точная формула)=$145 479. Полная таблица — `RESULTS.md`, раздел 5. Вход в P6 остаётся отдельным гейтом — нужно отдельное явное «да» владельца.

**ГЕЙТ 2, 2026-09-04 (владелец: «Да, при одном условии») — сбор (p6_hourly_snapshot + kill-флаги, cron :36) собран и сухо протестирован, вход НАЧАТ и ОСТАНОВЛЕН на реальной ошибке.** Отправлена 1 безвредная транзакция (`approve` USDG→SpokePool, `tx=0xe3ab9448...`, success). `depositV3($161)` реально упал на `eth_estimateGas` с кастомной ошибкой `0x356680b7` = **`InsufficientFunds()`** (подтверждено 4byte.directory + openchain.xyz, `hasVerifiedContract:true`) — **реальный баланс USDG на кошельке $80.282426, меньше запрошенных $161** (пробел в проверке перед сборкой tx: целевой капитал из margin-правила не сверялся с фактическим балансом USDG). WETH на кошельке (0.0607, ≈$149) не конвертирован в USDG — отдельная непротестированная транзакция. Варианты (A: войти на $80 реально ликвидных USDG, свободная маржа ≈70.7%; B: сначала свопнуть WETH→USDG; C: владелец пополняет USDG вручную) — решение за владельцем, `RESULTS.md` §6. Ничего дальше не отправлялось.

**Депеги (сток-токен ↔ базовый актив) — только мониторинг, не активная линия.**

## Побочный замер SC1 — ЗАВЕРШЁН (вердикт GO, V1-эра)

Sprint SC1 — вердикт **GO** (медианный конвейер 48 кластеров ≥50 запусков: $4 812 комиссий против $374 издержек, 14.5x против порога 2x; концентрация топ-5% 78.65% < порога 90%). Оговорка: вывод привязан к V1-эре, применимость к V2 — качественная. Топ-10 серийных кластеров по net/$/день: медиана $14 239.93/день (V1), $9 967.95/день (V2, ×0.7) — описательно, без порога GO/KILL. Детали: docs/RESULTS.md, docs/SC1_NOTE.md.

## Ограничения чейна / доступа к данным (проверять заново перед тем, как на них ссылаться)

**Исправлено 2026-09-04** (честно: точного раздела с таким названием раньше не было — устаревшее представление жило в выводах разных мест, ближайший письменный след — `docs/DATA_ACCESS.md` про Dune-бюджет и `analysis/alchemy_fallback.py::_chunked_get_logs` про реальную ошибку RPC). Было фактически принято как «полноценные бэктесты по ончейн-логам не работают» — это смешивало два РАЗНЫХ ограничения:

1. **Лимит `eth_getLogs` в 10 000 логов за один вызов** — это лимит ОДНОГО запроса к публичному RPC (реальная ошибка Alchemy: `"logs matched by query exceeds limit of 10000"`, см. `_chunked_get_logs`), не лимит на бэктест как таковой — обходится пагинацией по диапазону блоков, уже реализовано. Актуален и сейчас, но **только для прямых вызовов RPC** (Alchemy fallback путь), не для Dune.
2. **Старый Dune-аккаунт был близок к исчерпанию цикла** (2500/цикл, ~2138.84 потрачено на 2026-09-01, см. `data/credits_spent.json`) — это ограничение бюджета конкретного аккаунта, не архитектурное ограничение чейна/Dune.

**Новый факт (владелец, 2026-09-04):** появился ВТОРОЙ, отдельный Dune-аккаунт (`secrets.DUNE_API_KEY_MOZILA`, заявлено 2000+ кредитов, отдельный леджер `data/credits_spent_mozila.json` — НЕ смешивается со старым циклом). Это снимает бюджетное ограничение №2 для новых бэктестов через Dune SQL. Лимит №1 (10k логов) по-прежнему актуален, но только для прямого RPC-пути — у Dune SQL-движка нет такого построчного лимита на возврат (ограничения там — по кредитам/объёму читаемых данных, см. правила ниже).

**Общее правило (второй устаревший инфраструктурный факт за два дня — после ошибочной записи «нет исходящего TCP из сессии Code», см. §4 п.8):** ограничения инфраструктуры (сеть/RPC/кредиты/доступ к API) проверяются заново непосредственно перед тем, как на них ссылаются в решении — не берутся из памяти предыдущего чата или более раннего состояния проекта. Оба случая обнаружены не проактивной проверкой, а внешним триггером (задание владельца) — если в следующий раз такого триггера не будет, устаревший факт останется незамеченным.

**EDGAR 8-K acceptance-datetime — реальный баг найден и исправлен, 2026-09-04.** Полный текстовый файл заявки (`.../Archives/edgar/data/{cik}/{accession}.txt`, единственный источник ВРЕМЕНИ подачи, не только даты, через тег `<ACCEPTANCE-DATETIME>` в SGML-заголовке) требует accession **С дефисами** в этом конкретном плоском URL — вариант без дефисов (`accn_nodash`, верный для имени подкаталога заявки) на этом пути молча отдаёт 404. Первый полный прогон (203 тикера, 379 заявок с 2026-07-01) вернул 0/379 непустых `acceptance_datetime` без единой ошибки в логе — баг был найден только через прямую 5-вариантную пробу URL (`analysis/edgar_acceptance_debug.py`), не догадкой. После фикса (`analysis/edgar_8k_fetch.py::get_acceptance_datetime`), подтверждённого прямым вызовом функции (не только пробой URL): **379/379 заявок теперь имеют реальный `acceptance_datetime_utc_naive`**, 203/203 тикеров обработаны (реестр — весь ончейн-найденный список сток-токенов, не старые 9), 186/203 CIK разрешены, 0 ошибок, runtime ~187с. `data/p3_guard_cache/edgar_8k_result.json` готов как источник для Query 3 (реакция сток-токена на 8-K, пока акция не торгуется в выходные — план не выполнен, только вход-данные теперь есть).

**Query 3 (реакция сток-токена на 8-K в тёмном окне выходных) — ЗАКРЫТО, N=0, до похода в Dune.** Тёмное окно определено дословно (владелец): пятница 20:00 ET → воскресенье 20:00 ET + праздники NYSE (`analysis/edgar_8k_fetch.py::is_dark_window`, алгоритмический расчёт праздников, не список дат из памяти). Из 379 реальных заявок 8-K (203 тикера, покрытие 2026-07-01 → 2026-09-04, `acceptance_datetime_utc_naive` подтверждён для всех после фикса выше) — **0 (ноль) заявок имеют acceptance_datetime внутри тёмного окна.** Проверено надёжно, не одной цифрой: распределение по дням недели ET строго понедельник–пятница (72/65/103/92/47, суббота/воскресенье — 0/0); 4 заявки с наибольшим часом (19:16–20:26 ET) — все понедельник/среда, ни одна не пятница вечером — не пограничный артефакт классификации. **Причина — структурная, не баг**: 8-K подаются заявителями/системами подачи EDGAR в будние дни; расширение окна дат назад не помогло бы — сток-токены задеплоены единым батчем 2026-07-27 (см. §1 выше), ончейн-цены до этой даты не существует физически, так что более раннее EDGAR-покрытие не добавило бы проверяемых событий. **По прямой инструкции владельца («если N<20 — доложить честно без выводов, не подгонять фильтры под целевой N»): N=0 << 20, линия закрывается на этом шаге БЕЗ похода в Dune за пулами/ценами** — считать нечего, тестировать нечего. Скрипт: `analysis/edgar_8k_fetch.py` (уже написан, нового кода не потребовалось — фильтрация выполнена напрямую по уже собранным данным).

## Правила работы с Dune (оплачены ~1400 кредитами ошибок)

1. Сырые данные не покидают Dune: наружу через API — только агрегаты. Чтения биллятся по объёму.
2. Санитарная проверка ДО исполнения: оценка >40 кредитов ИЛИ множественное сканирование тяжёлых CTE = жёсткий стоп.
3. Отклонение факта от оценки >2x = немедленный стоп сессии.
4. Смоук-тест на одном дне данных до любого полнопериодного прогона.
5. Постоянный кэш `data/sprint*_cache/` с коммитом сразу после каждой оплаченной операции.
6. Бюджетный гард: `data/credits_spent.json`, проверка до КАЖДОЙ платной операции, лимит на спринт в конфиге. Лимит поднимается только явным решением владельца, максимум дважды.
7. Состояние цикла (2026-09-01): потрачено 2138.84 из 2500, сброс 14.09.2026. Резерв 20 неснижаемый. Мультиаккаунты запрещены.
8. Пороги в деньгах, не в голых кредитах: операции дешевле ~$1 не требуют доклада штабу.
9. **Новый Dune-аккаунт "Mozila" (2026-09-04, `secrets.DUNE_API_KEY_MOZILA`) — правила те же + два дополнительных (владелец):** (а) каждый НОВЫЙ запрос сначала на `LIMIT 100` для проверки синтаксиса/структуры, полный прогон — только вторым шагом; (б) фактическая стоимость каждого запуска — в `data/credits_spent_mozila.json` (отдельный от старого леджер, коммитится сразу после каждой платной операции, тот же механизм `credit_guard.py`, см. `CREDIT_GUARD_FILE`/`CREDIT_GUARD_NAMESPACE=funding_mozila`) — остаток виден в файле в любой момент, не должен обнаружиться нулём внезапно. Разведочный бюджет предзарегистрирован явно (`RECON_BUDGET=50` в `analysis/dune_mozila_recon.py`) — расходы сверх разведки требуют отдельного решения владельца, не подразумеваются автоматически.

## Протокол микро-лайва ($100) / P5 live

Логировать обязательно: полная стоимость раунд-трипа (газ входа+выхода, комиссия пула); слиппедж; задержки; каждый ханипот/реверт; отказы фильтра; ежедневная сверка балансов бот↔чейн. С первого дня: лимит дневного убытка, кап позиции, стоп по серии фейлов, стоп по расхождению факт/модель. Для P5 конкретно: kill-порог 30%/год после издержек (§1), буфер свободной маржи ≥25% при каждом открытии/ребалансе (§4, баг №1), проверка `_verify_hedge_filled` перед тем как считать хедж успешным (§4, баг №2).

## Дисциплина моделей и ролей

Штаб (чат Claude): стратегия, гейты, интерпретация — Fable/Opus + расширенное мышление. Исполнение (Claude Code): Sonnet 5, effort обычный. Рантайм-классификация (будущее): Haiku, батчи. LLM в горячем пути исполнения сделок запрещена. Любой бюджет сопровождается механизмом принудительного стопа, не обещанием.

## Консолидация активов в USDG — ЗАКРЫТО (2026-09-05, владелец завершил остаток вручную)

Base-нога (cbBTC→WETH, USDC→WETH) исполнена агентом и независимо верифицирована (см. ниже). Мост Base→Robinhood и финальный своп WETH→USDG на пуле P5 владелец **завершил самостоятельно** ("все обменял, эта задача закрыта", 2026-09-05) — код для Robinhood-ноги (`asset_consolidation_execute_robinhood.py`) не понадобился, не писался. Задача закрыта по прямому указанию владельца, остаток консолидации вне зоны дальнейших действий агента.

Владелец запросил консолидацию всех активов в USDG на Robinhood Chain. Dry-run + план с реальными котировками выполнены (`analysis/asset_consolidation_dryrun.py`, `analysis/asset_consolidation_plan_v2.py`, только чтение — **ни одной транзакции не отправлено**).

**Было (dry-run, реальные балансы кошелька `0x893f...08e75`, 2026-09-05T14:37Z):** Robinhood — ETH 0.002773 (~\$6.81), USDG 0.282426, WETH 0.060745 (~\$149.18); Base — ETH 0.000239 (~\$0.59), USDC 43.839028, cbBTC 0.0004529 (~\$36.02); Arbitrum/Ethereum mainnet — 0 везде. **Итого ~\$236.72.**

**План (v1 заблокирован — Across `suggested-fees` для Base→Robinhood USDC→USDG падает "Unsupported token address on given destination chain", хотя `available-routes` маршрут перечисляет; v2 обходит через WETH, владелец, 2026-09-05):**
1. Base: cbBTC → WETH, самый ликвидный пул (`0x70acdf2ad0bf2402c957154f944c19ef4e1cbae1`, TVL \$18.76M, fee 0.25%) → ~0.014632 WETH.
2. Base: USDC → WETH, самый ликвидный пул (`0x6c561b446416e1a00e8e93e221854d6ea4171372`, TVL \$116.24M, fee 0.30%) → ~0.017798 WETH.
3. Across мост WETH Base→Robinhood — **реальная котировка получена, маршрут работает** (в отличие от USDC→USDG): totalRelayFee=0.279%, вход 0.032429 WETH → выход ~0.032339 WETH на Robinhood.
4. Robinhood: остаток нативного ETH сверх \$0.50-резерва (0.002569 ETH, ~\$6.31) — дороже двух транзакций (~\$0.28 газ) → обернуть в WETH.
5. Robinhood: **ОДИН своп** — существующие 0.060745 WETH + 0.032339 WETH с моста + 0.002569 WETH обёрнутый остаток = 0.095653 WETH (~\$234.90) → USDG на пуле P5 (`0x52e65B17...`, fee 0.01%) → ~234.880 USDG.
6. Lighter — не трогать.

**Станет (оценка по спот-цене, БЕЗ полной v3-симуляции проскальзывания — суммы малы относительно TVL всех участвующих пулов):** Robinhood — ETH ~\$0.50 (резерв), USDG ≈ 0.282 + 234.880 = **≈235.16**, WETH 0; Base — ETH ~\$0.59 (не трогается, оставлен на газ), USDC 0, cbBTC 0. **Итого ≈\$236.25, суммарные оценочные издержки ≈\$0.61** (fee свопов \$0.09+\$0.13+\$0.02, мост \$0.22, газ на wrap ~\$0.14) — **из \$236.72 (≈0.26%)**.

**Владелец дал "да" на свапы (2026-09-05, дословно: "ок,даю добро на свапы") и сообщил, что USDG с Lighter уже выведен вручную на кошелёк** — реальный перечитанный баланс Robinhood USDG подскочил 0.282426 → **62.142426**, итог по всем активам ~\$298.73 (был ~\$236.72). Обновлённая финальная таблица будет построена по РЕАЛЬНЫМ прочитанным балансам после исполнения, не по этой оценке.

**Исполнение Base-ноги (`analysis/asset_consolidation_execute_base.py`) — router на Base реально подтверждён** (`0x2626664c2603336E57B271c5C0b26F421741e481`, bytecode + `factory()` совпадает с каноническим Uniswap V3 Factory на Base). Реальный ход событий:
- Run 33975314247: "no-deadline" 7-поле структура `exactInputSingle` провалилась на `eth_call`-симуляции. **0 газа потрачено.**
- Run 33975504003: добавлен fallback на 8-поле "with-deadline" структуру — **тоже провалилась**, диагноз выглядел как "обе ABI-структуры неверны". **0 газа потрачено.**
- Run 33975825755 (фикс approve-перед-simulate): **реальная транзакция `cbbtc_to_weth_approve` УСПЕШНО подтверждена on-chain** (tx `0x455df58172b6de2dce54c133629474cb3735c9f72d4c5f4f3015b3b1bbef0cfb`, gas_used=59940, ~\$0.0009 — первая реальная трата этой консолидации). Allowance реально выдан — но `exactInputSingle` **всё равно** провалился на обеих ABI-структурах.
- **Настоящая причина найдена (run 33976249528, диагностика):** на каноническом Uniswap V3 Factory (`0x33128a8fc17869897dce68ed026d694621f6fdfd`) для пары cbBTC/WETH **НЕТ пула на fee=2500 (0.25%)** — `factory.getPool()` вернул нулевой адрес. План v2 брал адрес пула `0x70acdf2ad0bf2402c957154f944c19ef4e1cbae1` (TVL \$18.76M, "fee 0.25%") из offchain-данных (Dune/GT) — либо это другой DEX/фабрика на Base, либо неверная атрибуция; на каноническом Factory её просто нет. Реально существующие пулы для этой пары: fee=100, fee=500, fee=3000 (has_code=True). Approve не пропал зря — allowance на router уже выдан для полного баланса cbBTC, пригодится для правильного fee tier.
- Попутно исправлен второй реальный баг: `RuntimeError` в `rpc()` клал params (длинный calldata) перед телом ошибки, а `str(exc)[:300]` обрезал именно ошибку — настоящий revert reason никогда не был виден в логах. Исправлено.
- Диагностика перезапущена с retry-on-429 и реальным сравнением резервов (run 33976559782, **успех**): cbBTC/WETH самый ликвидный tier — **fee=3000** (2524 WETH резерва, пул `0x8c7080564b5a792a33ef2fd473fba6364d5495e5`), и `eth_call`-симуляция `exactInputSingle` на нём реально прошла (output=0.0146488 WETH — почти точно совпадает с оценкой plan_v2 ~0.014632 WETH). USDC/WETH: fee=3000 подтверждён тем же (19484 WETH резерва) — адрес пула не менялся, был верным изначально.
- `CBBTC_WETH_POOL_FEE` в коде исправлен 2500→3000, execute_base.py триггернут повторно.

**Run 33976972830 — ОБА свопа Base РЕАЛЬНО УСПЕШНЫ on-chain:**
- `cbbtc_to_weth_swap`: tx `0x1d383012aab9750ea6bff6d5d5d450492b7980477e4c38259e649da4348350ec`, gas_used=134127, SUCCESS.
- `usdc_to_weth_approve`: tx `0xaa376676418c5178fe891ee6a4fb6ae5752e3f62e67fd0b3ca108343cae3db4b`, gas_used=55449, SUCCESS.
- `usdc_to_weth_swap`: tx `0x92c5541056508a14de7730a92e7af8007bc5bc93a4a3d839524bd747cdc3cd60`, gas_used=113277, SUCCESS.

**Но напечатанный "финальный баланс WETH" (0.014648849538673675) оказался недостоверен** — он бит-в-бит равнялся ТОЛЬКО симулированному output cbBTC-ноги, не учитывал реально прошедшую USDC-ногу. Похоже на лаг публичной RPC-реплики `mainnet.base.org` сразу после последней транзакции. **Не выдали это чтение за факт** — независимая верификация (`asset_consolidation_base_verify.py`, run 33977421746, только чтение) подтвердила реальную причину и дала окончательный ответ:

**BASE-НОГА ЗАВЕРШЕНА, реально подтверждено (`matches: true` — сумма Transfer-логов из квитанций совпала с живым перечитанным балансом):**
- cbBTC→WETH: 0.014648849538674 WETH (из квитанции tx `0x1d383012...`)
- USDC→WETH: 0.017801561015033 WETH (из квитанции tx `0x92c55410...`)
- **Итого реальный баланс WETH на Base сейчас: 0.03245041055370624**
- cbBTC и USDC на Base теперь оба **0** (полностью свопнуты)

5-секундная пауза перед финальным чтением баланса добавлена в `execute_base.py` на будущее (пригодится для Robinhood-ноги — тот же класс риска реплика-лага).

**Следующий шаг (ждёт решения, не начат):** получить свежую котировку Across для моста Base→Robinhood WETH→WETH на реальную сумму 0.03245041 WETH (предыдущая котировка в plan_v2 была на оценочные 0.032429 WETH — почти точно совпадает, но нужна свежая котировка на фактическую сумму перед реальным мостом).

Полные результаты — `data/p3_guard_cache/asset_consolidation_dryrun_result.json`, `data/p3_guard_cache/asset_consolidation_plan_v2_result.json`. Pre-flight чтение баланса перед каждой транзакцией и финальная таблица (прочитанная, не расчётная) — после исполнения.

## Ключевые артефакты

`docs/RESULTS.md` — результаты закрытых спринтов. `docs/COST_POSTMORTEM.md` — разбор перерасходов Dune. `docs/P5_HEDGED_LP.md`, `docs/P5_LIVE_DEPLOY.md` — дизайн и план деплоя P5. `docs/P3_GUARD.md`, `docs/P4_RECON.md`, `docs/P4_KILL.md` — P3/P4. `data/credits_spent.json` — счётчик кредитов Dune. `data/p5_live_position_state.json` — состояние живой позиции (§1). `data/p5_live_log.jsonl` — журнал событий P5. `data/p3_guard_cache/` — все реальные результаты прогонов этой сессии (не эфемерный кэш — коммитится).

## P5 LIVE — секреты засвечены в переписке штаба (2026-09-03), протокол ротации

При настройке первого VPS (216.146.25.26) владелец передал SSH-приватный ключ напрямую в переписке штаба — тот же канал, где ранее передавались `ALCHEMY_API_KEY` и ключи Lighter. **SSH-ключ VPS и `PRIVATE_KEY_NOX` считаются засвеченными.** Пока прогон остаётся микро-размером — риск принят явно владельцем.

**Жёсткое условие на будущее (владелец, дословно): при масштабировании капитала выше микро-прогона — хост, SSH-ключ и ВСЕ секреты (`PRIVATE_KEY_NOX`, `LIGHTER_API_KEY_PUBLIC`/`LIGHTER_API_KEY_PRIVATE`) пересоздаются с нуля**, не переносятся. Проверяется ДО повышения размера позиции, не постфактум.
