from pathlib import Path
import json,datetime,collections
P=Path(__file__).parent;a=json.loads((P/'analysis.json').read_text());m=a['meta'];r=json.loads((P/'selected.json').read_text());fees=json.loads((P/'fees.json').read_text())
labels={5:'5 с',15:'15 с',30:'30 с',60:'1 мин',180:'3 мин',300:'5 мин',900:'15 мин',3600:'1 ч',21600:'6 ч',86400:'24 ч'}
def pct(v):return '—' if v is None else f'{v*100:+.2f}%'
s=f'''# 100 покупок Beqv6dz…Xqeit больше 500 USDC

**Отбор 100 покупок завершён. Полный ценовой расчёт 100 случаев не завершён.** Проверены 768 успешных транзакций; отобраны последние 100 подходящих покупок из зафиксированной истории. Цена восстановлена для **140 из 1 000 точек**; все шесть точек от +5 секунд до +5 минут есть у **17 покупок**.

Выборка: **23 входа с нулевого остатка, 77 докупок, 39 токенов**. Период — **16 сентября 2026, 03:23:13 — 17 сентября 2026, 19:06:29 UTC**. Суммарный фактический расход лидера — **193 659,82 USDC**, размеры покупок — от **507,37 до 9 664,56 USDC**. USDC≈USD — рабочее допущение, историческая долларовая привязка отдельно не проверялась.

**Почему остановилось получение данных.** Новые RPC-запросы завершились ошибкой `curl: (56) CONNECT tunnel failed, response 403`. Это сетевое ограничение доступа, а не полученный от Solana ответ 429 о частоте запросов. После ошибки новые сетевые запросы были остановлены; оставшаяся обработка использовала сохранённые данные.

## Полнота

| Статус точки | Число |
|---|---:|
| Проверенная цена | 140 |
| Горизонт ещё не наступил на момент расчёта | 66 |
| Не загружены необходимые данные RPC | 727 |
| GatorSwap: нет восстановленной модели цены | 58 |
| Требуется продолжение в пуле после миграции LaunchLab | 8 |
| Не подтверждена актуальность курса промежуточного токена | 1 |
| **Всего** | **1 000** |

Все 100 покупок остаются в таблице. Отсутствующие цены не заменялись нулевым изменением и не замещались другими, удобными для расчёта покупками. Шесть покупок через GatorSwap относятся к одному mint; два из их 60 горизонтов ещё не наступили, поэтому в строке GatorSwap указано 58 точек.

## Доступные результаты по горизонтам

Каждая строка относится только к своему N, а не к 100 покупкам. Из-за незавершённой загрузки меняется состав наблюдений; сравнивать средние этих строк как поиск лучшего срока выхода нельзя.

| Горизонт | Есть цена / 100 | Среднее к цене покупки | Медиана | Доля положительных |
|---|---:|---:|---:|---:|
'''
for x in a['stats']:
 if x['group']=='all':s+=f"| {labels[x['seconds']]} | {x['n']} / 100 | {pct(x['mean'])} | {pct(x['median'])} | {x['positive_share']*100:.1f}% |\n" if x['n'] else f"| {labels[x['seconds']]} | 0 / 100 | — | — | — |\n"
s+='''
## Сравнение на одном и том же составе: 17 покупок

Это 7 входов с нулевого остатка и 10 докупок. Таблица использует один и тот же состав для всех шести горизонтов; это частичная выборка, определённая доступностью данных, а не независимая проверка торгового правила.

| Горизонт | Среднее к цене покупки, все 17 | Медиана, все 17 | Среднее от котировки +5 с, все 17 | От +5 с, 7 нулевых входов | От +5 с, 10 докупок |
|---|---:|---:|---:|---:|---:|
'''
co={(x['group'],x['seconds']):x for x in a['cohort']}
for sec in [5,15,30,60,180,300]:
 x=co[('all',sec)];s+=f"| {labels[sec]} | {pct(x['leader']['mean'])} | {pct(x['leader']['median'])} | {pct(x['after_5s']['mean'])} | {pct(co[('zero',sec)]['after_5s']['mean'])} | {pct(co[('add',sec)]['after_5s']['mean'])} |\n"
s+='''
На этих 17 случаях наибольшее среднее из шести показанных горизонтов — на +30 секунде. Это не устанавливает лучший выход для всех 100 покупок и не подтверждает достижимость входа по цене лидера. Кроме того, 39 из 100 покупок — один mint `6GmAFSYs4gk3FDao5FzzySQpPZaWsa4rUJHacpMpUNgx`, все с положительным остатком до покупки: сравнение групп зависит и от состава токенов.

## Что означают цены и проценты

Цена входа — фактический расход USDC, делённый на чистое количество токенов, поступившее кошельку. Поэтому комиссия и потери на входе, уже отражённые в этих двух величинах, учитываются в средней цене; повторно вычитать их из этого сравнения нельзя.

Последующая цена — предельная котировка выбранного пула, пересчитанная в USDC через исторические котировки промежуточных активов. Для многопулового маршрута берётся один зафиксированный пул, а не взвешенная котировка всего маршрута; его адрес указан в таблице. Формула основного процента: `цена_горизонта / фактическая_средняя_цена_покупки − 1`. Разница со средней ценой входа может включать воздействие самой покупки лидера на пул, а не только последующее движение. Это не фактически реализованная прибыль лидера или копировщика: продажа всего объёма, её комиссии, налоги на перевод токенов, проскальзывание, tips и сетевые расходы не моделировались.

Показатель «к +5 с» сравнивает две котировки: `цена_горизонта / цена_+5с − 1`. Он показывает оставшееся движение цены, но не является моделью исполненной покупки и продажи. Вход сразу после лидера и вход в следующем блоке в этом пакете отдельно не моделировались.

Время определяется фактическим `blockTime`, с секундной точностью. Используется последнее подтверждённое состояние пула на конец блока с временем не позже целевой точки; возраст цены и каждого промежуточного курса сохранён. Пропущенные слоты не переводились в секунды через постоянные 400 мс.

«Нулевой остаток» означает нулевой баланс по участвовавшим в транзакции токен-счетам владельца перед покупкой. Повторный вход после полной продажи также относится к этой группе; это не обещание первой покупки токена за всю историю. Для необычного хранения на нескольких неучаствующих счетах потребуется отдельное восстановление полного исторического остатка.

## Комиссии и проверка

Для 94 покупок указаны комиссия из известной конфигурации с наблюдавшимся creator fee либо фактические суммы комиссий из событий обмена. Для 6 покупок GatorSwap ставка не восстановлена. Ставки Meteora динамические: приведённая доля относится к данному обмену, а не ко всем будущим покупкам и продажам.

В выборке встречаются 1,25% на основной площадке Raydium/LaunchLab, 4% Raydium CLMM и динамическая комиссия Meteora около 5,85% у одного входа. Поэтому небольшое положительное движение котировки ещё не означает положительный результат продажи; расходы остальных звеньев маршрута и перевода токенов необходимо учитывать отдельно.

Проверены 310 последовательных переходов резервов CPMM в сохранённых блоках: расхождений не обнаружено. Для новых котировок проверялись наличие событий соответствующих вызовов обмена и порядок транзакций; обрезанные или нераспознанные события не должны превращаться в утверждение «обмена не было».

В новой таблице присутствуют прежние WOW, CRACKER, RUNNER и повторный вход CLANKER 17 сентября. Семь заново рассчитанных точек CLANKER совпали с прежним расчётом точно. Ещё 23 проверенные ценовые точки перенесены из прежних сохранённых расчётов; одна старая точка CRACKER +15 минут отмечена как неподтверждённая по актуальности промежуточного курса и исключена из статистики. Первый исторический вход CLANKER 15 сентября находится раньше окна этих 100 покупок.

Поток чужих сделок до и после входов для всей новой сотни не восстановлен. Числа в этой версии относятся к покупкам лидера и доступным котировкам.

## Файлы

`buyer_100_purchases.xlsx`: сводка с покрытием, все 100 покупок, все 1 000 горизонтов со статусами и источниками. `buyer_100_checkpoint.zip`: код, покупки, журналы отбора, комиссии, результаты, схемы событий и состояние для продолжения. `buyer_100_blocks_*.zip`: полные новые блоки; список частей и их содержимого — `raw_manifest.json`. Прежние полные блоки остаются в ранее сохранённых архивах `three_tokens_evidence.zip` и `WOW_evidence.zip`.
'''
(P/'buyer_100_status.md').write_text(s)
c=json.loads((P/'checkpoint.json').read_text());c.update(status='blocked_network_access',selected_so_far=100,zero_balance=23,checked_successful_transactions=768,price_status_counts=m['point_status'],fully_priced_to_5min=17,asof_utc=m['asof_utc'],error='CONNECT tunnel failed, response 403',resume='Run select.py only if selection changes. To resume prices: python price_batch.py --workers 4; retain selected.json unchanged. Regenerate offline/merge_prior/analysis/report only as needed; do not overwrite verified prior points with missing statuses.');(P/'checkpoint.json').write_text(json.dumps(c,indent=2))
print('report saved')
